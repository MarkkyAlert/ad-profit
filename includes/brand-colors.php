<?php

declare(strict_types=1);

/**
 * ⭐⭐ จุดเดียวของ "สีปุ่ม" ทั้งระบบ
 *
 * ⚠️⚠️ ทำไมต้องมีไฟล์นี้ — เพราะเคยพลาดมาแล้วแบบเต็ม ๆ:
 * รอบที่แก้คอนทราสต์ปุ่ม แก้ที่ `includes/header.php` ที่เดียว แล้วคิดว่าจบ
 * แต่ `login.php` · `forgot-password.php` · `reset-password.php` **ไม่ได้ใช้ header.php**
 * (ทั้งสามหน้ามี `<head>` ของตัวเอง) และนิยาม `.btn-orange` ซ้ำไว้เองด้วยสีชุดเก่า
 * ผลคือ **ปุ่ม "เข้าสู่ระบบ" ซึ่งเป็นปุ่มแรกสุดที่ทุกคนกด ยังตกเกณฑ์ที่ 2.80 : 1**
 * ขณะที่ปุ่มหน้าอื่นผ่านหมด — และแอปมีสีส้มสองเฉดพร้อมกัน ซึ่งแย่กว่าตอนก่อนแก้
 *
 * ⚠️ ไฟล์นี้คุมเฉพาะ **สี** ไม่คุมขนาด/ระยะขอบ — แต่ละหน้ายังจัดวางปุ่มเองได้ตามเดิม
 * (หน้าเข้าสู่ระบบใช้ปุ่มเต็มความกว้าง หน้าอื่นใช้ปุ่มพอดีข้อความ)
 *
 * ⚠️ ทุกค่าในนี้ต้องได้คอนทราสต์กับตัวอักษรสีขาว ≥ 4.5 : 1
 * `SharedHelperContractTest::testWhiteTextOnColouredButtonsIsDarkEnough()` คำนวณจริงจากไฟล์นี้
 * และมีตัวกวาดคู่กันห้ามไฟล์อื่นเขียนสีปุ่มเอง
 *
 * ⚠️ `error.php` **ไม่ใช้ไฟล์นี้โดยตั้งใจ** — มันต้องขึ้นได้แม้ตอนระบบพังจนตอบ 500
 * จึงห้ามพึ่ง include ใด ๆ (เหตุผลเดียวกับที่มันไม่เรียก bootstrap.php)
 */
?>
<style>
    :root {
        /* ⚠️ คอมเมนต์ในไฟล์นี้ห้ามเขียนข้อความที่ปรากฏบนหน้าจอแบบคำต่อคำ
           ไฟล์นี้ถูกส่งไปกับทุกหน้ารวมถึงหน้าที่ยังไม่ได้ล็อกอิน · เทสต์ที่ตรวจว่า
           "คนที่ยังไม่ล็อกอินต้องไม่เห็นเมนูของคนที่ล็อกอินแล้ว" ค้นด้วยข้อความตรง ๆ
           เขียนชื่อปุ่มลงคอมเมนต์จึงทำให้เทสต์แดงโดยที่หน้าเว็บไม่ได้ผิดอะไร (เจอมาแล้ว)
           — และการทำให้เทสต์นั้นฉลาดขึ้นไม่คุ้ม เพราะความ "โง่" ของมันคือสิ่งที่ทำให้จับได้จริง */

        /* ส้ม = ปุ่มหลักของทั้งระบบ */
        --btn-orange-from: #c2410c;
        /* 5.18 : 1 */
        --btn-orange-to: #9a3412;
        /* 7.31 : 1 */

        /* ม่วง–คราม = ปุ่มงานรอง */
        --btn-primary-from: #4f46e5;
        /* 6.29 : 1 */
        --btn-primary-to: #7c3aed;
        /* 5.70 : 1 */

        /* ฟ้า = ปุ่มดาวน์โหลด/ตั้งเป้า */
        --btn-teal-from: #0e7490;
        /* 5.36 : 1 */
        --btn-teal-to: #0369a1;
        /* 5.93 : 1 */

        /* แดง = ปุ่มที่ทำลายข้อมูล */
        --btn-danger-from: #dc2626;
        /* 4.83 : 1 */
        --btn-danger-to: #991b1b;
        /* 8.31 : 1 */
    }
</style>
