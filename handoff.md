# Handoff — Ad-Profit (ระบบบันทึกรายได้/ค่าโฆษณา/กำไรรายวัน แยกตามร้าน)

> **ประเภท:** working context ภายในทีม (ไม่ใช่เอกสารลูกค้า)
> **สร้างเมื่อ / state ล่าสุด:** 2026-08-06 — เพิ่งจบรอบตรวจ-แก้บั๊กรอบที่ 17
> คอมมิตล่าสุด `087ca67` (docs) · CI เขียว · `master` sync กับ `origin/master` แล้ว
> ชุดเทสต์ที่รันเต็มครั้งล่าสุดในเซสชัน: **1043 tests OK · PHPStan clean** (ที่ state ของคอมมิต `3bbd3a2`; คอมมิตถัดมาแตะแค่ `CLAUDE.md`)
> **กติกาอ่าน:** `Unknown` = ไม่พบข้อมูล · `Need confirmation` = ต้องยืนยันก่อนใช้

---

## 1. Project Overview

- ระบบบันทึกรายได้ / ค่าโฆษณา / กำไรรายวัน แยกตามร้าน (multi-shop)
- **PHP แบบไฟล์ล้วน ไม่มี framework** · MySQL/InnoDB · server-side render (Tailwind + Chart.js ผ่าน CDN, ไม่มี build step)
- Layer: `Page/API (controller) → Service → Repository → MySQL` · manual DI ไม่มี container
- รายละเอียดสถาปัตยกรรม กติกา และกับดักทั้งหมดอยู่ใน **`CLAUDE.md`** (ไฟล์นี้คือแหล่งความจริงหลัก — อ่านก่อนแก้โค้ดทุกครั้ง)
- **งานที่ทำอยู่ช่วงนี้:** loop ตรวจสอบตรรกะ (logic review) เป็นรอบ ๆ แล้วแก้บั๊กที่พบ · ทำมาถึงรอบที่ 17

---

## 2. Current Status

### เสร็จแล้ว (มีคอมมิต + เทสต์ยืนยัน)
- แก้บั๊กที่ผู้ใช้เจอจริงไป **26 คอมมิต** ในช่วง 2026-08-05 → 2026-08-06
- เทสต์เพิ่มจาก 933 → **1043**
- CI ครอบ 5 job: Syntax lint (PHP 8.2), Syntax lint (PHP 8.3), Quality (PHPStan + Unit), Integration (MariaDB 10.4), คำสั่งติดตั้งของ production (PHP 8.3)
- กลุ่มงานที่ปิดไปแล้ว (ดู `git log --since="2026-08-05"` สำหรับรายละเอียดต่อคอมมิต):
  - ความปลอดภัย: `.htaccess` กันไฟล์ภายใน · ตัวจำกัดการเดารหัสผ่าน (reserve-then-act, per-IP release, per-account throttle) · ปิดช่องพาออกนอกเว็บ 2 รอบ (`/\`, แท็บ/ขึ้นบรรทัด)
  - ความถูกต้องของข้อมูล: collation ชื่อร้าน (อิโมจิ) · ตารางกรอกหลายวันรับตัวเลขที่ทางอื่นปฏิเสธ · วันล่วงหน้าทำให้หลายหน้าบอกตัวเลขไม่ตรงกัน
  - บัญชีผู้ใช้: ยืนยันอีเมลก่อนเปลี่ยน · ลำดับล็อก `users` → token/คำขอ
  - เอกสาร: `docs/DEPLOYMENT.md` แก้ 7 จุดที่พาไปผิดทาง

### ยังไม่เสร็จ
- **รอบ 17 ยังปิดไม่ครบ** — ผู้ตรวจ 2 ชุดรายงานรวม ~14 + 4 ข้อ แก้ไปแล้วบางส่วน (ดูข้อ 5)
- ยังไม่เคยได้ verdict 🟢 Logic Passed (คำสั่งเดิมของเจ้าของงานคือให้วนจนกว่าจะ 🟢)

### สถานะล่าสุด
- ผู้ใช้สั่งปิดเครื่อง งานทั้งหมด push แล้ว ไม่มีอะไรค้างใน working tree
- ตรวจก่อนปิดแล้ว: ไม่มีโปรเซสค้าง · ไม่มีล็อกค้างใน MySQL · ฐานข้อมูลจริง (`ad_profit`) มี 0 users / 0 shops / 0 records

---

## 3. Important Decisions

> ทุกข้อระบุผู้ยืนยัน · `[Confirmed]` = เจ้าของงานเคาะแล้ว · `[Proposed]` = AI เสนอ ยังไม่มีคนเคาะ

- **[Confirmed]** เพิ่มเพดานการเดารหัสผ่าน "ต่อบัญชี" แบบ **หน่วงเวลา ไม่ใช่ล็อกบัญชี** — นับเฉพาะรหัสผิด, รีเซ็ตเมื่อสำเร็จ, เพดานหลวมกว่าต่อ IP, ตอบเท่ากันทุกบัญชี
  (ยืนยันโดย: ผู้ใช้ ตอบข้อความ "เพิ่มเพดานต่อบัญชี แบบ throttle (หน่วงเวลา/captcha) นับเฉพาะรหัสผิด รีเซ็ตเมื่อสำเร็จ เพดานหลวมกว่า IP ตอบเท่ากันทุกบัญชี — ไม่ใช่ hard lock")
- **[Confirmed]** รัน `database/migrations/2026-08-05-shop-name-collation.sql` กับฐานข้อมูลจริง
  (ยืนยันโดย: ผู้ใช้ ตอบว่า "รันเลย" · ตรวจแล้วว่ารันจริง — `shops.name` เป็น `utf8mb4_unicode_520_ci`)
- **[Confirmed]** เปลี่ยนอีเมลต้อง **ส่งลิงก์ยืนยันไปที่อีเมลใหม่** ก่อน (จาก 4 ตัวเลือกที่เสนอ)
  (ยืนยันโดย: ผู้ใช้ เลือกผ่าน AskUserQuestion — "ส่งลิงก์ยืนยันไปอีเมลใหม่")
- **[Confirmed]** การ์ด "เดือนกำไรดีสุด/แย่สุด" **ไม่นับเดือนปัจจุบันที่ยังไม่จบ**
  (ยืนยันโดย: ผู้ใช้ เลือกผ่าน AskUserQuestion — "ไม่นับเดือนที่ยังไม่จบ")
- **[Confirmed]** เมื่อยังไม่มีเดือนที่จบ การ์ดแสดงข้อความ **"รอให้จบเดือนก่อน"** แทนขีด (–)
  (ยืนยันโดย: ผู้ใช้ เลือกผ่าน AskUserQuestion)
- **[Proposed — ยังไม่ยืนยัน]** เพิ่มการแจ้งเตือนไปยัง **อีเมลเดิม** เมื่อมีคนขอเปลี่ยนอีเมล — ปิดช่องที่คนยึด session ได้เปลี่ยนอีเมลไปเป็นของตัวเองแล้วยืนยันเอง (AI เสนอ ผู้ใช้ยังไม่ตอบ)
- **[Proposed — ยังไม่ยืนยัน]** ทางแก้ปัญหา "พนักงานล็อกอินพร้อมกันแล้วเข้าไม่ครบ" — เสนอไป 3 ทาง ผู้ใช้ยังไม่เลือก (ดูข้อ 5)

---

## 4. Relevant Files / Locations

> ทั้งหมดเป็น path ที่มีจริงและถูกแตะในเซสชันนี้

| Path | หน้าที่ |
|---|---|
| `CLAUDE.md` | **แหล่งความจริงหลัก** — กติกาสถาปัตยกรรม, business rules, กับดักที่เคยเจอทั้งหมด · อ่านก่อนแก้โค้ดเสมอ |
| `app/Services/AuthService.php` | สมัคร/ล็อกอิน/ลืมรหัส/ตั้งรหัสใหม่ + ตัวจำกัดการเดารหัส (3 bucket) |
| `app/Services/ProfileService.php` | เปลี่ยนชื่อ/อีเมล/รหัสผ่าน + ขอ-ยืนยันเปลี่ยนอีเมล |
| `app/Services/RecordService.php` | บันทึก/แก้/ลบรายการ, month grid, weekday context |
| `app/Services/ShopService.php` | สร้าง/เปลี่ยนชื่อ/ลบร้าน + ชื่อยืนยันตอนลบ |
| `app/Services/AnnualService.php` · `OverviewService.php` · `OverviewDailyService.php` · `OverviewAnnualService.php` · `DashboardService.php` | รายงาน/ตัวเลขที่ผู้ใช้เห็น |
| `app/Repositories/EmailChangeRepository.php` | คำขอเปลี่ยนอีเมลที่รอยืนยัน (คลาสใหม่ในเซสชันนี้) |
| `includes/functions.php` | helper กลาง: `trim_unicode_whitespace()`, `resolve_safe_redirect_path()`, `comparison_range_end()`, `formatMoney()`, schema guard helpers |
| `includes/bootstrap.php` | session, security headers, schema guard, log file |
| `includes/config.php` | ค่าคงที่ทั้งหมดจาก env ผ่าน `config_positive_int()` |
| `verify-email.php` | หน้าปลายทางของลิงก์ยืนยันอีเมล (ไฟล์ใหม่ในเซสชันนี้) |
| `.htaccess` (ราก + 9 โฟลเดอร์) | กันไฟล์ภายในไม่ให้โหลดผ่านเว็บ |
| `database/migrations/` | 3 ไฟล์ — ดูข้อ 6 เรื่องลำดับการรัน |
| `docs/DEPLOYMENT.md` | คู่มือขึ้นเซิร์ฟเวอร์ (แก้ไป 7 จุดในเซสชันนี้) |
| `docs/manual-checks.md` | รายการที่ระบบทดสอบแทนไม่ได้ ต้องลองด้วยมือ |
| `tests/Unit/` · `tests/Integration/` | 1043 tests |

**Environment:** ไฟล์ทั้งหมดอยู่ในเครื่องผู้ใช้ที่ `/Applications/XAMPP/xamppfiles/htdocs/ad-profit` · ไม่ได้รีเซ็ตข้ามเซสชัน (เป็น repo จริงบนเครื่อง ไม่ใช่ sandbox)

---

## 5. PENDING / Work in Progress

### 5.1 รอบ 17 — ข้อที่ผู้ตรวจรายงานแต่ยังไม่ได้แก้

> มาจากผู้ตรวจ 2 ชุดในรอบ 17 · **ยังไม่มีใครยืนยันว่าแต่ละข้อเป็นบั๊กจริง** นอกจากที่ระบุว่า "วัดจริงแล้ว"
> แนะนำให้ทำซ้ำเองก่อนแก้ (ดูข้อ 7 เรื่องวิธีทำงาน)

| # | เรื่อง | สถานะหลักฐาน |
|---|---|---|
| A | แก้วันที่ของรายการเป็นเดือนอนาคต → ขึ้น "แก้ไขเรียบร้อยแล้ว" แต่รายการหายจากตาราง และ **ลบไม่ได้ผ่านหน้าเว็บอีกเลย** | ผู้ตรวจระบุว่าวัดจริง (ยิง `history.php?month=2026-11` ได้ 200 แต่ตัวเลือกเดือนโชว์ 2026-08) — ยังไม่ได้ทำซ้ำเอง |
| B | บอก "สำเร็จ" ทั้งที่ส่งอีเมลไม่ได้ — 2 จุด (ลืมรหัสผ่าน, เปลี่ยนอีเมล) · เปลี่ยนอีเมลขึ้นแถบ **เขียว ✅ คู่กับข้อความว่าล้มเหลว** | ผู้ตรวจอ้าง path:line — ยังไม่ได้ทำซ้ำเอง |
| C | CSRF หมดอายุตอนตั้งรหัสใหม่ → เด้งไปหน้าที่ขอลิงก์ใหม่ไม่ได้ + ข้อความเป็นภาษาอังกฤษ (`Invalid CSRF token`) ผิดกติกาโปรเจกต์ | ผู้ตรวจอ้าง path:line — ยังไม่ได้ทำซ้ำเอง |
| D | แดชบอร์ดกับหน้าประวัติรายการบอกยอด "เดือนนี้" คนละตัว (`getMonthlyRecords()` ไม่มี seam `$today`) | ผู้ตรวจระบุว่าวัดจริง · **[ต้องคนตัดสิน]** ว่าจะให้ตัดที่วันนี้ หรือยอมต่างแล้วติดป้าย |
| E | ป้าย ↑/↓ % ในไฟล์ Excel กับหน้าเว็บไม่ตรงกันที่ค่าใกล้ศูนย์ (Excel ตัดสินจากค่าดิบ หน้าเว็บตัดสินจากค่าที่ปัดแล้ว) | ผู้ตรวจระบุว่าวัดจริง |
| F | `GoalService::deleteGoal()` ไม่จองแถวร้านก่อน (ทางเขียนทางเดียวที่ตกสำรวจ) — CLAUDE.md อ้างว่า "ครบทั้ง 5 ทาง" | ผู้ตรวจอ้าง path:line |
| G | ลบร้านที่ใช้อยู่ → ระบบย้ายไปร้านอื่นเงียบ ๆ ไม่บอกชื่อร้านปลายทาง (ขัดกฎที่เขียนไว้ใน CLAUDE.md) | ผู้ตรวจอ้าง path:line |
| H | ฟอร์มใน `shops.php` ไม่มี `shop_context_field()` → แท็บที่เปิดค้างเขียนชื่อร้านเก่าทับของใหม่ได้ | ผู้ตรวจอ้าง path:line |
| I | `api/auth.php` (register/login) ไม่มีด่านกันคนที่ล็อกอินอยู่แล้ว | ผู้ตรวจอ้าง path:line |
| J | เปลี่ยนอีเมลยิงจดหมายไปที่อยู่ใดก็ได้ไม่จำกัด (ตัวจำกัดนับเฉพาะตอนรหัสผิด + ล้างถังเมื่อสำเร็จ) | ผู้ตรวจอ้าง path:line |
| K | `verify-email.php` ไม่อยู่ใน `phpstan.neon`, `PageRenderTest`, `BrowserScriptParityTest` — และ CLAUDE.md อ้างว่า PHPStan "ครอบทุกไฟล์ (177 ไฟล์)" ซึ่งไม่ตรงแล้ว | ผู้ตรวจตรวจไฟล์ config จริง |
| L | `.htaccess` กฎนามสกุลเป็น case-sensitive (`/CLAUDE.MD` โหลดได้บน filesystem ที่ไม่แยกตัวพิมพ์) | **ไม่กระทบ Linux/Hostinger** — เป็นข้อควรแข็งขึ้น |
| M | `EmailChangeRepository::findPendingByUserId()` ไม่ถูกเรียกจากที่ไหน (หน้าโปรไฟล์ไม่แสดงว่ามีคำขอค้าง) | ผู้ตรวจอ้าง path:line |
| N | `shops.php` สร้าง `ShopService` โดยไม่ส่ง `$pdo` → ทางเขียนในอนาคตจะรันโดยไม่มีล็อกแบบเงียบ ๆ | ผู้ตรวจอ้าง path:line |

### 5.2 เรื่องที่รอเจ้าของงานตัดสิน (บล็อกอยู่)

**พนักงานที่กดล็อกอินพร้อมกันเข้าได้ไม่ครบ** — วัดจริงแล้ว (8 คน รหัสถูกทุกคน IP เดียวกัน เพดาน 5):

| จังหวะ | เข้าได้ |
|---|---|
| ทีละคน | 8/8 ✓ |
| ห่างกัน 0.15 วิ | 5/8 |
| พร้อมกันเป๊ะ | 2–4/8 |

สาเหตุ: ต้องจองโควตา *ก่อน* ตรวจรหัสผ่าน (จำเป็นเพื่อกันการยิงพร้อมกันหลบด่าน) แล้วคืนหลังตรวจเสร็จ ~0.5 วิ
ทางเลือกทั้ง 3 (พร้อมข้อเสียของแต่ละทาง) บันทึกไว้ใน `CLAUDE.md` แล้ว — **ต้องให้เจ้าของระบบเลือก ห้ามแก้เงียบ ๆ เพราะทุกทางลดความปลอดภัยลงคนละแบบ**

---

## 6. Known Issues / Risks

### 6.1 ⚠️ ฐานข้อมูลในเครื่องยังไม่ได้รัน migration ตัวที่ 3
- ตรวจแล้ว: ตาราง `email_change_requests` **ไม่มี** ใน `ad_profit` (local)
- ผลคือ ทดสอบเปลี่ยนอีเมลบนเครื่องจะขึ้นว่า **"ระบบเปลี่ยนอีเมลยังไม่พร้อมใช้งาน"**
- แก้: `mysql -u USER -p ad_profit < database/migrations/2026-08-05-email-change-requests.sql`

### 6.2 ⚠️ ลำดับ deploy สำคัญมาก (อยู่ใน `docs/DEPLOYMENT.md` แล้ว)
1. สำรอง DB → รัน migration **ทั้ง 3 ไฟล์** → ตรวจผล → **แล้วค่อยอัปโหลดโค้ด**
   สลับลำดับ = Schema Guard ปฏิเสธการบูต ทุกหน้ารวมหน้า login ตอบ 503 และ cron ส่งอีเมลแจ้งพังทุกคืน
2. ติดตั้งด้วย `composer install --no-dev --optimize-autoloader` — **ลืม `--no-dev` = คำสั่งล้มทั้งคำสั่งบน PHP 8.3** แล้วเว็บยังเปิดได้ปกติ แต่ดาวน์โหลด Excel พังและอีเมลส่งไม่ออก เงียบทั้งคู่
3. เปิด extension `zip`, `gd` (+ dom, fileinfo, iconv, simplexml, xmlreader, xmlwriter, zlib) ก่อน

### 6.3 ⚠️ ต้องตรวจด้วยตาหลัง deploy — ระบบทดสอบแทนไม่ได้
- เปิด `https://<โดเมน>/.env` ต้องได้ **403** (ตัวกันคือ `.htaccess` ซึ่งเป็นหน้าที่ของเซิร์ฟเวอร์ ไม่ใช่ของโค้ด · nginx ไม่อ่านไฟล์นี้เลย)
- เช็กลิสต์เต็มอยู่ใน `docs/manual-checks.md` §3

### 6.4 ⚠️ 2 อย่างที่ไม่มีระบบทดสอบครอบเลย
- **การวางข้อมูลจาก Excel ลงตารางกรอกหลายวัน** (ต้องมีคลิปบอร์ดจริง) — เซสชันนี้แก้ JavaScript ในหน้านั้นไป 2 รอบ **จึงยิ่งต้องลองด้วยมือ**
- **อีเมลรีเซ็ตรหัสผ่าน/ยืนยันอีเมล ถึงกล่องจดหมายจริง**

### 6.5 ความเสี่ยงเชิงกระบวนการที่เจอซ้ำในเซสชันนี้
- **"บอกว่าแก้แล้ว" ≠ แก้จริง** — ผู้ตรวจที่ได้รับคำสั่งให้ *ทำซ้ำบั๊กทุกข้อ* พบว่า 4 ข้อที่ประกาศว่าแก้แล้วยังทำซ้ำได้
- **เทสต์เขียวไม่ได้แปลว่าโค้ดถูก** — เจอเทสต์ที่ "เขียวแต่ไม่ได้ตรวจอะไร" รวม ~15 จุด
- **เทสต์ที่เดี๋ยวเขียวเดี๋ยวแดงอาจเป็นบั๊กจริง** — ครั้งหนึ่งอาการนี้คือตัวจำกัดนับซ้ำตอนลองใหม่ (เกิดเฉพาะตอน DB มีคนแย่งกัน)

---

## 7. Rules / Standards to Follow

### จากโปรเจกต์ (ดูฉบับเต็มใน `CLAUDE.md`)
- Layer: Page/API → Service → Repository · **ห้ามใส่ business logic ใน Repository หรือ Page**
- Service คืน result-array เสมอ (`['success'=>bool, 'error'|'data'|'message'=>…]`) **ห้าม throw สำหรับ validation error**
- ข้อความที่ผู้ใช้เห็น = **ภาษาไทย** · log = อังกฤษ ผ่าน `error_log('[tag] ...')`
- SQL = prepared statement 100% · **ห้ามใช้ชื่อ placeholder ซ้ำในคำสั่งเดียว**
- state-changing ทุกตัวต้องผ่าน guard chain (POST → content-type → CSRF) และ `resolve_safe_redirect_path()`
- **`composer dump-autoload` ทุกครั้งที่เพิ่มคลาสใหม่** (โปรเจกต์ใช้ classmap ไม่ใช่ PSR-4)
- เพิ่ม endpoint ใหม่ใน `api/` → ต้องเพิ่มชื่อใน `EndpointGuardChainTest` ไม่งั้นเทสต์แดง

### วิธีทำงานที่ใช้ในเซสชันนี้ (แนะนำให้ทำต่อ)
1. **ทำซ้ำบั๊กเองก่อนแก้** — เขียนสคริปต์เรียก Service ตัวจริง อย่าเชื่อรายงานอย่างเดียว
2. **หลังแก้ ต้องจงใจทำให้โค้ดพังแล้วดูว่าเทสต์แดงไหม** — ถ้ายังเขียว เทสต์นั้นไม่มีค่า
3. รัน `composer stan` + `composer test` ให้ผ่านก่อน commit (ชุดเทสต์ ~4 นาที)
4. ทุกครั้งที่พบกับดักใหม่ ให้บันทึกลง `CLAUDE.md` พร้อม **ตัวเลขที่วัดได้จริง**
5. **เขียนกฎลงเอกสารแล้วต้องไล่ตรวจว่ามีที่อื่นละเมิดอยู่ไหม** (เคยพลาด: เขียนกฎลำดับล็อก แล้วมีโค้ดของตัวเองอีกจุดละเมิดอยู่)

### ข้อห้ามด้านความปลอดภัยของการทำงาน (สำคัญมาก)
- **ห้ามแตะฐานข้อมูล `ad_profit` (ของจริง)** — ใช้ `ad_profit_test` เท่านั้น · integration test loader จะ throw ถ้าชื่อ DB ไม่ลงท้าย `_test`
- **ห้ามรัน `database/schema.sql` กับ DB จริง** — เป็น DROP + CREATE และ hardcode ชื่อ DB จริงไว้
- ถ้าใช้ subagent ช่วยตรวจ: ห้ามให้มันรัน `git checkout/stash/restore/clean/reset` (เคยมี agent ลบงานที่ยังไม่ commit) และห้ามให้สร้าง database ใหม่

---

## 8. Next Recommended Steps

> ทั้งหมดเป็น **ข้อเสนอของ AI** ยังไม่มีใครเคาะลำดับ

1. **รัน migration ที่ขาดในเครื่อง** (ข้อ 6.1) ไม่งั้นทดสอบเปลี่ยนอีเมลไม่ได้
2. **ทำซ้ำข้อ A ในข้อ 5.1** (แก้วันที่เป็นเดือนอนาคตแล้วรายการหาย + ลบไม่ได้) — ร้ายแรงสุดในกองที่เหลือ เพราะข้อมูลเข้าถึงไม่ได้ถาวรผ่าน UI
3. **แก้ข้อ B** (บอกสำเร็จทั้งที่ส่งอีเมลไม่ได้) — กระทบการกู้บัญชี และแก้ไม่ยาก
4. **แก้ข้อ F, G, H** — ทั้ง 3 ข้อขัดกับกฎที่เขียนไว้ใน `CLAUDE.md` แล้ว (มีสเปกอยู่แล้ว ไม่ต้องตัดสินใจใหม่)
5. **ขอคำตัดสินเรื่องข้อ 5.2** (ล็อกอินพร้อมกัน) และข้อ D (ยอดหน้าประวัติ) จากเจ้าของระบบ
6. เดินรอบตรวจที่ 18 หลังปิดกองข้างบน

---

## 9. Unknowns / Need Confirmation

- `Need confirmation`: **ข้อ A–N ในข้อ 5.1 ยังไม่ได้ทำซ้ำด้วยตัวเอง** ทั้งหมดมาจากรายงานของ subagent · ควรพิสูจน์ก่อนแก้ (เซสชันนี้เคยเจอทั้งรายงานที่ถูกต้องและข้อที่ต้องตีความใหม่)
- `Need confirmation`: เกณฑ์ของ "🟢 Logic Passed" ที่เจ้าของงานใช้ — ในเซสชันนี้ยังไม่เคยประกาศ verdict นี้ ไม่ทราบว่าใครเป็นผู้ตัดสินและใช้เกณฑ์อะไร
- `Unknown`: แผนวันขึ้นเซิร์ฟเวอร์จริง — ยังไม่มีการระบุวันหรือผู้รับผิดชอบ
- `Unknown`: ค่า `max_user_connections` / จำนวน PHP worker ของแพ็กเกจโฮสต์ที่ใช้จริง — จำเป็นต่อการประเมินความเสี่ยงของ `usleep()` ในเส้นทางล็อกอิน (ผู้ตรวจตั้งข้อสังเกตไว้)
- `Unknown`: มีใครใช้ระบบนี้อยู่จริงหรือยัง — ฐานข้อมูลจริงในเครื่องว่างเปล่า (0/0/0) แต่ไม่ทราบสถานะบนเซิร์ฟเวอร์
- **Credential:** ค่า `.env` จริง (รหัส DB, รหัสอีเมล) **ไม่อยู่ในเอกสารนี้โดยเจตนา** — ขอผ่านช่องทางปลอดภัยแยกจากเจ้าของระบบ

---

## 10. Last Known Working Point

> ไม่มีงานค้างกลางทาง — working tree สะอาด และ push ครบแล้ว

- **ไฟล์/จุดล่าสุดที่แตะ:** `CLAUDE.md` (บันทึกข้อจำกัดของตัวจำกัดต่อ IP พร้อมตัวเลขที่วัดได้)
- **คำสั่งล่าสุดที่รัน:** `git push origin master` → `3bbd3a2..087ca67`
- **ผลลัพธ์ล่าสุด:** CI ของ `087ca67` = success (ทั้ง 5 job) · ไม่มี error ค้าง
- **จุดที่ควรเริ่มทำต่อ:** ข้อ 8 ลำดับที่ 1 (รัน migration ที่ขาดในเครื่อง) แล้วต่อด้วยข้อ 2

### หมายเหตุปฏิบัติการ
- ชุดเทสต์ใช้เวลา ~4 นาที (`composer.json` ตั้ง `process-timeout` ไว้แล้ว ไม่งั้นถูกฆ่าที่ 300 วิ)
- ถ้าเขียนสคริปต์ "รอให้เทสต์เสร็จ" **ห้ามใช้ `pgrep -f "phpunit"`** — คำสั่งของตัวเฝ้าเองมีคำว่า phpunit อยู่ด้วย มันจะหาเจอตัวเองแล้ววนไม่จบ (เกิดขึ้นจริงหลายครั้งในเซสชันนี้)
