# CLAUDE.md — Ad-Profit

คู่มือสำหรับ Claude Code เมื่อทำงานกับ repo นี้ อ่านก่อนเขียน/แก้โค้ดทุกครั้ง

---

## โปรเจกต์คืออะไร

ระบบบันทึกรายได้/ค่าโฆษณา/กำไรรายวัน แยกตามร้าน (multi-shop) — **PHP แบบไฟล์ล้วน ไม่มี framework** DB เป็น **MySQL/InnoDB** ฝั่งหน้าเว็บ render แบบ server-side (Chart.js + Tailwind ผ่าน CDN, ไม่มี build step)

PHP ที่รองรับ: **≥ 8.3** — **enforce ใน composer.json แล้ว** (`"require": { "php": ">=8.3" }`) ทุกไฟล์มี `declare(strict_types=1)` · เซิร์ฟเวอร์ production = Hostinger PHP 8.3

⚠️⚠️ **เพดานล่างคือ 8.3 ไม่ใช่ 8.2 — และเคยประกาศผิดมาก่อน** ตัวที่บังคับคือ `maennchen/zipstream-php 3.2.2` ที่ `phpoffice/phpspreadsheet` ลากมา ซึ่ง require `php-64bit ^8.3`
· ⚠️ มันประกาศไว้ใต้คีย์ **`php-64bit`** ไม่ใช่ `php` — อ่าน `composer.lock` เผิน ๆ จะมองไม่เห็น ต้องลองติดตั้งจริงถึงเจอ
· ⚠️ **CI job เดิม (`lint-php82`) เขียวตลอดทั้งที่ติดตั้งบน 8.2 ไม่ได้เลย** เพราะมันรันแค่ `php -l` ไม่เคยติดตั้ง dependency · วัดจริงด้วย `composer install --no-dev` บน PHP 8.2.4 → ล้มทันที ไม่มี vendor = เว็บเปิดไม่ขึ้นทั้งเว็บ
· ถ้าวันหนึ่งต้องรองรับ 8.2 จริง ต้องถอย zipstream เป็น 2.x ก่อน

⚠️ **extension ที่ host ต้องมี** (นอกจาก pdo_mysql/mbstring ตามปกติ): **`zip`, `gd`** — `phpoffice/phpspreadsheet` ประกาศเป็น hard requirement ถ้าขาด `composer install` ล้มตั้งแต่แรก (CI ทั้ง 2 jobs ที่ `composer install` ใส่ `zip, gd` ใน `setup-php` แล้ว)

⚠️ **ช่องว่างเรื่องเวอร์ชันที่ต้องรู้ (อย่าเข้าใจผิดว่าเทสต์ครอบ 8.3):**
- **test suite รันได้บน PHP 8.4+ เท่านั้น** — `phpunit/phpunit 13.x` require `php >= 8.4.1` → บน 8.3 `composer install` (dev) ไม่ผ่านตั้งแต่แรก
- ความเข้ากันได้กับ **8.3 พิสูจน์แค่ระดับ syntax** (`php -l`) ผ่าน CI job `lint-php83` + job `prod-install` ที่ลง dependency ชุด production บน 8.3 จริง — **ไม่ได้พิสูจน์ runtime behavior ของโค้ดเราเองบน 8.3** (คือเวอร์ชันที่ production ใช้จริง)
- ✅ **ปิดช่องว่างนี้แล้วด้วย smoke test** — `tests/smoke/pages.php` เขียนแบบไม่พึ่ง PHPUnit เลย จึงรันบน 8.3 ได้ · CI job `smoke-php83` ยกเซิร์ฟเวอร์จริง สมัครสมาชิก บันทึกข้อมูล แล้วเปิดทุกหน้า/ทุก endpoint/ไฟล์ดาวน์โหลด (21 รายการ) ดูว่ามีคำเตือนของ PHP หลุดออกมาไหม
  · ⚠️ **ต้องรันเซิร์ฟเวอร์ด้วย `APP_ENV=development`** ไม่งั้น `display_errors` ปิดอยู่ ตัวตรวจจะเขียวโดยไม่ได้ตรวจอะไรเลย (บทเรียนเดียวกับ `PageRenderTest`)
  · ⚠️ **ต้อง `strip_tags()` ก่อนหาคำว่า Warning** — PHP ห่อไว้เป็น `<b>Warning</b>:`
  · ⚠️ **ตัวตรวจต้องไม่ปล่อยคำเตือนของตัวเอง** — เวอร์ชันแรกเรียก `curl_close()` ซึ่งเลิกใช้ใน 8.5 คำเตือนของตัวเองไปปนกับสิ่งที่กำลังตามหา
  · มิวเทชันแล้ว: ใส่ตัวแปรที่ไม่มีจริงลง `dashboard.php` → จับได้ 5 หน้า
- ถ้าต้องการ **รันชุดเทสต์เต็ม** บน 8.3 ด้วย ต้อง downgrade PHPUnit (เช่น ^10) — **ยังไม่ทำ** เพราะ smoke test ครอบเส้นทางที่ผู้ใช้เดินจริงแล้ว

---

## กฎสถาปัตยกรรม (ห้ามฝ่าฝืนโดยไม่จำเป็น)

- **Layer:** `Page/API (controller) → Service (business logic) → Repository (SQL/PDO) → MySQL`
  - Page/API แค่รับ input, ตรวจ method/CSRF, เรียก Service, จัดรูป response
  - Service = business rule + ownership check + transaction/lock
  - Repository = SQL ล้วน ไม่มี business logic
- **Manual DI:** สร้าง Repository + Service เองในแต่ละ endpoint แล้วส่ง `$pdo`/repo เข้า constructor — **ไม่มี container**
- **Result pattern:** Service คืน array เสมอ ไม่ throw สำหรับ business/validation error
  - สำเร็จ: `['success' => true, 'data'|'message' => ...]`
  - ล้มเหลว: `['success' => false, 'error' => '<ข้อความไทย>']`
- **ไม่มี router:** endpoint ใหม่ = ไฟล์ใหม่ (front-controller-per-file)
- **Autoloader:** ชื่อคลาส = ชื่อไฟล์ อยู่ใน `app/Services/` หรือ `app/Repositories/`

## Convention การเขียนโค้ด

- คลาส = PascalCase (1 คลาส/ไฟล์), เมธอด = camelCase, global helper = snake_case
- เพจ/endpoint = kebab-case (`add-record.php`), คลาส = PascalCase (`RecordService.php`)
- **ข้อความ error ที่ user เห็น = ภาษาไทย; log = อังกฤษ** ผ่าน `error_log('[tag] ...')`
- SQL = prepared statement 100% (`PDO`, `EMULATE_PREPARES=false`) ห้าม concat user input
- state-changing action ทุกตัวต้องผ่าน guard chain: `ensure_post_request_or_respond` → `ensure_form_content_type_or_respond` → `ensure_valid_csrf_or_respond`
- redirect ที่รับค่าจาก user ต้องผ่าน `resolve_safe_redirect_path`
- **ใช้ shared component เดิม ห้ามสร้างใหม่:** helper ใน `includes/functions.php` (csrf, validation, format, flash, response), guard ใน `includes/auth.php`, `db()` ใน `includes/database.php`

## การเข้าถึงผ่านเว็บ (สำคัญมาก)

- ⚠️⚠️ **รากโปรเจกต์ = `public_html/` บน Hostinger** ทุกไฟล์จึงเสิร์ฟผ่านเว็บโดยปริยาย · เดิม **ไม่มี `.htaccess` เลยสักไฟล์** → `https://โดเมน/.env` ให้รหัสฐานข้อมูลกับรหัสอีเมลไปตรง ๆ (พิสูจน์แล้วว่า `database/schema.sql` · `composer.json` · `CLAUDE.md` โหลดได้จริง)
  · ตัวกันหลักอยู่ใน **`.htaccess` ที่ราก** — ปิดไฟล์ที่ขึ้นต้นด้วยจุด · ปิดตามนามสกุล (`env sql json lock md log …`) · ปิดโฟลเดอร์ภายในทั้งหมดด้วย `RewriteRule`
  · ⚠️ **ห้ามพึ่ง `.htaccess` ในแต่ละโฟลเดอร์อย่างเดียว** — `vendor/` `logs/` `uploads/` ถูก .gitignore ไฟล์ข้างในจึงอาจไม่ขึ้นเซิร์ฟเวอร์ (`.gitignore` มี `!logs/.htaccess` / `!uploads/.htaccess` กันไว้แล้ว แต่ `vendor/` ยังพึ่งกฎที่รากอย่างเดียว)
  · ⚠️ **โปรเจกต์นี้ไม่มีไฟล์ static ที่หน้าเว็บต้องโหลดเลย** (Tailwind/Chart.js มาจาก CDN) การปิด `.json`/`.xml` จึงปลอดภัย — ถ้าอนาคตเพิ่มไฟล์ที่เบราว์เซอร์ต้องโหลด ต้องยกเว้นในกฎ
  · `WebExposureTest` กวาดโฟลเดอร์จริงมาเทียบกับกฎ เพิ่มโฟลเดอร์ใหม่แล้วลืมปิด = เทสต์แดงทันที
  · ⚠️ **เทสต์ตรวจได้แค่ "กฎในไฟล์" ไม่ใช่พฤติกรรมจริง** (`php -S` ไม่อ่าน `.htaccess` · nginx ก็ไม่อ่าน) — **หลัง deploy ต้องเปิด `https://โดเมน/.env` ดูด้วยตาว่าได้ 403**

## Business rules ที่ต้องรักษา

- ผู้ใช้เห็นเฉพาะข้อมูลตัวเอง — ทุก Service ต้อง scope ด้วย user แต่วิธีต่างกัน:
  - single-shop (Record/Shop/Goal/Dashboard/Annual/Export): `userCanAccessShop()` / `findByIdAndUserId()`
  - overview (OverviewService/OverviewDailyService/OverviewAnnualService): `listByUserId($userId)`
- 1 ผู้ใช้สร้างได้สูงสุด 20 ร้าน; ลบร้านสุดท้ายไม่ได้
- `daily_records` unique ต่อ (shop, date) → บันทึกซ้ำวันเดิม = upsert
- revenue/ad_cost ≥ 0; note ≤ 255; ชื่อร้าน ≤ 100
- goal: เดือน `YYYY-MM`, ต้องมีอย่างน้อย 1 เป้า, ค่า ≥ 0
- `profit = revenue − ad_cost` (คำนวณ ไม่เก็บ), `ROAS = revenue / ad_cost` (null เมื่อ ad_cost = 0)
- **Overview (รวมร้าน) ดูได้เมื่อมี ≥ 2 ร้านเท่านั้น** (`can_view`)
- เปลี่ยน/รีเซ็ตรหัสผ่าน **และเปลี่ยนอีเมล** → increment `session_version` (เตะ session อื่น) — อีเมลคือช่องทางกู้บัญชี จึงถือเป็น credential เหมือนกัน

## โครงสร้างที่ต้องรู้ (gotchas)

- **Response layer:** controller ตอบผ่าน `api_respond()` — เลือก JSON (`jsonResponse`, กรณี XHR/`wants_json`) หรือ redirect+flash (กรณี form) อัตโนมัติ + `infer_http_status_from_error()`. Service คืน result-array, controller เป็นคนแปลงเป็น response
- **Frontend เป็น server-render เป็นหลัก:** state-changing = native `<form method="post" action="/api/...">` + `csrf_field()` + hidden `action` → redirect+flash; ปุ่มยืนยัน/loading/กันกดซ้ำ อยู่ใน `includes/footer.php`; **CSRF ไม่ได้ expose เป็น meta/JS**
- ⚠️ **AJAX มีจุดเดียวที่ตั้งใจ (controlled exception):** `GET api/month-grid.php` — โหลดข้อมูลทั้งเดือนมาเติมตาราง bulk ในหน้า `add-record.php` **read-only ไม่เปลี่ยน state จึงไม่มี CSRF** (auth ผ่าน session เหมือน `*-data.php`) · **การเขียนทุกอย่างยังเป็น form POST + CSRF เหมือนเดิม** · **ห้ามเพิ่มจุด AJAX ใหม่เพราะ "add-record ก็ทำ"** — ถ้าจะเพิ่มต้องเป็นการตัดสินใจที่มีเหตุผลชัดเจนเฉพาะกรณีนั้น
- **`api/dashboard-data.php`, `overview-data.php`, `annual-data.php` ไม่ถูกเรียกจาก UI** (data page เรียก Service ตรงในเพจ) — อย่าเข้าใจผิดว่าหน้าเว็บ fetch endpoint พวกนี้
- **กันกดซ้ำพึ่ง unique key ระดับ DB + row lock เท่านั้น** — ตาราง `idempotency_requests` + repository + cron ถูกลบทิ้งแล้ว (ไม่เคยถูกเรียกจากที่ไหนเลย) ⚠️ ผลข้างเคียงที่ดี: schema ไม่ต้องใช้คอลัมน์ชนิด JSON อีกต่อไป → host ไม่ต้องรองรับ JSON · **การลบซ้ำ (กด back แล้ว submit ใหม่) ตอบว่าสำเร็จแล้ว** — `deleteRecord`/`deleteGoal` ทำ idempotent เอง: หาไม่เจอในร้านปัจจุบัน → เช็กก่อนว่าอยู่ในร้านอื่นของผู้ใช้ไหม (ถ้าใช่ = ตอบ error ให้สลับร้าน) ถ้าไม่มีที่ไหนเลย = ผลตรงกับที่ขอ → ตอบสำเร็จ ⚠️ **ห้ามเพิ่ม flag/ข้อความแยกให้เคสนี้** ต้องเหมือนการลบที่สำเร็จจริงทุกไบต์ ไม่งั้นบอกใบ้ว่ารายการของคนอื่นมีอยู่จริงไหม · `ShopService::deleteShop` **ตั้งใจไม่ทำ idempotent** (เหตุผลอยู่ในคอมเมนต์ของเมธอด)
- ⚠️⚠️ **ตารางใหม่ต้องมาลงทะเบียนกับ Schema Guard ครบทั้ง 5 รายการ — และตอนนี้มีตัวกวาดบังคับแล้ว**
  · `email_change_requests` เคยตกสำรวจทั้ง 5 รายการ ทั้งที่สร้างตามแบบ `password_reset_tokens` ทุกอย่าง (unique key คู่เดียวกัน · FK cascade เหมือนกัน · แม้แต่คอมเมนต์ "ขอใหม่ทับของเดิม" ก็เหมือนกัน)
  · **วัดจริง 2 กรณี:** (1) ฐานข้อมูลขาดตารางนี้ตารางเดียว → guard ตอบว่า "ผ่าน" แอปเปิดได้ปกติทุกหน้า จนกว่าจะมีคนกดเปลี่ยนอีเมลถึงจะเจอทางตัน — ซึ่งคือกรณี "อัปโหลดโค้ดใหม่แล้วลืมรัน migration" และอาจไม่มีใครรู้ไปหลายเดือน · (2) migration ที่ทำมือแล้วพิมพ์ `KEY` แทน `UNIQUE KEY` → ขอเปลี่ยนอีเมล 2 ครั้งแล้ว **ลิงก์ทั้งสองใบยังใช้ได้** ทั้งที่กติกาคือขอใหม่ต้องทำให้ของเก่าใช้ไม่ได้ทันที (กุญแจตัวนั้นคือสิ่งเดียวที่บังคับกติกานี้)
  · ⚠️ **ตัวกวาดเดิมเทียบกับรายชื่อที่พิมพ์ไว้ตายตัว จึงไม่มีวันทักตารางใหม่** — ตอนนี้ `SchemaGuardTest` กวาดจาก `information_schema` ของฐานข้อมูลจริงแทน (unique index · ตาราง/InnoDB · FK cascade) เพิ่มตารางแล้วลืมลงทะเบียน = แดงทันที · **พอเปลี่ยนวิธีกวาด มันเจอช่องที่สองเองทันที: `auth_rate_limits` ก็ไม่เคยถูกตรวจว่าเป็น InnoDB**
- **Schema Guard ใน `includes/bootstrap.php`:** ถ้า schema ไม่ตรง (ตาราง/คอลัมน์/index ที่กำหนด) ระบบตอบ 503 / CLI exit(1) ควบคุมด้วย flag `SCHEMA_GUARD_ENABLED` — **เวลาแก้ schema ต้องอัปเดต guard ด้วย**
- ⚠️ **การเปลี่ยน collation ไม่ได้ "แยกอย่างเดียว" — มันรวมด้วย** มี 89 คู่อักขระที่ `utf8mb4_unicode_ci` ถือว่าต่างแต่ `utf8mb4_unicode_520_ci` ถือว่าเหมือน (กลุ่มเวียดนาม `D`/`Đ`/`Ð`/`đ` และ `6`/`ↅ`) · migration จึงต้อง **เติมเลขต่อท้ายชื่อที่จะชนกันก่อน** แล้วค่อย ALTER ไม่งั้นล้มด้วย duplicate key พร้อม error ที่ไม่พูดถึงชื่อร้านซ้ำเลย และการแก้ไปไม่ถึงเครื่องจริง
- ⚠️ **Schema Guard ต้องตรวจ `COLLATION_NAME` ด้วย** — `COLUMN_TYPE` มองไม่เห็น collation · ถ้า migration ล้มกลางคัน แอปจะบูตขึ้นมารายงานว่าปกติ ทั้งที่บั๊กอิโมจิยังอยู่ครบ
- ⚠️ **ตัวจำกัดการเดารหัสผ่านต้อง "นับก่อน แล้วดูเลข" ไม่ใช่ "ถามว่าเกินยัง แล้วค่อยนับ"** — เดิมมี `password_verify()` (~100ms) คั่นระหว่างอ่านกับเขียน คำขอที่ยิงพร้อมกันจึงอ่านค่าเดิมกันหมด · **วัดจริง: เพดาน 5 แต่ยิงพร้อมกัน 40 ผ่านเข้าไป 28** (ทีละครั้งผ่าน 5 ตามที่ควร) · `AuthService::reserveAttempt()` จองแบบ atomic แล้วคืนเลขที่นับได้ → เหลือ 2
- ⚠️ **ตั้ง `display_errors` ก่อนแตะระบบไฟล์ใน `bootstrap.php`** — ถ้า `LOG_FILE` ชี้ไปที่เขียนไม่ได้ `mkdir()` จะเตือนก่อนที่แอปจะปิดการแสดง error ผลคือ **เส้นทางเต็มของเซิร์ฟเวอร์หลุดออกไปในหน้าเว็บ** และ response ถูกส่งไปแล้ว → `header()` ทุกตัวหลังจากนั้นไม่ทำงาน หน้าที่ควรตอบ 503 กลายเป็น 200 · เขียน log ไม่ได้ต้องไม่ทำให้หน้าเว็บพัง แต่ก็ต้องไม่เงียบ (แจ้งผ่านปลายทางปริยายของ host แทน)
- ⚠️ **`phpunit.xml` ตั้ง `failOnRisky="true"`** — ไม่งั้นเทสต์ที่ไม่ยืนยันอะไรเลย (เช่นลูปที่รายชื่อว่าง) จะขึ้นแค่ "risky" แล้ว exit code ยัง 0 · CI เขียวทั้งที่ตัวกันหายไปแล้ว
- ⚠️ **collation ของ `shops.name` คือกติกา "ชื่อร้านซ้ำกันไหม"** — `uq_shops_user_name` ใช้มัน และ `findByNameAndUserId()` เทียบด้วย `name = :name` เฉย ๆ · ต้องเป็น **`utf8mb4_unicode_520_ci`** เท่านั้น
  · `utf8mb4_unicode_ci` (UCA 4.0.0) **ไม่ให้น้ำหนักกับอักขระนอกระนาบพื้นฐาน** → อิโมจิทุกตัวเท่ากันหมด · ผู้ใช้ที่มีร้าน 🚀 แล้วสร้างร้าน 🎉 ถูกพาไปร้าน 🚀 พร้อมข้อความว่าสำเร็จ แล้วข้อมูลลงผิดร้านทั้งหมด (เกิดขึ้นได้จริง พิสูจน์แล้ว)
  · `utf8mb4_bin` แก้อิโมจิได้แต่ทำให้ "ร้าน A" ≠ "ร้าน a" ซึ่งเปลี่ยนพฤติกรรม "พิมพ์ชื่อเดิม = สลับไปร้านนั้น" · 520_ci แก้อิโมจิโดยไม่กระทบส่วนนั้น
  · **ข้อความตอนสลับร้านต้องบอกชื่อร้านปลายทางเสมอ** — ระบบถือว่า "ร้าน A" = "ร้าน a" = "Café" = "Cafe" ผู้ใช้จึงถูกพาไปอีกร้านได้โดยไม่รู้ตัว
- ⚠️ **Schema Guard ต้องตรวจ "ชนิด" ไม่ใช่แค่ "ชื่อ"** — เดิมดูแค่มีตาราง/คอลัมน์/index ชื่อนี้ไหม · ย่อ `note` เหลือ VARCHAR(50) · เปลี่ยน `revenue` เป็น DECIMAL(8,2) · ลบ foreign key · เปลี่ยนเป็น MyISAM — **ผ่านหมดทุกอย่าง** ทั้งที่แต่ละอย่างทำข้อมูลหายคนละแบบ (FK หาย = ลบร้านแล้วข้อมูลค้าง · MyISAM = `rollBack()` ไม่ทำอะไรเลย) · ตอนนี้ตรวจ `COLUMN_TYPE` + `ENGINE` + `DELETE_RULE` ด้วย (`schema_required_column_types()`, `schema_transactional_tables()`, `schema_required_cascades()`)
- ⚠️ **ความยาวในโค้ดกับความยาวคอลัมน์ต้องเป็นค่าคงที่ที่ผูกกันไว้** — `RecordService::NOTE_MAX_LENGTH` · `ShopService::MAX_SHOP_NAME_LENGTH` · `ProfileService::MAX_DISPLAY_NAME_LENGTH` · `MAX_AMOUNT`/`AMOUNT_DECIMALS` ↔ `DECIMAL(12,2)` · `SchemaContractTest` ล็อกทุกคู่ · เดิมเป็นเลขลอย ๆ ตัวกันฝั่ง PHP จึงทำงานถูกเพราะ "บังเอิญ" ตรงกับคอลัมน์
- ⚠️ **ห้ามใช้ `PDO::MYSQL_ATTR_INIT_COMMAND`** — ถูกประกาศเลิกใช้ใน PHP 8.5 แล้ว **คำเตือนหลุดลงกลางหน้าเว็บ** ที่ผู้ใช้เห็น (เจอมาแล้ว เทสต์ `PageRenderTest` จับได้) · ตัวแทนใหม่ `Pdo\Mysql::ATTR_INIT_COMMAND` มีเฉพาะ 8.4+ ซึ่งสูงกว่าที่โปรเจกต์รองรับ (8.3) → **สั่ง `SET SESSION sql_mode` เป็น query ธรรมดาหลังต่อ** ทำงานเหมือนกันทุกเวอร์ชัน
- ⚠️ **`includes/database.php` ตั้ง `sql_mode` เองหลังต่อฐานข้อมูล** — ห้ามพึ่งค่าปริยายของโฮสต์ · ถ้าไม่ strict MySQL จะ "ตัดให้พอดี" แทนที่จะปฏิเสธ (โน้ต 256 → 255 · ยอด 10,000,000,000 → 9,999,999,999.99) เงียบสนิทและรายงานว่าสำเร็จ ⚠️ **ทศนิยมเกิน 2 ตำแหน่งถูกปัดเงียบในทุก sql_mode** — `hasTooManyDecimals()` เป็นด่านเดียวถาวร
- **`database/schema.sql` เป็น DROP + CREATE** — ห้ามรันทับ database จริง; ถ้าจะแก้โครงบน DB ที่มีข้อมูล ใช้ `ALTER` แยกต่างหาก ⚠️ ไฟล์**ขึ้นต้นด้วย `CREATE DATABASE ad_profit; USE ad_profit;` (hardcode ชื่อ DB จริง)** → `mysql < schema.sql` บนเซิร์ฟเวอร์ = **DROP ตารางใน `ad_profit` จริงทันที**; integration test loader (`tests/Integration/IntegrationTestCase.php`) จึง**ตัด 2 บรรทัดนี้ทิ้ง** ให้ DDL ลงเฉพาะ DB ที่ต่ออยู่
- ⚠️ **`session_start()` ต้องตั้ง `gc_maxlifetime` ให้ยาวกว่า idle timeout ของแอป** — ค่าปริยายของ PHP คือ 1440 วิ (24 นาที) ตัวเก็บกวาดของ PHP จึงลบไฟล์ session ทิ้งก่อนที่ guard ของแอปจะได้ทำงาน ผู้ใช้หลุดจากระบบตั้งแต่พักไป ~24 นาที ทั้งที่ตั้งไว้ 4 ชม. (พิสูจน์แล้ว) · ตอนนี้ `bootstrap.php` ตั้งเป็น `max(idle, absolute) + 3600`
- **ค่าคงที่จาก env ผ่าน `config_positive_int()`** — รับเฉพาะจำนวนเต็มบวก ค่าที่ตั้งผิด (`-1`, `4h`, `0`) กลับไปใช้ค่าปริยายพร้อมเขียน log ⚠️ `RATE_LIMIT_MAX_ATTEMPTS`/`RATE_LIMIT_WINDOW_SECONDS` เดิม **ไม่เคยอ่านจาก env เลย** ทั้งที่ `.env.example` เขียนไว้ให้ปรับ
- ⚠️⚠️ **กฎลำดับล็อกนี้ใช้กับ `email_change_requests` ด้วย** — `ProfileService::confirmEmailChange()` เคยจองแถวคำขอก่อนแถวผู้ใช้ ขณะที่ `changeEmail()` จองแถวผู้ใช้ก่อน · วัดจริงแล้วได้ deadlock 1213 และผู้ใช้เห็นข้อความชี้ผิดสาเหตุว่า "ลิงก์อาจหมดอายุหรือถูกใช้ไปแล้ว" ทั้งที่ลิงก์ยังดีอยู่
  · ⚠️ **เขียนกฎลงเอกสารแล้วต้องไล่ตรวจว่ามีที่อื่นละเมิดอยู่ไหม** — คอมมิตที่เพิ่มกฎนี้ แก้เฉพาะ `resetPassword` ส่วนโค้ดที่เขียนก่อนหน้านั้นแค่ก้าวเดียวยังผิดอยู่
- ⚠️⚠️ **`reserveAttempt()` ห้ามนับซ้ำตอนลองใหม่** — การจอง 1 ครั้งมี 2 ขั้น (เพิ่มเลข → อ่านเลข) ถ้าขั้นแรกสำเร็จแล้วขั้นสองล้ม การวนกลับไปทำใหม่ทั้งคู่จะเพิ่มเลขเป็นครั้งที่สอง → พิมพ์รหัสผิดครั้งเดียวถูกนับ 2 ครั้ง ชนเพดานเร็วกว่าที่ควรเท่าตัว
  · ⚠️⚠️ **`reserveAttempt()` นับให้แล้ว — ห้ามเรียก `markFailedAttempt()` ซ้ำในทางเดียวกัน** · เคยแก้ที่ `resetPassword()` ไปแล้วรอบหนึ่ง (`testAnExpiredLinkCountsOncePerClick`) แต่ `requestPasswordReset()` ยังเรียกซ้ำอยู่ **ขอลิงก์ 1 ครั้งถูกนับ 2** (วัดจริง) · ⚠️ **อาการมองไม่เห็นจากหน้าเว็บ** เพราะเพดานของทางนี้เป็น 1 พอดี ผลลัพธ์จึงถูกต้องโดยบังเอิญ — แต่ใครขยับเพดานเป็น 5 จะได้จริง 2 → เทสต์ต้องตรวจ **ตัวนับตรง ๆ** ไม่ใช่ตรวจว่าถูกกันตอนไหน (`testAskingForAResetLinkOnceCountsOnce`)
  · ⚠️⚠️ **กรอกฟอร์มผิดไม่ใช่ความพยายามสมัคร/รีเซ็ต ต้อง `releaseAttempt()` คืนโควตา** — ครบทั้ง `register()` และ `resetPassword()` แล้ว · วัดจริงตอน `register()` ยังไม่คืน: พิมพ์ช่องยืนยันรหัสผ่านไม่ตรง 5 ครั้ง (เกิดบ่อยมากบนมือถือ) → ถูกกัน 1 นาที และการสมัครที่กรอก **ถูกทุกช่อง** จากเบราว์เซอร์เดียวกันก็ถูกปฏิเสธ · ถัง `register_ip` ผูกกับ IP ล้วน ออฟฟิศจึงแชร์โควตากันทั้งตึก
  · ⚠️ **แต่กิ่ง "อีเมลนี้ถูกใช้แล้ว" ตั้งใจไม่คืน** — การถามซ้ำ ๆ คือการไล่เดาว่าบัญชีไหนมีอยู่จริง · เทสต์ที่ตรวจเรื่องนี้ต้องยิงด้วย **รหัสผ่านที่ถูกรูปแบบ** ไม่งั้นจะไปตกด่านตรวจรูปแบบก่อนถึงฐานข้อมูล แล้วไม่ได้ทดสอบสิ่งที่ชื่อเทสต์บอกเลย (เจอมาแล้ว)
  · ⚠️ `markFailedLoginAttempt()` · `markFailedRegisterAttempt()` · `markFailedAttempt()` · `isRateLimited()` เป็นซากจากยุค "ถามก่อนค่อยนับ" **ไม่มีใครเรียกเลยสักตัว** — ลบทิ้งหมดแล้ว ถ้าเห็นใครเขียนกลับมาแปลว่ากำลังจะนับซ้ำหรือกลับไปเป็น check-then-act
  · ⚠️ อาการที่เห็นคือ **เทสต์ตัวนับ "เดี๋ยวเขียวเดี๋ยวแดง"** เพราะการลองใหม่เกิดเฉพาะตอนที่ ฐานข้อมูลมีคนแย่งกันจริง (รันชุดเทสต์รวมทั้งชุด) · ถ้ามองว่าเป็นเทสต์งอแงแล้วรันใหม่จนเขียว บั๊กจะขึ้นเซิร์ฟเวอร์ไป และจะแรงที่สุดตอนเว็บมีคนใช้เยอะ (`RateLimitRetryTest` ล็อกไว้)
- ⚠️ **เทียบ "ชื่อร้านเปลี่ยนไหม" ต้องเทียบกับชื่อที่เก็บอยู่จริง ไม่ใช่ชื่อที่ normalize แล้ว** — ร้านเก่าที่ชื่อติด NBSP เทียบแบบ normalize สองฝั่งจะเท่ากันเสมอ → ตอบว่าสำเร็จโดยไม่ `UPDATE` อะไรเลย ผู้ใช้ล้างช่องว่างที่มองไม่เห็นออกไม่ได้ตลอดกาล
- ⚠️⚠️ **`normalize_email()` และ `ProfileService::updateProfile()` ก็ต้องใช้ `trim_unicode_whitespace()` — เดิมเป็น `trim()` ธรรมดาทั้งคู่**
  · **อีเมล:** ก๊อปจาก LINE/แชท/Word มาวางมักติดช่องว่างที่มองไม่เห็น → `is_valid_email()` ตอบว่ารูปแบบผิด ทั้งที่บนจอดูถูกทุกตัวอักษร · วัดจริง: สมัครสมาชิก **และลืมรหัสผ่าน** ได้ "กรุณากรอกอีเมลที่ถูกต้อง" · ล็อกอินที่ช่องว่างอยู่ **ข้างหน้า** ได้ "อีเมลหรือรหัสผ่านไม่ถูกต้อง" ทั้งที่ทั้งคู่ถูก แล้วการลองซ้ำไปชนตัวจำกัดจำนวนครั้ง
    · ⚠️ **เดิมล็อกอินผ่านเมื่อช่องว่างอยู่ท้ายสุด — เป็นความบังเอิญของ collation ของ MySQL ที่มองข้ามตัวท้ายให้ ไม่ใช่การออกแบบ** ฝั่ง PHP ยังถือว่ารูปแบบผิดอยู่ดี ทางอื่นจึงล้มหมด · **ห้ามพึ่งพฤติกรรมนั้น**
  · **ชื่อที่แสดง:** ด่าน "ชื่อว่าง" ถูกข้ามได้ด้วยตัวอักษรที่มองไม่เห็น ชื่อกลายเป็นช่องเปล่าเงียบ ๆ
    · ⚠️⚠️ **แก้ตัวตัดแล้วต้องแก้การเทียบด้วย** — `updateProfile()` เทียบชื่อเดิมกับชื่อใหม่ ถ้า normalize ทั้งสองฝั่ง ชื่อเก่าที่ติด NBSP จะเท่ากับชื่อสะอาดเสมอ → ตอบสำเร็จโดยไม่ `UPDATE` **ล้างออกไม่ได้ตลอดกาล** (บทเรียนเดียวกับชื่อร้าน · `ProfileServiceTest` ล็อกทั้งสองทาง)
- ⚠️ **`trim()` ของ PHP กับ `.trim()` ของเบราว์เซอร์ตัดคนละชุดตัวอักษร** — PHP ไม่ตัดช่องว่างยูนิโค้ด (NBSP, ช่องว่างญี่ปุ่น, zero-width) · ก๊อปชื่อร้านจาก LINE/Word มาวางมักติด NBSP ท้ายชื่อ → กล่องยืนยันตอนลบร้านแสดงชื่อที่เบราว์เซอร์ตัดแล้ว ผู้ใช้พิมพ์ตามที่เห็นเป๊ะ ๆ ก็ไม่ตรง **ลบร้านนั้นไม่ได้ตลอดกาล** และยังกินโควตา 1 ใน 20 · ใช้ `trim_unicode_whitespace()` ทุกจุดที่แตะชื่อร้าน (เทียบตอนลบก็ normalize ทั้งสองฝั่ง ร้านเก่าที่ติดปัญหาจึงลบได้ทันทีโดยไม่ต้องแก้ข้อมูล)
- ⚠️⚠️ **ลำดับการจองแถวของ `users` กับ `password_reset_tokens` ต้องเหมือนกันทุกทาง — `users` ก่อนเสมอ**
  · หน้าโปรไฟล์ (`changePassword`/`changeEmail`) จอง `users` ก่อนแล้วค่อยแตะ token · `AuthService::resetPassword` เดิมจองสลับกัน (`findByTokenHashForUpdate` ใช้ `JOIN users … FOR UPDATE` ซึ่งจองตาราง token ก่อน) → ทำพร้อมกันแล้ว deadlock โดยไม่มีฝ่ายไหนลองใหม่
  · วัดจริง 6 รอบ: ฝ่ายที่แพ้ได้ข้อความ "ไม่สามารถรีเซ็ตรหัสผ่านได้" ซึ่งไม่บอกอะไรเลย · หลังแก้ได้ "ลิงก์หมดอายุแล้ว" หรือ "รหัสปัจจุบันไม่ถูกต้อง" ซึ่งเป็นความจริงทั้งคู่
  · วิธีแก้: อ่าน token แบบไม่ล็อกก่อนเพื่อรู้ว่าเป็นของใคร → `lockForUpdate($userId)` → แล้วค่อยอ่านซ้ำแบบล็อก (การอ่านใต้ล็อกคือตัวที่ตัดสินจริง)
  · ⚠️ **เทสต์แบบ "จับแถวไว้แล้วดูว่าล้มไหม" พิสูจน์เรื่องนี้ไม่ได้** — `JOIN users` ล็อกแถวผู้ใช้ไปด้วย จับแถวไหนไว้ก็ล้มเหมือนกันทั้งสองลำดับ (ลองแล้ว เขียวทั้งคู่) · ต้องจับ **ลำดับการเรียก** ด้วย stub ที่บันทึกลำดับแทน (`ConcurrentWriteLockOrderTest`)
- **`ProfileService::revokePasswordResetTokens()` ต้องไม่ catch เอง** — ถูกเรียกอยู่ในทรานแซกชันเดียวกับการเปลี่ยน credential ถ้ากลืน error ระบบจะ commit รหัสผ่านใหม่โดยที่ลิงก์รีเซ็ตเก่ายังใช้ได้ แล้วบอกว่า "สำเร็จ"
- **Auth/Session:** idle timeout 14400s, absolute 86400s; `requireAuth`/`requireGuest` เป็น guard; `isSessionVersionValid()` เช็ก DB **ทุก request**
- **Rate limiting:** auth ใช้ตาราง `auth_rate_limits` (DB) + fallback session; profile (email/password) ใช้ session-based ตอบ 429 · **login และ register มี 2 bucket เหมือนกัน**: ต่อ (IP + อีเมล) และต่อ IP ล้วน (`login_ip` / `register_ip`) — bucket ต่อ IP ไม่ถูกล้างตอนสำเร็จโดยตั้งใจ (ไม่งั้นสำเร็จ 1 ครั้งจะล้างประวัติการไล่เดาบัญชีอื่นทิ้ง)
- ⚠️ **ตัวจำกัดต้อง "จองก่อนตรวจ" (reserve-then-act) ไม่ใช่ "ถามก่อนแล้วค่อยนับ"** — ระหว่างอ่านตัวนับกับเขียนตัวนับมี `password_verify()`/`password_hash()` (~100 มิลลิวินาที) คั่นอยู่ ยิงพร้อมกัน 40 ครั้งจึงผ่านเข้าไป 28 ครั้งทั้งที่เพดาน 5 (วัดจริง) · ตอนนี้ `login`/`register`/`password_reset`/`reset_password` `reserveAttempt()` ก่อนทั้งหมด
  · ⚠️ **จองไม่สำเร็จ = ปฏิเสธไว้ก่อน (`PHP_INT_MAX`)** ห้ามตกไปใช้ตัวนับใน session — process ใหม่เริ่มที่ 0 เสมอ ตัวจำกัดจะเปิดช่องพอดีตอนที่ต้องการมันที่สุด (วัดจริง 16 จาก 40) · ลองใหม่ 3 ครั้งก่อนสำหรับ 1213/1205
  · ✅ **[เจ้าของระบบตัดสิน 2026-08-06 — ยอมรับข้อจำกัดนี้ ไม่ต้องแก้]** ยืนยันด้วยข้อความ: "พนักงานล็อกอินพร้อมกันแล้วเข้าไม่ครบ ไม่เป็นไร ใช้กันแค่ 2 คน" — ระบบนี้มีผู้ใช้ 2 คน เพดาน 5 จึงไม่มีทางถึงในทางปฏิบัติ · **ถ้าวันหนึ่งมีผู้ใช้เกิน 5 คนในออฟฟิศเดียวกัน ต้องกลับมาทบทวนข้อนี้**
  · ⚠️⚠️ **การคืนโควตาแก้ได้เฉพาะกรณีเข้าทีละคน** · วัดจริง (พนักงาน 8 คน รหัสถูกทุกคน IP เดียวกัน เพดาน 5):
    - เข้าทีละคน → **8/8** ✓
    - กดห่างกัน 0.15 วิ → **5/8**
    - กดพร้อมกันเป๊ะ → **2–4/8**
    สาเหตุ: จองโควตา *ก่อน* ตรวจรหัสผ่าน (จำเป็น เพื่อกันการยิงพร้อมกันหลบด่าน) แล้วคืนหลังตรวจเสร็จ ~0.5 วิ · หน้าต่างนั้นจึงรับได้แค่ 5 คน
    ทางเลือกที่มีข้อแลกเปลี่ยนทั้งหมด: (ก) ให้ "รหัสถูก" ผ่านเสมอ → คนร้ายทำให้ตรวจรหัสไม่จำกัด = กินซีพียู · (ข) นับเฉพาะครั้งที่รหัสผิดจริง → ยิงเป็นชุดใหญ่ผ่านได้ 1 ชุด · (ค) เพิ่มเพดานต่อ IP → กัน password spraying ได้น้อยลง
    **อาการจริงไม่รุนแรง**: เกิดเฉพาะตอนกดพร้อมกันในวินาทีเดียว (เปิดคอมพร้อมกันตอนเช้า) คนที่ถูกกันรอ 1 นาทีแล้วเข้าได้
  · ⚠️ **ล็อกอิน/สมัคร "สำเร็จ" ต้อง `releaseAttempt()` คืนโควตาของ bucket ต่อ IP** — เพราะจองไปตั้งแต่ก่อนรู้ผล และ bucket ต่อ IP ไม่ถูกล้าง · ไม่คืน = ออฟฟิศที่ใช้เน็ตร่วมกัน คนที่ 6 ที่พิมพ์รหัส **ถูก** ถูกปฏิเสธ (เคยชิปจริง) · คืนแค่ 1 ครั้ง ไม่ใช่ล้างทั้งถัง ประวัติการไล่เดาของคนอื่นจึงยังอยู่
  · ⚠️ **bucket ต่อ (IP + อีเมล) ยิงไม่ออกในทางปฏิบัติ** — เพดานเท่ากัน (5/60 วิ) และ `login_ip` นับ *ทุก* อีเมลจาก IP นั้น ค่าจึง ≥ ตัวนับต่อบัญชีเสมอ ชนเพดานก่อนหรือพร้อมกันเสมอ → **การไล่เดาบัญชีเดียวจากหลาย IP ไม่มีเพดานของตัวเอง**
- ⚠️ **bucket ที่ 3: `login_account` ผูกกับ "อีเมลล้วน" ไม่มี IP — และมันต้อง "หน่วงเวลา" ไม่ใช่ "ปฏิเสธ"** (`LOGIN_ACCOUNT_MAX_ATTEMPTS` 20 / `LOGIN_ACCOUNT_WINDOW_SECONDS` 900 · หลวมและยาวกว่าเพดานต่อ IP โดยตั้งใจ)
  · เกิดขึ้นเพราะอีก 2 bucket ผูกกับ IP ทั้งคู่ คนร้ายที่หมุนเปลี่ยน IP จึงเลี่ยงได้หมด (IP ใหม่เริ่มนับ 0)
  · ⚠️⚠️ **ห้ามเปลี่ยนเป็นการปฏิเสธเด็ดขาด** — คนร้ายจะพิมพ์รหัสผิดรัว ๆ เพื่อล็อกบัญชีเหยื่อไม่ให้เข้าใช้เอง (แลกช่องโหว่หนึ่งกับอีกช่องโหว่หนึ่ง) · การหน่วงทำให้การไล่เดาแพงขึ้นมากโดยที่เจ้าของบัญชีที่พิมพ์รหัส **ถูก** ยังเข้าได้เสมอ
  · ⚠️ **ทางที่ "ไม่มีอีเมลนี้" ต้องหน่วงเท่ากับทางที่ "รหัสผิด" เป๊ะ ๆ** ไม่งั้นจับเวลาแล้วรู้ได้ว่าอีเมลไหนมีบัญชีจริง — ซึ่งเป็นสิ่งที่ข้อความตอบกลับที่เหมือนกันทุกกรณีตั้งใจปิดไว้
  · ⚠️ **เวลาหน่วงต้องมีเพดาน** (`LOGIN_ACCOUNT_MAX_DELAY_MS` ปริยาย 1500 · เพดานแข็ง 5000) ไม่งั้นการยิงจำนวนมากทำให้ PHP ค้างรอกันจนเว็บล่ม = คนร้ายล่มเว็บได้ด้วยการเดารหัส
  · ล็อกอินสำเร็จ = `clearRateLimit()` ล้าง bucket นี้ทิ้ง (ต่างจาก `login_ip` ที่คืนแค่ 1) เจ้าของบัญชีที่เพิ่งพิมพ์ผิดหลายครั้งจึงกลับมาเร็วทันที
  · **หน้าต่างเวลาแยกตาม action แล้ว** (`getWindowSecondsForAction()`) — เดิม `RATE_LIMIT_WINDOW_SECONDS` ถูกฮาร์ดโค้ดไว้ 5 ที่
  · ⚠️ **เทสต์ที่จับเวลาต้องเทียบกับ "เวลาพื้นฐานที่วัดจริง" ไม่ใช่เลขตายตัว** — การตรวจรหัสผ่านกินเวลา ~550ms อยู่แล้ว เทียบกับเลขเล็ก ๆ จะเขียวแม้ถอดการหน่วงออก · และต้อง **ยิงทิ้ง 1 ครั้งก่อนจับเวลา** เพราะครั้งแรกของโปรเซสต้องสร้าง hash หลอกซึ่งกินเวลาหลายร้อยมิลลิวินาที
  · ⚠️ **"ผู้ใช้ปกติไม่โดนหน่วง" จับเวลาพิสูจน์ไม่ได้** — ถ้าโค้ดหน่วงตั้งแต่ครั้งแรก ตัวที่เอามาเทียบก็โดนหน่วงเท่ากัน ผลต่างเป็นศูนย์ เทสต์เขียวทั้งที่พัง (ลองแล้ว) → ต้องถามกติกาตรง ๆ ผ่าน `accountThrottleDelayMilliseconds()`
  · ทั้งหมดถูกล็อกไว้ใน `LoginRateLimitTest` (ฝั่ง "กันคนร้าย" **และ** ฝั่ง "ไม่กันคนที่ไม่ได้ทำผิด") — `AuthServiceLoginTest` เขียว 18/18 ตลอดทั้งสองเหตุการณ์ที่พลาด
- ⚠️⚠️ **ลิงก์ยืนยันอีเมลต้องไม่แตะบัญชีของคนที่กด ถ้าลิงก์เป็นของคนอื่น** (`verify-email.php`)
  · เป็นบั๊กคลาสเดียวกับที่ `reset-password.php` แก้ไปแล้ว — หน้านี้เขียนทีหลังแล้วพลาดซ้ำ **อ่านคอมเมนต์ในไฟล์นั้นก่อนแก้หน้าที่รับ token ใด ๆ**
  · วัดจริง: ผู้ส่งขอเปลี่ยนอีเมลบัญชีตัวเอง → ส่งลิงก์ให้เหยื่อกด → **เหยื่อถูกเตะออกจากระบบทุกเครื่องทันที** พร้อมหน้าจอที่บอกว่า "เปลี่ยนอีเมลเรียบร้อยแล้ว — ต่อไปนี้ให้เข้าสู่ระบบด้วย `<อีเมลของผู้ส่ง>`" ทั้งที่บัญชีเหยื่อไม่ถูกแตะเลย (เป็นฉากตั้งต้นของการหลอกเอารหัสผ่าน)
  · วิธีแก้: อ่านว่า token เป็นของ user ไหน **ก่อนแตะอะไรทั้งสิ้น** ถ้าล็อกอินอยู่แต่ไม่ใช่เจ้าของ = ไม่ยืนยัน ไม่ล้าง session บอกให้ออกจากระบบก่อนแล้วกดใหม่ · เจ้าของตัวจริงยังยืนยันได้ทั้งแบบล็อกอินอยู่และไม่ได้ล็อกอิน (`VerifyEmailPageTest` ล็อกทั้ง 4 ทาง)
- ⚠️ **token รีเซ็ตรหัสผ่านเดินทางผ่าน hidden field ของฟอร์มเท่านั้น ห้ามเก็บลง `$_SESSION`** — เดิม `reset-password.php` รับ `?token=` แล้วยัดลง session + เตะผู้ใช้ออกจากระบบ ทำให้ใครก็ได้ส่งลิงก์ของตัวเองให้เหยื่อกด แล้วรหัสที่เหยื่อพิมพ์ไปตกที่บัญชีผู้ส่ง (ทำซ้ำได้จริง) · ตอนนี้หน้าจอ **แสดงอีเมลของบัญชีที่ลิงก์นั้นเป็นเจ้าของ** และเตือนเมื่อไม่ตรงกับบัญชีที่ล็อกอินอยู่ · `api/auth.php` อ่าน token จาก `$_POST` เท่านั้น
- ⚠️⚠️ **เปลี่ยนอีเมลต้องยืนยันที่กล่องจดหมายใหม่ก่อน** — `ProfileService::changeEmail()` แค่บันทึกคำขอลง `email_change_requests` + ส่งลิงก์ · อีเมลเปลี่ยนจริงที่ `confirmEmailChange()` (ปลายทางคือ `verify-email.php`)
  · เดิมเปลี่ยนทันทีโดยไม่มีชั้นไหนกันเลย · พิมพ์ผิดตัวเดียว (`@gmial.com`) ระบบขึ้นว่าสำเร็จ แต่วันไหนหลุดจากระบบจะเข้าไม่ได้อีก และลิงก์ลืมรหัสผ่านจะไปกล่องจดหมายที่ไม่มีจริง = **บัญชีหายถาวร**
  · ⚠️ **ตอน "ขอ" ต้องไม่แตะอะไรเลย** — ห้าม `updateEmail` ห้าม `incrementSessionVersion` ห้ามแตะ `$_SESSION['email']` · เตะ session และล้าง token รีเซ็ตทั้งหมดเกิดที่ **ตอนยืนยัน**
  · ⚠️ **ต้องเช็ก "อีเมลถูกใช้ไปแล้วหรือยัง" ซ้ำตอนยืนยันด้วย** — ระหว่างลิงก์รออยู่อาจมีคนสมัครไปก่อน (ฐานข้อมูลปฏิเสธให้อยู่แล้ว แต่ผู้ใช้จะได้ข้อความกลาง ๆ ที่ไม่บอกว่าต้องทำอะไรต่อ)
  · ⚠️ **`composer dump-autoload` ทุกครั้งที่เพิ่มคลาสใหม่** — โปรเจกต์ใช้ classmap ไม่ใช่ PSR-4 คลาสใหม่จะหาไม่เจอจนกว่าจะ dump (เจอมาแล้วตอนเพิ่ม `EmailChangeRepository`)
  · **[ต้องคนตัดสิน] ยังไม่มีการแจ้งเตือนไปอีเมลเดิม** — คนที่ยึด session ได้ยังเปลี่ยนอีเมลไปเป็นของตัวเอง แล้วยืนยันจากกล่องจดหมายตัวเองได้ · เจ้าของระบบเลือก "ส่งลิงก์ยืนยันไปอีเมลใหม่" เมื่อ 2026-08-05 การแจ้งอีเมลเดิมเป็นชั้นถัดไปที่ปิดช่องนี้
- ⚠️ **`APP_URL` ต้องเป็น URL เต็มในโหมด production — ตรวจทั้ง 2 ทางที่ส่งลิงก์** (ลืมรหัสผ่าน **และ** เปลี่ยนอีเมล) · ลืมตั้งแล้วจดหมายจะมีลิงก์แบบ `/verify-email.php?token=…` ซึ่งกดจากกล่องจดหมายไม่ได้ ขณะที่หน้าเว็บบอกว่า "ส่งลิงก์แล้ว" ⚠️ `.env.example` ส่งค่าว่างมา จึงพลาดได้ง่ายมาก
- ⚠️ **กรอกอีเมลเดิมของตัวเองต้องไม่ตอบว่า "ส่งลิงก์ยืนยันแล้ว"** — เดิม Service คืน success โดยไม่มี `message` แล้ว controller เติมข้อความปริยายให้ ผู้ใช้เห็นแถบเขียวแล้วนั่งรอจดหมายที่ไม่มีวันมา (เกิดแม้พิมพ์ตัวพิมพ์ใหญ่ต่างกัน เพราะ normalize แล้วเท่ากัน)
- ⚠️ **ข้อความของด่านกลางที่ผู้ใช้เห็นต้องเป็นภาษาไทยและบอกว่าต้องทำอะไรต่อ** — `ensure_valid_csrf_or_respond` / `ensure_post_request_or_respond` / `ensure_form_content_type_or_respond` ตอบผ่าน `api_respond()` ซึ่งในโหมดฟอร์มกลายเป็น flash บนหน้าจอ · เดิมขึ้น `Invalid CSRF token` กลาง UI ภาษาไทย (เกิดบ่อยสุดตอนเปิดหน้าค้างข้ามวัน) ⚠️ endpoint ที่ตอบ JSON ล้วน (`*-data.php`) ยังใช้อังกฤษได้ ⚠️ **เปลี่ยนข้อความแล้วต้องเพิ่มคำไทยใน `infer_http_status_from_error()` ด้วย**
- ⚠️⚠️ **"ระบบอีเมลยังไม่ได้ตั้งค่า" กับ "ตั้งค่าแล้วแต่ส่งไม่ออก" ต้องตอบคนละอย่าง**
  · **ยังไม่ได้ตั้งค่า** → ปฏิเสธตั้งแต่ต้น **ก่อน**จองโควตาและก่อนเขียนคำขอทับของเดิม (`ProfileService::changeEmail`) เพราะกดอีกกี่ครั้งก็ไม่มีทางสำเร็จ
  · **ตั้งค่าแล้วแต่ส่งไม่ออก** → บันทึกคำขอไว้ ตอบ 503 ให้รอสักครู่แล้วกดใหม่ (มีโอกาสสำเร็จ)
  · ⚠️ **วัดจริงตอนยังไม่แยกสองกรณี** (ระบบอีเมลปิดอยู่): ครั้งที่ 1 ได้ "ส่งอีเมลยืนยันไม่สำเร็จ **กรุณาลองใหม่อีกครั้ง**" → ทำตามทันทีกลับได้ "ส่งลิงก์บ่อยเกินไป กรุณารอ 1 นาที" = **สั่งให้ลองใหม่แล้วลงโทษที่ทำตาม** · ครบ 5 ครั้งถูกกันอีก 1 ชั่วโมง ทั้งที่รออีกกี่ชั่วโมงก็ไม่สำเร็จ · และคำขอเดิมที่ลิงก์ยังใช้ได้อยู่ถูกทับหายไปฟรี ๆ
  · ⚠️ **ข้อความ "ส่งไม่สำเร็จ" ต้องบอกให้ "รอสักครู่" ไม่ใช่ "ลองใหม่อีกครั้ง"** — คูลดาวน์คือ 1 นาที กดทันทีจะโดนกันเสมอ
  · ⚠️ **เซิร์ฟเวอร์ทดสอบปริยายคือ "ยังไม่ได้ตั้งค่า"** ถ้าปล่อยไว้ เทสต์ทั้งไฟล์จะไปตกทางนั้นหมดแล้ว **ทางที่สองไม่มีใครทดสอบเลย** ทั้งที่มันคือทางที่เกิดจริงบนเซิร์ฟเวอร์ (SMTP ล่มชั่วคราว) → `ControllerTestCase::serverEnvironmentOverrides()` ให้คลาสลูกเปิดอีเมลแล้วชี้ไปโฮสต์ `.invalid` ได้ (`GoalAndProfileEndpointTest` · `AuthEndpointTest` ใช้ทางนี้ · `EmailSystemNotConfiguredTest` ใช้ปริยาย)
  · ⚠️⚠️ **กติกานี้ใช้กับ "ลืมรหัสผ่าน" ด้วย — เดิมตกสำรวจ** (`AuthService::requestPasswordReset`) · เขียนกฎไว้ตอนแก้ทางเปลี่ยนอีเมลแล้วไม่มีใครไล่ดูทางอื่น (รูปแบบเดิมที่เตือนไว้ตลอดทั้งไฟล์นี้)
    · **ทางนี้ร้ายแรงกว่า** เพราะคนที่มาถึงหน้านี้คือคนที่เข้าระบบไม่ได้แล้ว ลิงก์รีเซ็ตคือประตูบานเดียวที่เหลือ
    · วัดจริงก่อนแก้ (เซิร์ฟเวอร์ที่ยังไม่ได้ตั้ง SMTP): ครั้งที่ 1 ได้ "ระบบส่งอีเมลไม่สำเร็จในขณะนี้ **กรุณาลองใหม่อีกครั้ง**" → ทำตามทันทีกลับได้ "ลองขอรีเซ็ตรหัสผ่านบ่อยเกินไป กรุณารอ 1 นาที" · **เพดานของทางนี้คือ 1 ครั้ง/นาที กินโควตา 1 คือกินหมดพอดี** · และ `createToken()` ทำให้ลิงก์ใบก่อนหน้าที่ผู้ใช้อาจถืออยู่ตายฟรี ๆ
    · ตอนนี้ปฏิเสธที่ Service **ก่อน** `reserveAttempt()` และก่อนสร้าง token · controller เหลือหน้าที่เดียวคือแปลงเป็น 503
  · ⚠️⚠️ **เทสต์ที่ส่ง `emailService = null` เข้า `AuthService` เขียวโดยไม่ได้ตรวจอะไรแล้ว** — คำขอถูกปฏิเสธตั้งแต่ต้น ไม่เคยเดินไปถึงตรรกะ token/โควตาที่ชื่อเทสต์บอก · วัดจริง: `testRepeatedPasswordResetRequestsAreThrottled` **ยังเขียวแม้ถอดตัวกันออกทั้งตัว** → ใช้ `Tests\Integration\StubEmailService` แทน null ทุกที่ (ไม่ override `isEnabled()` โดยตั้งใจ ให้กติกาตัวจริงตัดสิน)
- ⚠️⚠️ **หน้าที่รับ token ห้ามพิมพ์คำแนะนำตายตัว — ต้องมาจากสาเหตุจริง** (`verify-email.php` · `$hintMessage` เลือกจาก `reason` ที่ `ProfileService::confirmEmailChange()` คืนมา)
  · เดิมทุกกรณีที่ล้มเหลวพิมพ์บรรทัดเดียวกัน "ลิงก์อาจหมดอายุหรือถูกใช้ไปแล้ว … ขอลิงก์ใหม่ได้จากหน้าโปรไฟล์" ซึ่ง**ขัดกับหัวข้อที่อยู่เหนือมันบนจอเดียวกันใน 2 กรณีจาก 6**:
    - **ลิงก์ของบัญชีอื่น** — หัวข้อบอกว่าลิงก์ยังดี ให้ออกจากระบบแล้วกดใหม่ · บรรทัดล่างบอกว่าลิงก์เสียแล้วให้ไปขอใหม่ ซึ่ง**ขอไม่ได้ เพราะเจ้าของลิงก์คือคนอื่น**
    - **อีเมลปลายทางถูกใช้แล้ว** — บอกให้ขอลิงก์ใหม่ไปที่อีเมลเดิม ซึ่งล้มเหมือนเดิมทุกครั้ง · สิ่งที่ต้องทำคือเปลี่ยน**อีเมลปลายทาง**
  · ⚠️ **ห้ามให้หน้าเว็บเดาสาเหตุจากข้อความไทยที่ Service คืนมา** — ข้อความเปลี่ยนเมื่อไหร่ คำแนะนำเพี้ยนเงียบ ๆ · Service คืน `reason` (`bad_link` · `email_taken` · `system_unavailable` · `failed`) หน้าเว็บเติม `another_account` เอง
  · หลักเดียวกับ `extremes_not_comparable_text()` — **ห้ามเดาสาเหตุแทนข้อมูล**
- **เปลี่ยนรหัสผ่าน/อีเมลในหน้าโปรไฟล์ต้องล้าง token รีเซ็ตที่ค้างอยู่ด้วย** (`ProfileService::revokePasswordResetTokens()`) — คู่กับ `AuthService::resetPassword` ที่ลบอยู่แล้ว ไม่งั้นลิงก์เก่ายังเขียนทับรหัสใหม่ได้
⚠️ **ห้ามใช้ชื่อ placeholder ซ้ำในคำสั่ง SQL เดียว** — `EMULATE_PREPARES=false` ทำให้ MySQL ตอบ `HY093` แล้ว query ล้มเงียบ (เคยทำให้ rate limit ตายทั้งระบบ)
- **Security extra:** CSV export กัน formula injection (เติม `'` หน้า cell ที่ขึ้นต้น `= + - @ \t \r`) — **guard นี้อยู่ใน controller `api/export.php` (closure `$sanitizeCsvCell`) ไม่ใช่ `ExportService` → unit-test ที่ระดับ service ไม่ได้ ต้องเทสต์ผ่าน integration ที่ยิง endpoint จริง**; ⚠️ **guard ทำเฉพาะคอลัมน์โน้ต** (ช่องเดียวที่ผู้ใช้พิมพ์ — ตำแหน่งมาจาก `note_column_index` ใน payload) เซลล์ที่ระบบสร้าง (วันที่ ISO/ตัวเลข/%) ออกดิบ เพื่อให้ Excel อ่านเป็นตัวเลข/วันที่ ไม่ใช่ข้อความ; reset token เก็บเป็น hash + TTL, security headers เซ็ตใน bootstrap

## Logic ที่อยู่ที่ controller/view (ไม่ใช่ service)

> จุดที่ business logic หลุดออกจาก Service มาอยู่ที่ controller/page — ต้องรู้ก่อนแก้ (verified จากโค้ดจริง)

- **Rate-limit ของ profile:** การเดารหัสผ่านปัจจุบันยังอยู่ใน `api/profile.php` เป็น closure session-based (5 ครั้ง/60s → 429) และนับเฉพาะ `credential_failure` · แต่การส่งลิงก์เปลี่ยนอีเมลใช้ `RateLimitRepository` + ตาราง `auth_rate_limits` ต่อผู้ใช้: 1 ครั้ง/60s และ 5 ครั้ง/ชั่วโมง
- ⚠️ **`includes/header.php` ถูก include กลางหน้า — ตัวแปรอยู่ scope เดียวกับเพจ** ห้ามใช้ชื่อที่เพจใช้ (`$shopId`, `$userId`, `$selectedMonth` …) ในนั้น เคยใช้ `$shopId` ในลูปเลือกร้านแล้วทับค่าของเพจด้วย "ร้านสุดท้ายในรายการ" · โค้ดของเพจที่อยู่ **หลัง** `require header.php` จะได้ค่าผิดทั้งหมด (มีเทสต์ `ShopContextGuardTest` ล็อกไว้)
- **ฟอร์มที่เขียนข้อมูลทุกอันต้องมี `shop_context_field($shopId)` คู่กับ `csrf_field()`** — 7 ฟอร์มใน `add-record.php` (3), `history.php` (2), `dashboard.php` (2) · controller ตรวจด้วย `ensure_shop_context_or_respond()` ตอบ 409 เมื่อ session ชี้คนละร้านกับตอนที่หน้าถูกเรนเดอร์ ⚠️ guard ต้องวาง **หลัง** `$shopId` ถูกกำหนดค่า · ฟอร์มที่ไม่ส่งค่านี้มาจะผ่านโดยตั้งใจ (กันอุบัติเหตุ ไม่ได้กันการโจมตี — สิทธิ์ยังตรวจที่ Service)
- **ฟอร์มหลักในหน้าบันทึกเติมค่าเดิมของวันนั้นเสมอ** (`RecordService::getRecordForDate()` ฝั่งเซิร์ฟเวอร์ + JS ดึงผ่าน `api/month-grid.php` ตอนเปลี่ยนวัน) — การบันทึกเป็น upsert ที่เขียนทับทุกช่อง ถ้าไม่เติมโน้ตเดิมกลับมา การแก้แค่ยอดขายจะลบโน้ตทิ้ง
- ⭐ **`normalize_money_string()` ใน `includes/functions.php` คือจุดเดียวของกฎการอ่านตัวเลขเงิน** — ฟอร์มเดี่ยว · ตารางหลายวัน · เป้าหมาย · ไฟล์ CSV ต้องผ่านตัวนี้ทั้งหมด (`parse_decimal_input()` เรียกต่อ) · **คู่แฝดฝั่ง JS** = `cleanAmountCell()` ใน `add-record.php` ต้องให้ผลเหมือนกันทุกค่า
  · รับ: `1234` `1234.56` `1234,56` `1,234.56` `1.234,56` `1,234,567.89` `1 234,56` `฿1,234.56`
  · **ปฏิเสธ (ห้ามเดา):** `1.234` / `1,234` (อ่านได้ทั้งหลักพันและทศนิยม 3 ตำแหน่ง) · `1.2.3` · `1..2` · `1e3` · ทศนิยมเกิน 2 ตำแหน่ง — หลักเดียวกับ "วันที่กำกวม"
  · ⚠️ ประวัติ: เคยลบจุลภาคทิ้งเสมอ (`1234,56` → 100 เท่า) แล้วเปลี่ยนเป็นเดาตัวคั่น (`1.234` → 1000 เท่า) **ห้ามกลับไปเดาอีก**
- **`monthly_goals`: ช่องที่ไม่ได้กรอกมา = คงค่าเดิม ไม่ใช่ล้างทิ้ง** (`GoalService::upsertGoal` อ่านของเดิมใต้ล็อกก่อนเขียน) — ฟอร์มที่เปิดค้างไว้เคยล้างเป้าที่อีกแท็บเพิ่งตั้ง · จะล้างทั้งเดือนใช้ปุ่ม "ลบเป้าหมาย"
- **โน้ต: "ช่องว่าง" ต่างจาก "ไม่มีคอลัมน์"** — ตัวกันของ `rejectBlankCellsOverwritingExistingDays()` ใช้ธง `note_was_missing` (ไฟล์ CSV ที่ไม่มีคอลัมน์โน้ตเลย) ไม่ใช่ "ค่าที่ส่งมาว่าง" · ตารางกรอกหลายวันเติมโน้ตเดิมให้เห็นก่อน (JS `change` ของ `record_date[]`) การล้างจึงเป็นการตั้งใจ ⚠️ **แก้แล้ว: วันอนาคตที่มีข้อมูลอยู่แล้วเติมให้ได้** — `buildEditableMonthGrid()` ส่ง `future_days_with_records` แยกคีย์มาให้ฟอร์มวันเดียวใช้ (ตารางกรอกหลายวันยังอ่านจาก `days` ซึ่งตัดที่วันนี้เหมือนเดิม) · **เดิมเตือนอย่างเดียวแล้วปล่อยให้โน้ตหาย** ทั้งที่ข้อมูลมีให้ใช้อยู่แล้ว — "เตือนผู้ใช้" ไม่ใช่การแก้บั๊ก
- ⚠️⚠️ **ตารางกรอกหลายวันเคยลบโน้ตเดิมทิ้งเงียบ ๆ พร้อมตอบว่าสำเร็จ** — ทั้งที่ไฟล์ CSV ที่ทำสิ่งเดียวกันเป๊ะ ๆ ถูกปฏิเสธ
  · ตารางเติมโน้ตเดิมกลับมาให้เห็นก่อน ช่องว่างจึงแปลว่า "ตั้งใจล้าง" — **แต่จริงเฉพาะเมื่อการเติมนั้นเกิดขึ้นจริง**
  · ⚠️ ตัวเติมเคยไม่ทำงานใน 2 ท่าที่ใช้จริงบ่อยที่สุด: (1) พิมพ์ยอดก่อนแล้วค่อยเลือกวันที่ — เงื่อนไข "มีช่องไหนกรอกแล้วให้ออกทันที" ข้ามช่องโน้ตที่ผู้ใช้ไม่ได้แตะไปด้วย · (2) วางจาก Excel — `input.value = …` **ไม่ทำให้ event `change` ทำงาน** ตัวเติมจึงไม่เคยถูกเรียกเลย (ตอนนี้ยิง event เองหลังวาง ทั้งสองทางเดินโค้ดชุดเดียวกัน)
  · ⚠️ **ฝั่งเซิร์ฟเวอร์ต้องมีตัวกันด้วย** — ธง `note_checked[]` ต่อแถว: โหลดข้อมูลเดือนไม่สำเร็จแล้วจะทับโน้ตที่มีอยู่ = ปฏิเสธทั้งชุดด้วยข้อความเดียวกับ CSV · เห็นโน้ตเดิมแล้วลบเอง = ล้างได้ตามปกติ (ไม่งั้นลบโน้ตผ่านตารางไม่ได้เลยตลอดกาล)
- **ตัวกัน "เว้นช่องแล้วของเดิมหาย" ใช้กับทุกทางที่เขียนข้อมูล** (`rejectBlankCellsOverwritingExistingDays()`) — ทั้ง CSV และตารางกรอกหลายวัน · คุมทั้ง รายได้/ค่าแอด/**โน้ต** ⚠️ เตือนเฉพาะเมื่อ "มีของจริงจะหาย" (ค่าเดิม `0` หรือโน้ตว่าง ไม่นับ) และรายงาน **แถวที่ผู้ใช้เห็นเป็นแถวแรก** ไม่ใช่วันที่น้อยสุด
- **หน้าตั้งรหัสผ่านใหม่ต้องพา token กลับมาเมื่อบันทึกไม่ผ่าน** (`api/auth.php` → `/reset-password.php?token=…`) — ไม่งั้นพิมพ์รหัสยืนยันผิดครั้งเดียวจะถูกเด้งไปหน้า "ลืมรหัสผ่าน" พร้อมข้อความที่ไม่จริง และ `reset-password.php` ต้องไม่เขียนทับ flash error ที่ API ตั้งไว้
- ✅ **[เจ้าของระบบตัดสิน 2026-08-07] ช่องยอดที่เว้นว่างในไฟล์ CSV = ปฏิเสธ ไม่ใช่ 0**
  · เดิม CSV แปลงช่องว่างเป็น 0 ให้เอง โดยคอมเมนต์อ้างว่า "UI ก็ปล่อยว่างได้" ซึ่ง **ไม่จริง** — ทางเขียนอีกสองทางบังคับกรอกทั้งคู่ (ฟอร์มวันเดียว `required`+`parse_decimal_input` · ตารางกรอกหลายวัน `upsertManyRecords`) · CSV เป็นทางเดียวที่เดาแทนผู้ใช้
  · เหตุผลที่เปลี่ยน: **ความเสียหายสองฝั่งไม่เท่ากัน** — เดาเป็น 0 แล้วผิด ได้ "วันที่รายได้ ฿0 แต่จ่ายค่าแอดจริง" คือวันขาดทุนปลอมที่ไหลเข้าทุกรายงาน (กำไรเฉลี่ยต่อวัน · วันแย่สุด · ROAS · เป้า · กราฟ) โดยผู้ใช้ไม่รู้ตัว · ปฏิเสธแล้วผิด แค่พิมพ์ 0 เอง
  · ไฟล์ที่ระบบ export ออกไปไม่เคยมีช่องยอดว่าง ท่า "โหลดออกไปแก้ใน Excel แล้วนำเข้ากลับ" จึงไม่กระทบ
  · ⚠️ **ผลข้างเคียง: กิ่ง "ช่องว่างทับยอดจริง" ของ `rejectBlankCellsOverwritingExistingDays()` ไม่มีทางถูกกระตุ้นผ่าน CSV อีกแล้ว** (ตารางกรอกหลายวันไม่เคยตั้งธง `revenue_was_blank` อยู่แล้ว) · เก็บไว้เป็นด่านที่สองเผื่อมีคนคลายกติกากลับ · `RecordServiceOverwriteGuardTest` เรียก `upsertManyRecords()` ตรง ๆ พร้อมตั้งธงเอง และเขียนกำกับไว้ว่าเป็นการยืนยัน **สัญญาของเมธอด** ไม่ใช่เส้นทางที่ผู้ใช้เดินได้จริง
  · ⚠️ กิ่ง **โน้ต** ยังทำงานปกติ (ไฟล์ที่ไม่มีคอลัมน์โน้ตเลย + ธง `note_checked[]` ของตาราง) — ห้ามลบตัวกันนี้ทิ้ง
- **สูตร profit/ROAS ซ้ำใน view:** `RecordService->getRecentRecords()` คืน row ดิบ → `add-record.php` (ตาราง "รายการล่าสุด") คำนวณ profit/ROAS เองในเพจ ⚠️ **แก้สูตร profit/ROAS ต้องตามไปอัปเดต view นี้ด้วย ไม่ใช่แค่ service**
- **`$today` seam:** service ที่ผลลัพธ์ขึ้นกับวันที่ (`RecordService`, `AnnualService`, `OverviewAnnualService`, `ExportService`, `DashboardService`) รับ `?string $today = null` ท้าย param ไว้ให้เทสต์ล็อกวันได้ — **ถ้าเพิ่ม logic ที่อ่านวันที่ ต้องรับ seam ต่อไปด้วย** ไม่งั้นบางส่วนอ้างวันนี้จริงขณะที่ส่วนอื่นใช้ค่าที่ส่งเข้ามา (เคยเป็นบั๊กใน `DashboardService::resolveRange`) · ใน `RecordService` ใช้ `resolveToday()` ตัวเดียว (เคยคัดลอกบล็อกเดิมไว้ 4 ที่)
- ✅ **[เจ้าของระบบตัดสิน 2026-08-06] `daily_records` เป็น actuals เท่านั้น — ห้ามบันทึกวันอนาคต**
  · ยืนยันด้วยข้อความ: "บันทึกข้อมูลวันอนาคตไม่ได้แล้ว ไม่ต้องบันทึกล่วงหน้าก็ได้"
  · **อย่าย้อนกลับเป็น "ลงล่วงหน้าได้"** — คอมเมนต์เก่าในโค้ดที่เขียนว่า "ลงวันที่ล่วงหน้าทำได้ (บางคนตั้งใจ)" ไม่ใช่กติกาปัจจุบันแล้ว
  · ⚠️ **แต่โค้ดที่รองรับ legacy future rows ต้องคงไว้** — ข้อมูลที่ลงไว้ก่อนกติกานี้ยังอยู่ในฐานข้อมูลได้ ถ้าถอดออก แถวพวกนั้นจะแก้/ลบผ่านหน้าเว็บไม่ได้อีกเลย ได้แก่:
    - `buildEditableMonthGrid()` → `future_days_with_records` (ฟอร์มวันเดียวเติมค่าเดิมได้)
    - `history.php` เปิดเดือนอนาคตได้เมื่อเดือนนั้นมีข้อมูลจริง (ไม่งั้นลบแถวเก่าไม่ได้)
    - รายงาน/analytics ปีปัจจุบันตัดที่วันนี้ (ไม่ให้แถวเก่าโผล่ในยอดรวม)
- **`daily_records` เป็น actuals เท่านั้น** — `RecordService::upsertRecord()` / `upsertManyRecords()` / `updateRecord()` ปฏิเสธ `record_date` ที่มากกว่าวันนี้ · ถ้ามี legacy future rows จากข้อมูลเก่าหรือ fixture, report/analytics ปีปัจจุบันต้องตัดที่วันนี้ และ "วันล่าสุด" ยังต้องหมายถึงวันล่าสุดที่ ≤ วันนี้
- **ทุกเพจต้องเรียก `resolve_current_shop_id($pdo, $userId)` ก่อนดึงข้อมูล** แทนการอ่าน `$_SESSION['current_shop_id']` ตรง ๆ — helper ซ่อม session ที่ชี้ไปร้านที่ถูกลบไปแล้ว (จากอุปกรณ์อื่น) และตั้ง flash บอกผู้ใช้ว่าถูกสลับร้านให้ · เดิมการซ่อมอยู่ใน `header.php` ที่ include ท้ายไฟล์ หน้าจึงขึ้น "คุณไม่มีสิทธิ์เข้าถึงร้านค้านี้" + ฿0 หนึ่งครั้งก่อนหายเอง · `shop_context_for_user()` cache ต่อ request จึงไม่ยิง query ซ้ำ
- **`ShopService::createShop` เช็ก "ชื่อซ้ำ" ก่อน "โควตา 20 ร้าน" เสมอ** — ชื่อที่มีอยู่แล้วคือการสลับไปร้านนั้น ไม่ได้สร้างแถวใหม่ โควตาจึงไม่ควรมาขวาง (เดิมผู้ใช้ที่ครบ 20 ร้านพิมพ์ชื่อร้านตัวเองไม่ได้เลย)
- **ทศนิยมของค่าเงินเกิน 2 ตำแหน่งถูกปฏิเสธ** (`RecordService::hasTooManyDecimals()` · `AMOUNT_DECIMALS`) ใช้ร่วมกับ `GoalService` — ไม่งั้น MySQL ปัดให้เงียบ ๆ แล้วระบบรายงานว่าสำเร็จ ⚠️ เทียบด้วย epsilon ไม่ใช่สตริง เพราะ `0.1 + 0.2` = `0.30000000000000004`
- **เพดานค่าเงิน `9,999,999,999.99`** = `RecordService::MAX_AMOUNT` และ `GoalService::MAX_AMOUNT` (alias ของตัวแรก) — คอลัมน์เป็น `DECIMAL(12,2)` ทั้งคู่ ขยับต้องขยับพร้อม schema
- **JavaScript ในหน้าบันทึก (ไม่มี test runner — ต้อง verify มือทุกครั้งที่แก้):**
  · ตัวเลือกเดือนของตาราง **ต้องเชื่อ `data.month` ที่เซิร์ฟเวอร์ส่งกลับ** ไม่ใช่ค่าที่ผู้ใช้พิมพ์ — เดือนอนาคตถูก clamp เงียบ ๆ ทำให้ตารางเต็มไปด้วยข้อมูลเดือนปัจจุบันขณะที่ช่องเลือกยังโชว์เดือนอนาคต
  · การข้ามแถวหัวตารางตอนวางจาก Excel **ต้องรองรับการวางที่คอลัมน์ตัวเลขด้วย** (`isNumericCell`) — ท่าที่ใช้จริงคือกดเติมวันที่ขาดแล้ววางเฉพาะคอลัมน์ยอด ถ้าไม่ข้ามหัวตาราง ยอดทุกตัวเลื่อนไปผิดวัน
  · วางกว้างเกินตารางต้องเตือน ไม่ทิ้งเงียบ · ปุ่ม "เติมวันที่ขาด" ต้องถามก่อนล้างตาราง · ข้อความ hint ต้องคืนค่าเดิมหลังโหลดสำเร็จ · เปลี่ยนวันเร็ว ๆ ต้องกันผลลัพธ์เก่ามาทับ (`pendingRequestId`)
- ⚠️⚠️ **ฟังก์ชัน JS ที่ใช้ข้ามก้อน `<script>` ต้องประกาศในก้อนแรกและนอก IIFE** (ที่เดียวกับ `MONTH_GRID_URL` / `loadMonthData` ใน `add-record.php`)
  · เกิดขึ้นจริง: ย้าย `todayIso()` ไปประกาศใน IIFE ของก้อนล่าง → คำเตือน "วันอนาคต" ของตารางกรอกหลายวัน (อยู่ก้อนบน) **พังทั้งหมด** และขึ้นข้อความผิดว่า "โหลดข้อมูลเดิมไม่สำเร็จ" เพราะ ReferenceError ถูกกลืนด้วย `catch` · คอมมิตที่ตั้งใจ *เพิ่ม* คำเตือนให้ฟอร์มหลัก กลับ *ย้าย* คำเตือนออกจากตาราง
  · ⚠️ **`BrowserScriptParityTest` เดิมจับไม่ได้** เพราะเอาทุกก้อนมาต่อกันเป็นสตริงเดียวแล้วไม่รู้จัก scope — ตอนนี้ไล่ทีละก้อนตามลำดับ (ก้อนก่อนหน้ามองเห็นได้ · ก้อนหลังมองย้อนไม่ได้) ⚠️ ยังไม่ครอบกรณี "ก้อนเดียวกันแต่คนละ IIFE"
  · ⚠️ ตอนเขียนตัวตรวจนี้พลาดซ้ำ: เขียน `$visible[$name] = isset($known[$name])` เฉย ๆ ทำให้จุดเรียกในก้อนหลัง (ซึ่งมองเห็น) ไปลบผลของก้อนหน้า (ซึ่งมองไม่เห็น) ทิ้ง — ต้องเป็น `&&` สะสม
- ⚠️⚠️⚠️ **`<input type="number">` ทิ้งค่าที่ไม่ใช่ตัวเลขเงียบ ๆ — กติกา "คืนค่าดิบให้เซิร์ฟเวอร์ปฏิเสธ" จึงใช้ไม่ได้กับช่องเงิน**
  · `cleanAmountCell()` ตั้งใจคืน "ค่าดิบ" เมื่ออ่านไม่ออก เพื่อให้เซิร์ฟเวอร์ปฏิเสธพร้อมข้อความเดียวกันทุกทาง — แต่ค่าดิบไปไม่ถึงเซิร์ฟเวอร์ เพราะเบราว์เซอร์ลบทิ้งตั้งแต่ตอนกำหนดค่า
  · ⚠️ **วัดจริง**: Excel ที่ตั้งรูปแบบเงินแบบมีจุลภาคไม่มีสตางค์ (`12,500` `9,800`) ซึ่งเป็นรูปแบบมาตรฐานของเงินบาท → ก๊อปทั้งคอลัมน์มาวาง **ทุกช่องว่างหมด ไม่มีข้อความเตือนเลย** · ไม่ทันสังเกตแล้วกดบันทึก = วันใหม่ถูกบันทึกเป็นรายได้ ฿0
  · ⚠️ **ห้ามแก้ด้วยการทำให้ตัวอ่านรับ `12,500`** — มันกำกวมจริง (12500 หรือ 12.5) และเคยทำให้ยอดผิด 1000 เท่ามาแล้ว · ต้อง **เตือนผู้ใช้** แทน (`unreadableAmounts` ในตัววาง)
  · วิธีจับ: หลังกำหนดค่าแล้วเช็ก `input.value === '' && cell !== ''` — ครอบทุกกรณีที่เบราว์เซอร์ปฏิเสธ โดยไม่ต้องเดาว่าค่าไหนอ่านออก
  · ⚠️⚠️⚠️ **แค่นับแล้วเตือนยังไม่พอ — ต้องติดธง `data-unreadable` ไว้ที่ช่องนั้นด้วย**
    · ตัวเติมค่าเดิมแยก "ว่างเพราะผู้ใช้ไม่ได้กรอก" กับ "ว่างเพราะเบราว์เซอร์ทิ้งค่าที่ผู้ใช้วาง" ไม่ออก → เห็นว่าว่างก็เติม **ยอดเดิมของวันนั้น** ลงไป
    · ⚠️ **วัดจริงจนถึงฐานข้อมูล**: วาง `12,500 / 4,200` ทับ 1–3 ส.ค. → บนจอกลายเป็น `5,000 / 2,000` (ของเดิม) ดูเหมือนกรอกครบ · กดบันทึก → **ยอดที่วางมาไม่เคยถูกบันทึกเลย** และฐานข้อมูลไม่เปลี่ยนแม้แต่แถวเดียว
    · ผู้ใช้แตะช่องนั้นเมื่อไหร่ ธงหลุดทันที (ตัวฟัง `input` ตัวเดียวกับ `data-prefilled`)
  · ⚠️⚠️ **คำเตือนตอนวางต้อง "ติดหนึบ" — ไม่งั้นถูกลบทิ้งภายในเสี้ยววินาที**
    · การวางจบด้วยการยิง `change` ให้ทุกแถว → ตัวเติมค่าเดิมทำงานต่อแล้วเรียก `showBulkNotice()` ซึ่งเขียนทับกล่องเดียวกัน · วัดจริง: คำเตือน "มี 6 ช่องจำนวนเงินที่อ่านไม่ได้" หายไปเหลือแต่ "มีข้อมูลอยู่แล้ว — ระบบเติมค่าเดิมไว้ให้" ซึ่งอ่านแล้วสบายใจ ทั้งที่ยอดถูกทิ้งไปหมดแล้ว
    · แยก 2 ชนิด: **เล่าเหตุการณ์** (วางเกิน 31 แถว · กว้างเกินตาราง) จบในตัว · **อธิบายสภาพที่ยังคาอยู่บนจอ** (อ่านยอด/วันที่ไม่ได้) ต้องอยู่ต่อ
    · ⚠️ ต้อง **หายเอง** เมื่อไม่มี `[data-unreadable]` เหลือแล้ว ไม่งั้นคำเตือนเก่าโผล่ซ้ำทุกครั้งที่มีข้อความใหม่
- ⚠️⚠️⚠️ **ตัวเติมค่าเดิมต้องแยก "ค่าที่ระบบเติม" ออกจาก "ค่าที่ผู้ใช้พิมพ์เอง"** (`data-prefilled`)
  · กติกา "ห้ามทับสิ่งที่ผู้ใช้พิมพ์เอง" ถูกต้องแล้ว แต่เดิมแยกสองอย่างนี้ไม่ออก · พอระบบเติมค่าของวันหนึ่งไปแล้ว ช่องครบทั้ง 3 ช่อง ด่าน "กรอกครบแล้วให้ออกทันที" จึงทำงานพอดี → **เปลี่ยนวันแล้วไม่มีอะไรเกิดขึ้นเลย**
  · ⚠️ **วัดจริงจนถึงฐานข้อมูล** (เลือกผิดเป็น 1 ส.ค. → รู้ตัวแก้เป็น 3 ส.ค. → กดบันทึก):
    - บนจอ: วันที่ **3 ส.ค.** แต่ยอดเป็นของ **1 ส.ค.** (5,000 / 2,000 / "โน้ตเดิมวันที่ 1")
    - ข้อความใต้ตารางยังเขียนว่า **"วันที่ 2026-08-01 มีข้อมูลอยู่แล้ว…"** ทั้งที่ไม่ได้เลือกวันนั้นแล้ว
    - ผลลัพธ์: 3 ส.ค. จาก `4,000 / 3,000 / ไม่มีโน้ต` **ถูกทับด้วยข้อมูลของ 1 ส.ค. ทั้งแถว**
    - ธง `note_checked[]` ยังเป็นของวันเก่า ด่านฝั่งเซิร์ฟเวอร์จึงยอมให้ผ่าน
  · วิธีแก้: ติดธง `data-prefilled` ตอนระบบเติม · ผู้ใช้แตะช่องไหนธงหลุดทันที (`input` listener) · เปลี่ยนวันแล้วล้างเฉพาะของที่ระบบเติมก่อนโหลดวันใหม่
  · ⚠️ ต้องทดสอบ **ทั้งสองทาง**: "แก้วันที่หลังระบบเติมให้แล้ว" ต้องเปลี่ยนตาม · "พิมพ์ยอดเองก่อนแล้วค่อยเลือกวัน" ต้องไม่ถูกล้าง
- ⚠️⚠️⚠️ **ตัวแปร JS ที่ประกาศ "ลึกกว่า" จุดที่ใช้ = ReferenceError ที่เทสต์ทุกตัวมองไม่เห็น**
  · เกิดขึ้นจริงและขึ้นเซิร์ฟเวอร์ไปแล้ว: ตัวยิง event หลังวางจาก Excel เขียน `rows.forEach(...)`
    แต่ `rows` ประกาศอยู่ **ข้างใน** callback ของ `grid.forEach` → **พังทุกครั้งที่วาง**
    แล้วโค้ดหลังจุดนั้นตายหมด · ข้อมูลยังลงช่องได้ (ส่วนนั้นทำงานก่อนจุดพัง) ผู้ใช้จึงไม่เห็นอะไรผิด
  · ⚠️ **สิ่งที่หายไปคือตัวกันทั้งหมดที่อยู่หลังจุดนั้น** (วัดจริงบนเบราว์เซอร์):
    - โน้ตเดิมไม่ถูกเติมกลับ → วางทับวันที่มีโน้ตแล้วกดบันทึก **โน้ตหายพร้อมข้อความว่าสำเร็จ**
      (ทางเลือกวันที่เองเติมให้ปกติ — ข้อมูลชุดเดียวกัน สองทางเข้า ผลต่างกัน)
    - คำเตือน 3 แบบไม่ขึ้นเลย: วางเกิน 31 แถว · วางกว้างเกินตาราง · วันที่อ่านไม่ออก/กำกวม
  · ⚠️ `php -l` ไม่เห็น · PHPStan ไม่เห็น · ตัวกวาดฟังก์ชันไม่เห็น (เพราะ `rows` ไม่ได้ตามด้วยวงเล็บ)
    **จับได้ตอนเปิดเบราว์เซอร์จริงเท่านั้น**
  · ✅ ตอนนี้มี `testEveryVariableTheScriptReadsIsDefined()` — คำนวณ **เส้นทางขอบเขตจริง**
    ของทุกตำแหน่ง แล้วตรวจว่าตัวที่ประกาศอยู่ในขอบเขตแม่ของจุดที่ใช้ไหม
  · ⚠️ **ตัวกวาดนี้ล้มเหลว 3 รอบก่อนใช้ได้จริง — ถ้าจะแก้ ต้องมิวเทชันเทสต์ทุกครั้ง:**
    1. ไม่รู้จักพารามิเตอร์ของฟังก์ชัน → รายงานผิด 7 หน้า
    2. `if (rowIndex >= rows.length) {` ทำให้ `rows` ถูกนับว่า "ประกาศตรงนั้น" → เขียวทั้งที่พัง
       (ต้องข้าม `if/for/while/switch` ก่อนเก็บพารามิเตอร์)
    3. วัดแค่ "ความลึก" แยกตัวแปรชื่อเดียวกันคนละฟังก์ชันไม่ได้ → ยังเขียว
- ⚠️ **ลบฟังก์ชัน JS ต้องตามไปลบจุดที่เรียกมันด้วย** — เคยเกิดจริง: การรวมกติกาอ่านตัวเลขลบ `const isNumericCell = …` ทิ้งแต่ทิ้งจุดเรียกไว้ ผลคือ **วางยอดจาก Excel ลงคอลัมน์รายได้แล้วไม่มีอะไรเกิดขึ้นเลย** (ReferenceError กลางคัน) ซึ่งเป็นท่าที่ผู้ใช้ทำบ่อยที่สุด และไม่มีอะไรจับได้เพราะ JS ไม่มี test runner · ตอนนี้ `BrowserScriptParityTest::testEveryFunctionTheScriptCallsIsDefined()` กวาดทุกชื่อที่ถูกเรียกในบล็อก `<script>` เทียบกับชื่อที่ประกาศไว้ (ตัดคอมเมนต์และโค้ด PHP ที่แทรกอยู่ออกก่อน)
- **`isNumericCell()` ต้องไม่พึ่ง `cleanAmountCell()`** — ตัวหลังคืน "ค่าดิบ" เมื่ออ่านไม่ชัด (เช่น `1.234` ที่กำกวม) ซึ่งยังหน้าตาเหมือนตัวเลขอยู่ดี ถ้าตอบว่า "ไม่ใช่ตัวเลข" แถวข้อมูลแรกจะถูกนับเป็นหัวตารางแล้วหายไป · มันมีหน้าที่เดียวคือ **แยกหัวตารางออกจากข้อมูล** ไม่ใช่ตัวอ่านค่า
- **วันที่กำกวมถูกปฏิเสธทั้งสองฝั่ง:** `05/03/2026` (ทั้งสองเลข ≤ 12) อ่านได้ทั้ง D/M และ M/D → PHP `RecordService::isAmbiguousSlashDate()` (import CSV) และ JS `parseDateCell()` ใน `add-record.php` (วางจาก Excel) คืน null เหมือนกัน · JS ใช้ `looksLikeDateCell()` แยกต่างหากสำหรับเช็กแถวหัวตาราง — **ห้ามเอา `parseDateCell` ไปเช็ก header** ไม่งั้นแถวข้อมูลแรกที่วันกำกวมจะถูกทิ้ง
- ⚠️⚠️ **"เทียบปีก่อน (ช่วงเดียวกัน)" ต้องตัดถึง *วันเดียวกัน* ไม่ใช่แค่เดือนเดียวกัน**
  · วัดจริง: ร้านที่ทำกำไรวันละ ฿1,000 เท่ากันเป๊ะทั้งปี 2025 และ 2026 · วันนี้ 7 ส.ค. → ปีนี้ ฿219,000 (219 วัน) เทียบปีก่อน ฿243,000 (**243** วัน) = ป้าย **↓9.9%** · ตัวเลขนี้ "ดีขึ้นเอง" ทุกวันจนเป็น 0.0% เฉพาะวันสุดท้ายของเดือน
  · ใช้ `comparison_range_end()` กับเดือนสุดท้ายของปีก่อน (หดวันตัดให้พอดีเดือนสั้น/ปีอธิกสุรทิน)
  · ⚠️ **`getMonthlyTotalsByShopIdsAndMonthRange()` เดิมไม่มีพารามิเตอร์ `$notAfterDate` เลย** ทั้งที่ฝาแฝดแบบร้านเดียวมี — ทุกจุดที่เรียกมันจึงได้ยอดทั้งเดือนขณะที่ตัวเลขข้าง ๆ บนจอเดียวกันตัดที่วันนี้ (หน้ารายปี · หน้ารวมร้านมุมรายปี · กราฟ 6 เดือน · กริดฤดูกาล)
  · ⚠️ **stub ที่ละเลย `$notAfterDate` ทำให้เทสต์เขียวทั้งก่อนและหลังแก้** — ต้องจำลองการตัดวันจริง
- ⚠️ **การ์ด "เทียบวันในสัปดาห์" เทียบกับเดือนของ *รายการล่าสุด* ไม่ใช่เดือนปัจจุบันเสมอไป** — ห้ามเขียน "เดือนนี้" ตายตัวในหน้าเว็บ · วัดจริง: เดือน ส.ค. ยังไม่มีข้อมูลเลย แต่การ์ดขึ้น "วันอังคาร**เดือนนี้**เฉลี่ย ฿8,000 (จาก 3 วัน)" ซึ่งเป็นข้อมูล ก.ค. ขณะที่ตารางใต้การ์ดบนจอเดียวกันว่างเปล่า · Service คืน `window_month` / `window_is_current_month` มาให้หน้าเว็บเรียกชื่อเดือนจริง
- ✅ **[เจ้าของระบบตัดสิน 2026-08-07] หน้ารวมร้านแท็บรายวัน: "กรอกครบ" = ครบทุกร้านที่ *เริ่มบันทึกแล้ว*** (ไม่ใช่ทุกร้านที่มี — ไม่งั้นร้านใหม่ที่เพิ่งเริ่มกรอกจะทำให้ทุกวันก่อนหน้ากลายเป็น "ไม่ครบ" ย้อนหลังทั้งหมด)
  · ⚠️ **ตัวหารที่หน้าเว็บโชว์ต้องเป็นตัวเดียวกับที่ตัดสินว่าครบ** (`shops_tracked`) — เดิมโชว์ `1/3 ร้าน` (ร้านทั้งหมด) โดยไม่มีเครื่องหมายเตือน คู่กับสรุปที่เขียนว่า "กรอกครบทุกร้าน" บนจอเดียวกัน
- ✅ **[เจ้าของระบบตัดสิน 2026-08-07] แถวเก่าที่เป็นวันอนาคต แก้ยอดได้ถ้าไม่เปลี่ยนวันที่** — กติกาคือห้าม *สร้าง* ข้อมูลวันอนาคต การแก้ยอดของแถวที่มีอยู่แล้วไม่ได้สร้างอะไรใหม่ · เดิมกดแก้แค่ยอดก็ได้ error "วันที่ต้องไม่อยู่ในอนาคต" ซึ่งชี้ไปที่ช่องที่ไม่ได้แตะ ทำได้อย่างเดียวคือลบทิ้ง · **ย้ายวันไปข้างหน้ายังห้ามเหมือนเดิม**
- **การเทียบ "เดือนนี้ vs เดือนก่อน" ตัดที่วันเดียวกันทุกหน้า** — `resolve_comparison_cutoff_day()` + `comparison_range_end()` ใน `includes/functions.php` ใช้ร่วมกันทั้ง `DashboardService` และ `OverviewService` · เดิมมีแค่แดชบอร์ดที่ตัดวัน หน้ารวมร้านจึงบอก −87.1% ขณะที่แดชบอร์ดบอก 0% จากข้อมูลชุดเดียวกัน ⚠️ **แก้แล้ว: ตอนนี้ตารางหน้ารวมร้านตัดที่วันนี้ด้วย** (`OverviewService.php:52`) ข้อความเดิมที่ว่า "ยังโชว์ทั้งเดือน" ไม่ตรงกับโค้ดแล้ว
- **ทุกตัวเลขของ "เดือนนี้" ตัดที่วันนี้เหมือนกันหมด** — การ์ดสรุป · การ์ดเป้าหมาย (`buildGoalProgress`) · คอลัมน์กำไรในหน้ารวมร้าน (`OverviewService`) · ป้ายเทียบเดือนก่อน ทุกจุดผ่าน `resolve_comparison_cutoff_day()` + `comparison_range_end()` ⚠️ **เพิ่มจุดใหม่ที่แสดงยอดของเดือนปัจจุบัน ต้องผ่านคู่นี้ด้วย** ไม่งั้นการ์ดสองใบในหน้าเดียวกันจะบอกคนละเรื่อง (เคยเป็น ฿4,000 กับ "ถึงเป้าแล้ว 100%")
- ⚠️ **"เดือนนี้" ตัดที่วันนี้ — แต่ไม่ใช่ทุกที่ กติกาต่างกันโดยตั้งใจ**
  · **ตัดที่วันนี้:** การ์ดสรุป · การ์ดเป้าหมาย **ทั้งของแดชบอร์ดและของหน้ารายปี/Excel** (`AnnualService::totalsUpTo()`) · กราฟ 6 เดือนของแดชบอร์ด · คอลัมน์กำไรหน้ารวมร้าน · ป้ายเทียบเดือนก่อน
  · **ตัดที่วันนี้:** ยอดรวมทั้งปี ตารางรายเดือน และแผ่นรายวันของหน้ารายปี/Excel สำหรับปีปัจจุบัน — daily_records เป็น actuals แล้ว จึงต้องไม่รวมวันที่ยังไม่เกิดขึ้น
  · เกิดขึ้นจริงตอน policy เก่า: Excel/annual เคยนับแถวล่วงหน้าในเดือนนี้เพื่อ parity กันเอง แต่ขัดกับ dashboard/goal; policy ปัจจุบันให้ parity กันโดยตัดที่วันนี้ทั้งหมด
  · `RecordRepository::getMonthlyTotalsByMonthRange()` รับ `?string $notAfterDate` — **ผู้เรียกส่งวันมาเอง** ห้ามให้ repository อ่านวันนี้เอง ไม่งั้น seam `$today` ของ service พัง (บางส่วนอ้างวันจริง บางส่วนใช้วันที่เทสต์กำหนด)
  · ⚠️ **เทสต์ที่ใช้ตัวจำลอง repository พิสูจน์ SQL ไม่ได้** — `FutureRowsConsistencyTest` เดิม stub `getMonthlyTotalsByMonthRange` ให้คืน `[]` เสมอ ทั้งที่หน้ารายปี · Excel · กราฟ 6 เดือน ใช้ query นี้ทั้งหมด เทสต์ชื่อ "รายการล่วงหน้าต้องไม่ขัดกันเอง" จึงครอบแค่ 2 จาก 5 จุด · ต้องมี integration test คู่กันเสมอ (`RecordRepositoryTest`)
  · ⚠️ **แท็บ "รายวัน" ของหน้ารวมร้านต้องใช้คู่ helper เดียวกัน** — `OverviewDailyService` เดิมไล่ `Y-m-t` เสมอและ **ไม่มี seam `$today` เลยทั้งคลาส** กดสลับแท็บเฉย ๆ ยอดรวมเปลี่ยนจาก ฿8,000 เป็น ฿14,000 และแถวสุดท้ายเป็นวันที่ยังมาไม่ถึง (วัดจริงแล้ว)
  · ⚠️ **หน้ารายปีอ่านเป้าหมายด้วย `getByShopAndMonthRange()` ไม่ใช่ `findByShopAndMonth()`** — stub ผิดตัวแล้วการ์ดเป้าหมายจะว่างเปล่า เทสต์เขียวเพราะไม่มีอะไรให้ตรวจ
- **`month_pick` ที่เป็นเดือนอนาคตถูกหดเป็นเดือนปัจจุบัน** — ไม่ใช่ปล่อยให้ clamp ทำช่วงกลับหัว (`start_date > end_date` → ทุกการ์ดเป็น ฿0 ขณะที่ตัวเลือกยังโชว์เดือนอนาคต)
- **ไฟล์ Excel ใช้รูปแบบเงินเดียวกันทุกแท็บ** (`"฿"#,##0.00`) — เดิมแท็บสรุป/รายเดือนตัดสตางค์ทิ้ง ค่าเดียวกันจึงอ่านได้ ฿100 ในแท็บหนึ่งและ ฿100.40 ในอีกแท็บของไฟล์เดียวกัน
- ⚠️ **กราฟ "ย้อนหลัง 6 เดือน" มีป้ายของตัวเอง จึงต้องจบที่เดือนปัจจุบันเสมอ** — เดิมเอา "วันสิ้นสุดของช่วงที่ผู้ใช้เลือก" มาเป็นจุดจบของกราฟด้วย · เลือกช่วงกำหนดเอง 1 ก.ค.–31 ธ.ค. (วันนี้ 7 ส.ค.) ได้แท่ง ก.ค.→ธ.ค. คือ **4 ใน 6 เดือนยังมาไม่ถึง** (บนเครื่องจริงเป็น ฿0 กราฟดิ่งลงศูนย์) และ **แท่งเดือนปัจจุบันไม่ถูกตัดที่วันนี้** จึงขัดกับการ์ดสรุปที่อยู่เหนือมัน · ช่วงที่จบในอดีตยังย้อนหลังจากเดือนนั้นได้ตามปกติ
- **ช่วงของแดชบอร์ดไม่เลยวันนี้** (`clampRangeEndToToday()`) — `week_this`/`month_this`/`month_pick` ตัดที่วันนี้ · เดิมการ์ดรวมยอดวันอนาคตขณะที่ป้ายใต้การ์ดตัดที่วันนี้ (การ์ด ฿9,000 คู่กับป้าย 0% และ "วันดีสุด" เป็นวันที่ยังมาไม่ถึง) ⚠️ **ช่วง `custom` ไม่ถูกตัด** — ผู้ใช้เลือกเองย่อมตั้งใจ · ความคืบหน้าเป้ายังใช้ช่วงเดียวกับการ์ดสรุป
- **สูตร % เป้าและ "ถึงเป้าไหม" อยู่ที่ `GoalService::progressPercent()` / `isReached()`** — ใช้ร่วมกันทั้งแดชบอร์ด หน้ารายปี และ Excel · เดิมแดชบอร์ดปัดลงแต่หน้ารายปีปัดขึ้น หน้ารายปีจึงขึ้น 100.0% คู่กับ "ยังไม่ถึงเป้า" · **เป้า 0 ไม่นับว่าถึงเป้า** (เดิมสองหน้าตอบต่างกัน)
- **`%` เปลี่ยนแปลงของ ROAS คิดจากค่าดิบ ไม่ใช่ค่าที่ปัดแล้ว** — ปัดก่อนหาผลต่างทำให้ 0.05% กลายเป็น 0.5% และป้ายเปลี่ยนจากเทาเป็นเขียว
- **`formatMoney()` แสดงสตางค์เฉพาะตอนมีเศษ** — เดิมตัดทิ้งทุกช่อง ทำให้แถวรวมไม่เท่ากับผลบวกของแถวที่เห็น (฿100 ×3 แต่รวม ฿301)
- **สัดส่วนกำไรของแถวรวม = 100% ตามนิยาม** ห้ามบวกค่าที่ปัดแล้วทีละแถว (3 ร้านเท่ากัน → 99.9%) · **"วันที่กรอก" ของแถวรวมใช้ค่ามากสุด ไม่ใช่ผลบวกข้ามร้าน** (3 ร้าน × 31 วัน ≠ 93 วันในเดือนที่มี 31 วัน)
- **`%` ความคืบหน้าเป้าปัดลง** (`DashboardService::calculateGoalPercent` ใช้ `floor`) — `round` ทำให้ 99.996% ขึ้นเป็น 100.0% คู่กับป้าย "ยังไม่ถึงเป้า" ในการ์ดเดียวกัน (ป้ายเทียบค่าจริง)
- ⚠️ **`resolve_safe_redirect_path()` ต้องกันสแลชกลับหัวด้วย** — เบราว์เซอร์อ่าน `/\evil.com/x` เหมือน `//evil.com/x` คือพาออกนอกเว็บได้ · เดิมกันแค่ `//` และ **ไม่มีเทสต์เลยทั้งตัว** ทั้งที่ 3 endpoint ใช้คุม `Location:` และค่านั้นถูกคำนวณ *ก่อน* ด่าน CSRF · ตอนนี้แปลง `\` เป็น `/` ก่อนตรวจ (`SharedHelperContractTest` ล็อกไว้)
- ⚠️ **helper 4 ตัวนี้เพิ่งมีตาข่ายเป็นครั้งแรก** (`SharedHelperContractTest`) — ก่อนหน้านี้ทำให้พังได้โดยเทสต์ทั้งชุด 1,000 ตัวยังเขียว: `comparison_range_end()` (หดวันตัดให้พอดีเดือนสั้น) · `formatMoney()` (สตางค์เฉพาะตอนมีเศษ) · `resolve_safe_redirect_path()` · `WEEKDAY_MIN_SAMPLE`
  · ⚠️ **เทสต์เดิมปักวันไว้ที่ 4 ส.ค. ทุกไฟล์** ซึ่งเดือนก่อน (ก.ค.) มี 31 วัน `min(4,31)` กับ `4` จึงเท่ากันพอดี — **แยกสองพฤติกรรมไม่ได้เลยไม่ว่าจะเขียน assert กี่ตัว** ต้องใช้วันที่เดือนก่อนสั้นกว่า (เช่น 31 มี.ค. เทียบ ก.พ.)
- **ป้าย `↑/↓ %` ทุกที่ต้องผ่าน `format_change_badge()`** ใน `includes/functions.php` — 5 จุด (`history.php`, `dashboard.php`, `annual.php`, `overview.php` ×2) เคยเขียนเองแล้วไม่ตรงกัน บางที่ `>= 0` จึงขึ้น "↑ 0.0%" สีเขียวทั้งที่ยอดเท่าเดิม · helper ตัดสินจากค่า **หลังปัด 1 ตำแหน่ง** ให้ลูกศรตรงกับเลขที่เห็นเสมอ
- ⚠️⚠️⚠️ **กติกาของ "รายงาน" ต้องลงถึงไฟล์ Excel ด้วยเสมอ ไม่ใช่แค่หน้าจอ**
  · คอมมิตที่แก้กริดฤดูกาลไม่ให้ตัดสินเดือนที่ยังไม่จบ แก้ `annual.php` + `AnnualService` + เทสต์ **แต่ไม่ได้แตะ `XlsxReportService`** → หน้าจอระบายเทา แต่ไฟล์ยังเขียวเหมือนอีกสองปี · คนเปิดไฟล์อ่านคอลัมน์ ส.ค. เป็น 31,000 → 31,000 → 7,000 แล้วสรุปว่าเดือนนี้ตกไป 77% ทั้งที่เพิ่งผ่าน 7 จาก 31 วัน
  · ⚠️ **ทับสีทีหลังไม่ได้ผล** — ใน Excel กฎสีอัตโนมัติ (conditional formatting) ชนะสีพื้นที่ตั้งตรง ๆ ต้องไม่ให้กฎครอบช่องนั้นตั้งแต่แรก (ใส่กฎทีละแถวแล้วตัดช่องนั้นออก)
  · ⚠️ **จำนวนเงินในช่องที่เป็น "ประโยค" ต้องใช้ `formatMoney()`** ไม่ใช่ `number_format()` ดิบ ไม่งั้นในแผ่นเดียวกันจะมี `฿219,000.00` (ช่องตัวเลข) อยู่ไม่กี่บรรทัดเหนือ `219,000.00` (ช่องข้อความ)
  · ⚠️ **แถวรวมท้ายตารางต้องมีเหมือนหน้าเว็บ** — แท็บ "เทียบร้าน" เคยไม่มี ทำให้ยอดขายรวม/ค่าแอดรวมของทุกร้านไม่ปรากฏที่ไหนเลยทั้งไฟล์ (บล็อกสรุปมีแต่กำไร)
  · ⚠️ **แถวรวมที่เพิ่งเพิ่มก็ต้องเงียบเมื่อไม่มีข้อมูล** — แถวรวมนี้เคยเขียน "สัดส่วนกำไร 100.0%" ขณะที่ทุกแถวเหนือมันเว้นว่าง (ไม่มีร้านไหนมีรายได้) · ตัวเลขที่ "จริงตามนิยาม" ยังต้องมีข้อมูลรองรับถึงจะพิมพ์ได้
  · ⚠️ **กติกา "ดีสุด/แย่สุดต้องคนละเดือน" ไม่เคยลงถึงไฟล์เลย** — `describeMonthExtremes()` เพิ่งมี · ก่อนหน้านี้ไฟล์เขียน "ก.ค. (฿30,000)" เป็นทั้งเดือนดีสุดและแย่สุดของปีเดียวกัน เกิดกับทุกร้านใน 1–2 เดือนแรกที่เริ่มใช้ระบบ
  · ⚠️ **ไฟล์ต้องรู้จัก 3 สาเหตุของ "ไม่มี % เทียบปีก่อน"** (`describeMissingYoy()`) — มีข้อมูล/ไม่มีข้อมูล/**อ่านไม่สำเร็จ** · หน้าเว็บแยกไว้แล้วแต่ไฟล์ยุบสองกรณีหลังเป็นอันเดียว
- ⚠️⚠️ **กราฟต้องใช้กติกาเดียวกับตารางที่อยู่เหนือมัน** — เดิมตารางดันร้านที่ไม่มีข้อมูลไปท้าย แต่กราฟแท่ง/กราฟเส้นยังวาดเป็น ฿0 ซึ่ง **อยู่เหนือร้านที่ขาดทุนจริงทั้ง 6 เดือน** อ่านว่าเป็นร้านที่ดีที่สุด · ลำดับเส้นก็ต้องเรียงเหมือนตาราง (ส่ง `$comparisonRows` ไม่ใช่ `$shops`) · กราฟแท่งรายวันต้องทำเครื่องหมายวันที่กรอกไม่ครบ (`is_complete`) เพราะข้อความเหนือกราฟบอกว่าเทียบไม่ได้อยู่แล้ว
  · ⚠️ ชุดสีของกราฟต้องมีอย่างน้อย 20 สี (เพดานจำนวนร้าน) — เดิมมี 6 ร้านที่ 7 จึงได้สีเดียวกับร้านที่ 1
- ⚠️ **เทสต์ไฟล์ Excel ห้ามล็อกเลขแถว/เลขเซลล์ตายตัว** — เพิ่มแถวรวม 1 แถวทำให้เทสต์ 8 ตัวใน 2 ไฟล์แดงทั้งที่พฤติกรรมถูกต้องขึ้น · ให้ค้นหาแถวจากป้ายกำกับแล้วอ่านค่าแทน (ล็อกสิ่งที่ต้องเป็นจริง ไม่ใช่ล็อกการจัดวาง)
- ⚠️⚠️ **กติกา "เดือนที่ยังไม่จบยังตัดสินไม่ได้" ใช้กับกริดฤดูกาลด้วย** — เดิมใช้แค่กับการ์ด · ยอดของเดือนปัจจุบันเป็นแค่บางส่วน (ตัดที่วันนี้) ระบายสีเทียบกับเดือนที่จบแล้วไม่ได้ · วัดจริง: ร้านที่ทำกำไรวันละเท่ากันทุกวัน ช่องเดือนปัจจุบันได้ความเข้มต่ำสุดที่ระบบมี (0.12) ขณะที่เดือนอื่น 0.50–0.55 → กริดบอกว่าเดือนนี้แย่สุดของปี · `is_unfinished` ต้องกันทั้งการระบายสี **และ** การคำนวณค่าสูงสุดที่ใช้ normalize
- ⚠️⚠️⚠️ **เขียนกติกาซ้ำ = จุดที่พังแน่นอน — ต้องเรียกกติกาตัวเดิมเสมอ**
  · ✅ **รวมกติกาที่เคยกระจาย 12 ที่ ให้เหลือ 4 ตัวกลางใน `includes/functions.php` แล้ว** — ทั้งสี่เคยพังจริงเพราะเขียนซ้ำ:
    - `month_is_unfinished()` (เดิม 3 ที่) — ที่หนึ่งลืมเงื่อนไข "ยังไม่ถึงวันสุดท้ายของเดือน"
    - `compare_shop_rows_for_ranking()` (เดิม 2 ที่) — มุมรายปีไม่มีกติกา "ร้านที่ไม่มีข้อมูลไปท้าย" เลย
    - `extremes_are_comparable()` (เดิม 4 ที่) — มีแค่ระดับวัน ระดับเดือนตกสำรวจ
    - `extremes_not_comparable_text()` (เดิม 3 ที่) — ข้อความเดาสาเหตุเอาเอง (ดูหัวข้อถัดไป)
    - `resolve_month_allowing_legacy_future()` (เดิม 2 ที่ + เงื่อนไขซ้ำอีก 1 ที่ในข้อความแจ้งผู้ใช้) — เคยมีที่เดียวแล้วปุ่ม Export ตกสำรวจ **กดแล้วได้ไฟล์คนละเดือนกับที่หน้าจอแสดงอยู่** · ตอนนั้นแก้ด้วยการคัดลอกกติกาไปใส่อีกที่ ซึ่งแก้อาการได้แต่ทิ้งความเสี่ยงเดิมไว้
  · ✅ **`month_is_unfinished()` คือจุดเดียวของกติกา "เดือนนี้จบหรือยัง"** (เดิมเขียนไว้ 3 ที่) · `SharedHelperContractTest` ล็อกกติกา 11 กรณีขอบ **และมีตัวกวาดห้ามเขียนซ้ำ** (`testNobodyReimplementsTheUnfinishedMonthRule`) ซึ่งแดงแม้โค้ดที่เขียนซ้ำจะให้ผลถูกต้องทุกประการ — เพราะปัญหาอยู่ที่วันที่มีคนแก้ที่หนึ่งแล้วอีกที่ไม่ตาม
  · วัดจริง: กริดฤดูกาลเขียนเงื่อนไข "เดือนนี้จบหรือยัง" ขึ้นใหม่ โดยลืมเงื่อนไข "ยังไม่ถึงวันสุดท้ายของเดือน" ที่กติกาตัวจริงมีอยู่ (`$isUnfinishedCurrentMonth`) → **ในวันสุดท้ายของทุกเดือน** การ์ดยกให้เดือนนี้เป็นเดือนกำไรดีสุด ขณะที่กริดใต้การ์ดบนจอเดียวกันยังระบายเทาว่า "ยังตัดสินไม่ได้" · และค่าที่ใช้ normalize ความเข้มตัดเดือนที่กำไรสูงสุดของปีออก ทำให้ทุกช่องที่เหลือเข้มเกินจริง
  · เจอด้วยการตรวจการถดถอย 703 การเปรียบเทียบ — เจอจุดเดียวและเป็นจุดที่เพิ่งเขียนวันนั้นเอง
- ⚠️ **การ์ดคู่ "ดีสุด/แย่สุด" ต้องเป็นคนละ *เดือน* ด้วย ไม่ใช่แค่คนละวัน** — ร้านที่มีเดือนที่จบแล้วเดือนเดียว (ทุกร้านใน 1–2 เดือนแรกที่ใช้ระบบ) เห็นเดือนเดียวกันตัวเลขเดียวกันโผล่สองการ์ด ที่หนึ่งเขียว ที่หนึ่งเทา · ตัวกันมีอยู่แล้วที่ระดับวัน ระดับเดือนตกสำรวจ
- ⚠️⚠️ **ยอดรวมที่บวกในภาษา PHP ต้องผ่าน `money_total()` (ปัดเป็นสตางค์) ก่อนใช้ต่อ — 11 จุด ใน 7 ไฟล์**
  · เงินเป็น `DECIMAL(12,2)` แต่ PHP เก็บเป็นทศนิยมฐานสอง · **หน้าไหนบวกเอง กับหน้าไหนใช้ `SUM()` ของ MySQL จึงได้คนละค่า** — เป็นสาเหตุของ "สองหน้าตอบไม่ตรงกัน" ที่หายากที่สุด เพราะ `formatMoney()` ปัดตอนแสดงผลอยู่แล้ว **ตัวเลขบนจอจึงเท่ากัน ต่างกันเฉพาะตอนเอาไปเทียบ**
  · ⚠️ วัดจริง (ส.ค. 2569 กรอก 7 วัน `72328.46 63411.33 81239.59 60534.09 80981.37 77855.05 63650.11` · เป้ารายได้ ฿500,000 ซึ่งลงตัวพอดีเป๊ะ): แดชบอร์ดบวกได้ `499999.99999999994` → **"เป้ารายได้ 99.9%" · ไม่ขึ้น "🎉 ถึงเป้าแล้ว" · "เหลือ 24 วัน ต้องได้อีกวันละ ฿0"** ทั้งสามบรรทัดอยู่ในการ์ดใบเดียวกัน ขณะที่หน้ารายปีและไฟล์ Excel (ใช้ `SUM()`) บอก **"✓ ถึงเป้า (100.0%)"**
  · เกิดกับพฤติกรรมที่ตั้งใจทำ (เล็งเป้ากลม ๆ แล้วกรอกวันสุดท้ายให้ครบ) ซึ่งเป็น **เส้นแบ่งที่สำคัญที่สุดของฟีเจอร์เป้าหมายพอดี** · สุ่ม 200,000 เดือน: ผลบวกของ PHP ต่ำกว่าค่าเป๊ะ **32.1%** ของเดือน (ทิศทางเดียว ไม่มีทางสูงเกิน)
  · ⚠️ **ปัดครั้งเดียวตอนจบ ไม่ใช่ปัดทีละแถวระหว่างบวก** — ปัดทีละแถวคือการเปลี่ยนตัวเลขของผู้ใช้
  · ⚠️ **เทสต์ต้องยืนยันก่อนว่าข้อมูลตั้งต้นทำให้เกิดเศษจริง** (`assertNotSame($target, $naive)`) ไม่งั้นชุดตัวเลขที่บังเอิญบวกลงตัวจะทำให้เทสต์เขียวโดยไม่ได้ตรวจอะไรเลย
- ⚠️⚠️ **ข้อความ "ยังเทียบไม่ได้" ห้ามบอกสาเหตุที่เดาเอา** (`extremes_not_comparable_text()`)
  · `best` = เดือนกำไรสูงสุด · `worst` = ต่ำสุด → ชี้เดือนเดียวกันได้ **2 กรณี**: มีเดือนที่จบแล้วเดือนเดียว **หรือ ทุกเดือนที่จบแล้วกำไรเท่ากันหมด** · เดิมเขียนตายตัวว่า "มีเดือนที่จบแล้วเดือนเดียว"
  · วัดจริง: ร้านที่กรอก ม.ค.–ก.ค. กำไรเดือนละ ฿19,800 เท่ากันเป๊ะ → การ์ดขึ้น "มีเดือนที่จบแล้วเดือนเดียว" ขณะที่บรรทัดถัดมาบนจอเดียวกันเขียน **"8 เดือนมีข้อมูล"** และตารางใต้การ์ดแสดง **7 แถว** ของเดือนที่จบแล้ว · **การซ่อนการ์ดถูกแล้ว คำอธิบายผิด**
- ⚠️ **ข้อความบอกเกณฑ์ต้องตรงกับเกณฑ์จริง** — "ต้องมีอย่างน้อย 2 เดือนที่กรอกแล้ว" ผิด · เกณฑ์จริงของประมาณการคือ **2 เดือนที่กรอกเกินครึ่งเดือน** (`AnnualService`: `$daysCount * 2 < $daysInMonth` → ตัดทิ้ง) · คนที่ยิงแอดไม่ทุกวัน (กรอกเดือนละ 12–14 วัน) เห็น "8 เดือนมีข้อมูล" คู่กับข้อความนี้ตลอดไปโดยไม่มีทางรู้ว่าต้องทำอะไร
- ⚠️ **"มีข้อมูลไหม" ต้องนับจากจำนวนแถว ไม่ใช่จากยอดเงิน** — `$hasAnnualData`/`$hasDailyData`/`$hasYearlyData`/`$hasOverviewData` · วันหยุดหรือวันที่ไม่ได้ยิงแอดถูกกรอกไว้เป็น 0 อย่างถูกต้อง แต่เกณฑ์ที่ดูยอดเงินนับว่า "ไม่มีข้อมูล" → **"ปี 2569 ยังไม่มีข้อมูล" คู่กับตารางที่แสดง "3 วัน" บนจอเดียวกัน**
- ⚠️ **"อ่านข้อมูลไม่สำเร็จ" ≠ "ไม่มีข้อมูล"** — `catch` แล้วตั้งเป็นอาร์เรย์ว่างทำให้หน้าเว็บบอกว่า "ไม่มีข้อมูลปีก่อน" คู่กับ "ปีก่อนช่วงเดียวกัน ฿0" ทั้งที่ปีก่อนมีกำไรจริง ฿219,000 และไม่มีแถบแดงเตือนเลย · ใช้ `prev_year_unavailable` แยก
- ⚠️ **ป้าย "ใหม่" ในตารางเทียบร้านต้องไม่ผูกกับ "% เป็น null"** — % เป็น null ได้ทั้งตอนไม่มีข้อมูลเดือนก่อน **และ** ตอนเดือนก่อนเท่าทุนพอดี · ร้านที่กรอกมา 2 ปีครึ่งจึงถูกป้ายว่า "ใหม่" (`prev_has_data` แยกให้)
- ⚠️ **ปีที่จบไปแล้วไม่มี "เดือนที่ยังไม่จบ" ให้รอ** — ข้อความ "รอให้จบเดือนก่อน" ต้องใช้เฉพาะปีปัจจุบัน ไม่งั้นขัดกับแถบ "ปีนี้ยังไม่มีข้อมูล" บนจอเดียวกัน
- ⚠️ **การ์ดคู่ "ดีสุด/แย่สุด" ต้องเป็นคนละวันจริง ๆ ถึงจะโชว์** — ข้อมูลวันเดียว (หรือทุกวันกำไรเท่ากัน) ทำให้วันเดียวกันโผล่สองการ์ด ที่หนึ่งเขียว ที่หนึ่งแดง · เป็นสิ่งแรกที่ผู้ใช้ใหม่เห็น · ตัวเลขจาก service ถูกตามคณิตศาสตร์ ที่ผิดคือการเอามาโชว์เป็นขั้วตรงข้าม (ตารางกำไรตามวันมีตัวกันนี้อยู่แล้ว)
- ⚠️ **ห้ามเขียน "เดือนนี้/ปีนี้" ตายตัวในข้อความ "ยังไม่มีข้อมูล"** — ผู้ใช้เลือกดูเดือน/ปีอื่นได้ · เปิด `?month=2025-05` แล้วขึ้น "เดือนนี้ยังไม่มีข้อมูล" ทั้งที่ตัวเลือกเดือนบนจอโชว์ 2025-05 · เคยพลาด 5 จุด (แดชบอร์ดเขียนถูกอยู่แล้วว่า "ในช่วงเวลานี้")
- ⚠️ **"ครบทุกวัน" ต้องนับวันที่ไม่มีใครกรอกเลยด้วย** — query คืนเฉพาะวันที่มี record วันที่ไม่มีใครกรอกจึงไม่โผล่เป็นแถวและไม่ถูกนับว่าไม่ครบ · วัดจริง: 2 ร้านกรอกแค่ 1–3 ส.ค. แถวรวมบอก "ครบทุกวัน" ขณะที่แดชบอร์ดบนข้อมูลชุดเดียวกันเตือน "ไม่ได้กรอกมา 4 วันแล้ว" (`missing_days`)
- ⚠️⚠️ **การ์ดเป้าหมาย: "วันที่เหลือ" ห้ามนับวันนี้ซ้ำ ถ้าวันนี้กรอกไปแล้ว** — ยอดของวันนี้อยู่ใน actual แล้ว · วัดจริง: เป้า ฿100,000 กรอกครบ 1–7 ส.ค. ได้ ฿7,000 → แนะนำ "เหลือ 25 วัน วันละ ฿3,720" ทำตามเป๊ะ ๆ ได้ ฿96,280 **ขาดไปหนึ่งวันพอดี** · ที่ถูกคือ 24 วัน × ฿3,875 · ยังไม่กรอกวันนี้ = ยังนับวันนี้ (`$todayAlreadyRecorded`)
- ⚠️⚠️ **กราฟ 6 เดือน: แต่ละแท่งแทน "ทั้งเดือน" — ตัดที่วันนี้เท่านั้น ห้ามตัดที่ปลายช่วงที่ผู้ใช้เลือก** · กดสลับ "สัปดาห์นี้" เป็น "สัปดาห์ก่อน" เฉย ๆ แท่งเดือนเดียวกันเปลี่ยนจาก ฿7,000 เป็น ฿2,000 · เลือกช่วงเอง 1–10 มี.ค. แท่ง มี.ค. เหลือ ฿10,000 แทน ฿31,000 (แก้รอบแรกครอบแค่ฝั่งอนาคต ฝั่งอดีตยังผิดอยู่)
- ⚠️ **การ์ด "เดือนกำไรดีสุด/แย่สุด" ไม่นับเดือนปัจจุบันที่ยังไม่จบ** (`AnnualService` และ `OverviewAnnualService` — ต้องเหมือนกันทั้งคู่) · การ์ดเทียบด้วย **ยอดสะสม** เดือนที่เพิ่งกรอกไม่กี่วันจึงชนะเดือนที่กรอกครบเสมอ · ร้านที่ทำกำไรวันละ ฿1,000 เท่ากันทุกเดือนเห็น "เดือนแย่สุด = เดือนนี้" ตลอดต้นเดือน ทั้งที่ตารางในหน้าเดียวกันแสดง `profit_per_day` เท่ากันเป๊ะ · **ยังไม่จบเดือน = ยังตัดสินไม่ได้ ไม่ใช่ "แย่"** (หลักเดียวกับที่หน้ารวมร้านดันร้านที่ยังไม่มีข้อมูลไปท้ายตาราง) — **เป็นการตัดสินใจของเจ้าของระบบ** เมื่อ 2026-08-05
  · ⚠️ กติกานี้ถูกล็อกไว้ใน 4 ไฟล์เทสต์ · เปลี่ยนกฎแล้วต้องแก้ให้ครบ และ **ต้องเพิ่มเดือนที่จบแล้วเข้าไปในข้อมูลทดสอบ ไม่ใช่แก้แค่ตัวเลขคาดหวัง** ไม่งั้นเทสต์อ่อนลงจนไม่ได้พิสูจน์อะไร (เจอมาแล้วตอนแก้รอบนี้)
- ⚠️⚠️ **กติกา "ร้านที่ยังไม่มีข้อมูลไปท้ายตาราง" ต้องใช้กับ *มุมรายปี* ด้วย** — เดิมบังคับใช้แค่ `OverviewService` (มุมเดือน) ส่วน `OverviewAnnualService` เรียงด้วยกำไรล้วน · วัดจริง (ร้าน A/B กรอกครบ 219 วันแต่ขาดทุน · ร้าน C ไม่เคยกรอก): **อันดับ 1 = ร้าน C ฿0** ขณะที่แท็บรายเดือนของหน้าเดียวกัน ข้อมูลชุดเดียวกัน ตอบตรงกันข้าม · ไฟล์ Excel ชีต "เทียบร้าน" ใช้ลำดับนี้จึงผิดตามไปด้วย
- ⚠️ **"ปีก่อนไม่มีข้อมูล" ≠ "ปีก่อนเท่าทุนพอดี"** — `yoy_profit_change_percent` เป็น `null` ทั้งสองกรณี (หารด้วยศูนย์ไม่ได้) แต่หน้าเว็บ/Excel เคยแปลเป็น "ไม่มีข้อมูลปีก่อน" ตายตัว → ปีก่อนที่กรอกครบทั้งปีแต่กำไรรวมเป็น ฿0 ก็ขึ้นแบบนั้น ขัดกับบรรทัดถัดลงมาที่พิมพ์ "ปีก่อนช่วงเดียวกัน ฿0" · ใช้ `prev_year_has_data` แยก (4 จุด: `annual.php` · `overview.php` · Excel 2 ชีต)
- ⚠️ **ปุ่ม Export ต้องได้เดือนเดียวกับที่หน้าจอแสดงอยู่** — `history.php` เปิดเดือนอนาคตที่มีข้อมูลจริงได้ (แถวเก่า) แต่ `api/export.php` เรียก `resolve_calendar_month()` ซึ่งหดกลับเป็นเดือนปัจจุบันเงียบ ๆ · วัดจริง: ดูหน้าประวัติเดือนหน้า 2 รายการ ฿110,000 กด Export ได้ไฟล์เดือนปัจจุบัน 3 แถว ฿3,000 ชื่อไฟล์ก็คนละเดือน โดยไม่มีข้อความบอก
  · ✅ **ตอนนี้กติกาอยู่ที่ `resolve_month_allowing_legacy_future()` ที่เดียว** (ผู้เรียกส่ง callback มาบอกว่าเดือนนั้นมีรายการไหม) · `SharedHelperContractTest` มีตัวกวาดห้ามเขียน `$requestedMonth > $selectedMonth` ซ้ำ
  · ⚠️ **ข้อความ "เดือนนี้ยังไม่มีรายการ ระบบจึงแสดง … ให้แทน" ต้องเทียบกับ *เดือนที่แสดงจริง*** (`$requestedMonth !== $selectedMonth`) ไม่ใช่เขียนเงื่อนไข "เดือนอนาคต" ขึ้นใหม่ — ไม่งั้นกลายเป็นสำเนาที่ 3 ของกติกาเดียวกัน
- ⚠️ **ตารางเทียบร้านต้องมีทุกร้านครบทั้ง 3 แท็บ** — `OverviewDailyService::buildShopRows()` เดิมสร้างแถวจาก *ผลลัพธ์ query* ร้านที่ยังไม่กรอกจึงหายไปทั้งแถว · **กดสลับแท็บเฉย ๆ ตารางมี 2 แถวในแท็บรายวัน แต่ 3 แถวในอีกสองแท็บ** · ตอนนี้ไล่จากรายชื่อร้านแล้วเรียงด้วย `compare_shop_rows_for_ranking()` เหมือนอีกสองแท็บ (ร้านที่ยังไม่กรอกอยู่ท้ายตาราง ไม่ใช่หายไป)
- **อันดับร้านในหน้ารวมร้าน:** `OverviewService::buildShopComparison` ดัน **ร้านที่ยังไม่มีข้อมูล (`days_count = 0`) ไปท้ายตารางเสมอ** — กำไร `0.0` ของร้านที่ไม่ได้กรอก "มากกว่า" ร้านที่ขาดทุนจริง จะขึ้นอันดับ 1 ทั้งที่ควรแปลว่า "ยังไม่รู้" · กำไรเท่ากันตัดด้วย `days_count` แล้วชื่อ (query ไม่การันตีลำดับ)
- ⚠️ **เดือนปัจจุบันที่เข้าฐานคำนวณประมาณการ ต้องถูกเทียบเป็น "ถ้าทำแบบนี้ทั้งเดือน" ก่อน** — เดือนอื่นในฐานเป็นกำไรเต็มเดือน ถ้าเอากำไรครึ่งเดือนไปเฉลี่ยปนตรง ๆ ค่าเฉลี่ยจะถูกลากลงทันทีที่เดือนนี้ผ่านครึ่งเดือน (เกณฑ์ที่ให้เข้าฐาน) · วัดจริง: ร้านทำกำไรวันละ ฿1,000 เท่ากันทุกวัน **15 ส.ค. ประมาณการ ฿362,484 → 16 ส.ค. เหลือ ฿299,742** ทั้งที่บันทึกวันธรรมดาเพิ่มอีก 1 วัน
  · ⚠️ **ขยายด้วย "วันที่ผ่านไปแล้ว" ไม่ใช่ "วันที่กรอก"** — สิ้นเดือนแล้วกรอก 28 จาก 31 วัน คือ *ไม่ได้กรอก* ไม่ใช่ *ยังไม่ถึงวัน* ขยายให้ = เดาแทนผู้ใช้ (เขียนผิดรอบแรกแล้วเทสต์เดิม 5 ตัวจับได้)
  · ⚠️ **ข้อมูลตั้งต้นของเทสต์ต้องเป็นไปได้จริง** — เทสต์เดิม 3 ที่ใส่ `days_count = 31` ของเดือน ส.ค. คู่กับ `$today = 4 ส.ค.` ซึ่งเกิดขึ้นไม่ได้ เคสที่พังจริงจึงไม่เคยถูกแตะ
- ⚠️⚠️ **ฐานประมาณการสิ้นปีเก็บเป็น "กำไรต่อวัน" ไม่ใช่ "กำไรทั้งเดือน" และตัวคูณคือ `remaining_days` ไม่ใช่จำนวนเดือน**
  · เดือนยาวไม่เท่ากัน (ก.พ. 28 · เม.ย. 30 · มี.ค. 31) การเทียบยอด **ทั้งเดือน** จึงอ่าน ก.พ. ว่าเป็น "เดือนที่แย่กว่า" ทั้งที่ผลงานต่อวันเท่ากันเป๊ะ · พอฐาน 3 เดือนล่าสุดเลื่อนไปแล้ว ก.พ. หลุดออก ค่าเฉลี่ยก็กระโดดขึ้นเอง
  · ⚠️ **วัดจริง** (ร้านทำกำไรวันละ ฿1,000 เท่ากันเป๊ะทุกวัน กรอกครบทุกวัน · เดินทีละวันทั้งปี):
    - 15 พ.ค. ฿357,978 → **16 พ.ค. ฿365,505 (+฿7,527 ข้ามคืน)** — ก.พ. หลุดออกจากฐาน
    - 15 มี.ค. ฿354,726 → **16 มี.ค. ฿359,516 (+฿4,790)** — เดือนปัจจุบันเข้าฐานเป็นเดือนที่ 3
    หลังแก้: **฿365,000 นิ่งทุกวันตลอดปี** (`testTheForecastDoesNotMoveWhenNothingChanged` เดินทั้ง 15 และ 16 ของทุกเดือน)
  · ตัวหาร = **วันที่ผ่านไปแล้ว** ของเดือนปัจจุบัน · **วันทั้งเดือน** ของเดือนที่จบแล้ว (เดือนที่จบแล้วแต่กรอกไม่ครบต้องใช้ค่าตามที่กรอกมา ไม่ใช่คูณขึ้น)
  · `months_remaining` / `current_month_remaining_days` ยังคงอยู่เพื่อให้ป้าย "เหลืออีก …" อ่านแล้วตรงกับปฏิทิน ⚠️ ป้ายนี้อยู่ทั้งใน `annual.php` และ `XlsxReportService` — **แก้ต้องแก้คู่**
  · ⚠️ **ข้อมูลทดสอบต้องเขียนเป็น "อัตราต่อวัน × จำนวนวันของเดือนนั้น"** ไม่ใช่ยอดเดือนลอย ๆ — ไม่งั้นแก้ตัวเลขคาดหวังให้ผ่านได้โดยไม่ได้พิสูจน์กติกา และเคสเดือนสั้นจะไม่เคยถูกเดินผ่าน
- **กริด/ชีต "ฤดูกาล" โผล่เมื่อมีข้อมูล ≥ 2 ปีเท่านั้น** — `buildMonthlyHeatmap()` คืน `comparable` มาให้ ทั้ง `annual.php` และ `api/export-xlsx.php` ใช้ค่านี้ตัดสิน (ปีเดียว = กริดว่าง 2 ใน 3 แถว ไม่ใช่ฤดูกาล)
- **`DashboardService` memo `getByDateRange` ต่อการเรียก 1 ครั้ง** (`fetchRecords()`) — โหลดแดชบอร์ดเคยยิงช่วง `เดือนนี้ทั้งเดือน` ซ้ำ 2 รอบ (การ์ดสรุป + ความคืบหน้าเป้า) · cache key มี `shopId` ด้วย และอ็อบเจ็กต์สร้างใหม่ทุก request จึงไม่ค้างข้ามคำขอ — **เพิ่มจุดอ่านใหม่ใน service นี้ให้เรียก `fetchRecords()` ไม่ใช่ repo ตรง**
- **`cron/cleanup-logs.php` กวาด `dirname(LOG_FILE)` ไม่ใช่ `<project>/logs`** — default ของ `LOG_FILE` คือ `sys_get_temp_dir()/ad-profit/php-error.log` เดิม cron hardcode ไปที่โฟลเดอร์ `logs/` ในโปรเจกต์ซึ่งมีแต่ `.gitkeep` (log จริงจึงโตไปเรื่อย ๆ) · `LogCleanupRepository` ลบเฉพาะไฟล์ที่หน้าตาเป็น log (`*.log`, `*.log.1`, `*.log-20260101`, และ `error_log` ที่ cPanel/Hostinger สร้างเอง) — เดิมลบทุกไฟล์ในโฟลเดอร์ ถ้ามีคนวาง `.htaccess` กันไม่ให้เข้าถึง log ผ่านเว็บ ไฟล์นั้นจะหายไปเงียบ ๆ
- **`bootstrap.php` ตรวจว่าเขียน `LOG_FILE` ได้ไหม ต้องถามถึง *ตัวไฟล์* ไม่ใช่แค่โฟลเดอร์** — บนโฮสต์จริงไฟล์ log มักถูกสร้างโดยผู้ใช้คนอื่น (root ตอน deploy, ตัวหมุน log ของ cPanel) แล้วเหลือสิทธิ์อ่านอย่างเดียว ขณะที่โฟลเดอร์ยังเขียนได้ · เงื่อนไขเดิมจึงผ่านแล้วชี้ `error_log` ไปที่ไฟล์นั้น = ข้อความของทั้งระบบหายเงียบ ทั้งที่โค้ดตรงนั้นเขียนขึ้นมาเพื่อกันกรณีนี้พอดี (`ConfigEnvironmentTest` ล็อกไว้ · ข้ามเองถ้ารันเป็น root เพราะ `chmod` ไม่มีผล)
- **คอลัมน์ "เทียบครั้งก่อน" ใน CSV ต้องไม่มีตัวคั่นหลักพัน** — คอลัมน์ตัวเลขอื่นส่ง `''` เป็น separator อยู่แล้ว เฉพาะช่องนี้ที่เคยใช้ค่า default ทำให้ค่าโต ๆ ออกมาเป็น `+9,999,900.0%` ที่ Excel อ่านเป็นสูตรผิดไวยากรณ์
- **ข้อความ error ตอน import CSV ใช้ "แถวที่ N" ไม่ใช่ "บรรทัดที่ N"** — `fgetcsv` คืน 1 record ต่อครั้ง โน้ตที่มีขึ้นบรรทัดใหม่ในเครื่องหมายคำพูดกินหลายบรรทัดในไฟล์ เลขนี้จึงตรงกับเลขแถวใน Excel (สิ่งที่ผู้ใช้เปิดดูจริง) ไม่ตรงกับเลขบรรทัดของไฟล์ดิบ
- **PHPStan ครอบ "ทุกไฟล์ PHP ในโปรเจกต์" แล้ว (177 ไฟล์)** รวมหน้าเว็บที่รากและ `cron/` (`dashboard.php`, `overview.php`, `annual.php`, `history.php`, `add-record.php`, `shops.php`, `profile.php`, `reset-password.php`) — ชั้นนี้มี business logic อยู่จริง (ดูหัวข้อ "Logic ที่อยู่ที่ controller/view") และเป็นชั้นที่เคยมีบั๊กหลุดมาแล้ว ⚠️ `php -l` บอก "ไม่มี error" กับตัวแปรที่ไม่มีจริง — PHPStan เป็นตัวเดียวที่จับได้
- **ตัวแปรของมุมมอง "รายปี" ใน `overview.php` ประกาศไว้ก่อนเข้ากิ่ง** (`$yearlyBestMonth`, `$yearlyYoyText` ฯลฯ) — เดิมกำหนดในกิ่ง `$view === 'year'` แล้วใช้ในกิ่งเดียวกันตอนเรนเดอร์ ซึ่งถูกต้องตราบใดที่เงื่อนไขสองที่ตรงกันเป๊ะ · ประกาศไว้ก่อนทำให้แก้เงื่อนไขผิดแล้วได้ค่าว่างแทน warning กลางหน้า และทำให้ PHPStan วิเคราะห์ทั้งไฟล์ได้โดยไม่ต้อง ignore
- **หน้าเว็บห้ามเดาแทนข้อมูล — โหลดไม่สำเร็จ = ไม่แสดงตัวเลขเลย** `dashboard.php` / `annual.php` / `history.php` / `overview.php` ห่อทั้งเนื้อหาไว้ใน `if (error === null)` · เดิมค่าตั้งต้น ฿0 ถูกเรนเดอร์ต่อ ผู้ใช้จึงเห็น "ไม่มีสิทธิ์เข้าถึงร้านค้านี้" คู่กับ "ทั้งปีทำได้ ฿0 · เดือนกำไรดีสุด ม.ค." และคำเชิญ "ลองเริ่มบันทึกข้อมูล" ในหน้าเดียวกัน ⚠️ **แถวรวมใน `<tfoot>` ก็ต้องซ่อนด้วย** (พลาดมาแล้ว) · มีเทสต์ `PageRenderTest` ล็อกทั้ง 3 หน้า
- **ข้อความ/สูตรที่หน้าเว็บกับไฟล์ Excel ใช้ร่วมกัน ต้องอยู่ใน helper ตัวเดียว** — `projection_remaining_label()`, `projection_footnote_text()`, `annual_month_outcome_counts()` ใน `includes/functions.php` ⚠️ เดิมคัดลอกไว้ 2 ที่พร้อมคอมเมนต์ "แก้ต้องแก้คู่" แล้วก็เพี้ยนจริง (หน้าเว็บ "ไม่คิดฤดูกาล/การเปิดรอบ" ส่วน Excel "ไม่คิดฤดูกาล"; หน้าเว็บนับ "เท่าทุน" ส่วน Excel ไม่นับ) · `AnnualReportParityTest` ล็อกไว้
- ⚠️ **ทุกทางที่ "เขียน" ต้องจองแถวร้านก่อนด้วย `ShopRepository::lockForWrite()`** (`FOR UPDATE`) — ครบทั้ง 5 ทาง: `upsertRecord`, `upsertManyRecords`, `updateRecord`, `deleteRecord`, `GoalService::upsertGoal` · ทำ 2 อย่างพร้อมกัน: **(1) ลำดับล็อกตรงกับตอนลบร้าน** (ร้าน → ข้อมูลในร้าน) และ **(2) บังคับให้การเขียนของร้านเดียวกันเข้าคิว** ซึ่งเป็นสิ่งเดียวที่ทำให้ตัวกัน "ช่องว่างทับของเดิม" (อ่านแล้วค่อยเขียน) ใช้ได้จริง
  · ⚠️ **ห้ามเปลี่ยนเป็น `LOCK IN SHARE MODE`** — สองคนถือล็อกแบบอ่านพร้อมกันได้ ข้อ (2) จึงหายไปทั้งหมด (เคยเขียนผิดมาแล้ว: ตัวกันเห็นภาพก่อนอีกฝ่ายบันทึก แล้วทับด้วยศูนย์ พร้อมข้อความ "สำเร็จ" ทั้งสองฝั่ง)
  · เดิม `RecordService` จองแถว *วัน* ด้วย `FOR UPDATE` ก่อน upsert ซึ่งไม่ช่วยอะไร (`INSERT … ON DUPLICATE KEY UPDATE` กันชนกันเองอยู่แล้ว) และเมื่อวันนั้นยังไม่มีข้อมูล MySQL จองเป็น **gap lock** สองคนที่บันทึกคนละวันในช่องว่างเดียวกันจึง deadlock · `upsertManyRecords` ยัง `usort` ตามวันที่ก่อนเขียนเสมอ (ลำดับที่ผู้ใช้พิมพ์ทำให้สองแท็บจองไขว้กัน)
  · ⚠️ **เทสต์ concurrency ต้องให้ "ผู้ถือ" จองด้วย `lockForWrite()` เอง ไม่ใช่เขียน `FOR UPDATE` ตรง ๆ ในเทสต์** — ไม่งั้นเทสต์เขียวแม้ตอนที่โค้ดใช้ล็อกแบบอ่าน (เทสต์ไปบล็อกให้เอง = พิสูจน์ผิดตัว) · `ConcurrentWriteLockOrderTest` พิสูจน์ว่าได้ **1205 (รอ)** ไม่ใช่ **1213 (deadlock)** และแดงจริงเมื่อสลับกลับเป็น share lock
- **ตัวกัน "ช่องว่างทับของเดิม" ต้องอ่านข้างใน transaction หลังจองแถวร้านแล้ว** — `rejectBlankCellsOverwritingExistingDays()` อยู่หลัง `beginTransaction` + `lockForWrite` · เดิมอ่านก่อนเปิด transaction อีกแท็บที่บันทึกแทรกเข้ามาระหว่างนั้นจะถูกทับด้วยศูนย์พร้อมข้อความ "สำเร็จ" ⚠️ **ห้ามใส่ `SELECT` ธรรมดาไว้ระหว่าง `beginTransaction` กับตัวกันนี้** — REPEATABLE READ จะตรึงภาพไว้ตั้งแต่ query แรก ตัวกันจะกลับไปอ่านของเก่าอีกโดยไม่มีอะไรเตือน (`lockForWrite` เป็น locking read จึงไม่ตรึงภาพ)
- **คอลัมน์ "เทียบครั้งก่อน" ใน CSV ห้ามขึ้นต้นด้วย `+`** — Excel แปลงเซลล์ที่ขึ้นต้นด้วย `+` เป็น *สูตร* (มรดก Lotus 1-2-3) ช่องนี้จึงเป็นสูตรช่องเดียวในไฟล์และพังบนเครื่องที่ตั้งทศนิยมเป็นจุลภาค · เครื่องหมายลบไม่มีปัญหา (Excel อ่าน `-50.0%` เป็นจำนวนลบ) · `ExportServiceTest::testNoSystemGeneratedCellStartsAFormula` กวาดทุกเซลล์ที่ไม่ใช่โน้ต
- **405 ต้องมาคู่กับ header `Allow: POST` เสมอ** (`ensure_post_request_or_respond`) — RFC 9110 บังคับ ไม่งั้นฝั่งที่เรียกได้แต่ "ห้ามใช้วิธีนี้" โดยไม่รู้ว่าต้องใช้อะไรแทน
- **`config_positive_int()` ห่อด้วย `function_exists`** — `includes/config.php` ถูก include ได้จากหลายทาง เดิม include ซ้ำ = fatal "Cannot redeclare" ทั้งที่ค่าคงที่ข้างล่างกันตัวเองด้วย `if (!defined(...))` อยู่แล้ว ⚠️ และอ่าน env ด้วย `getenv() === false` ไม่ใช่ `?: ''` เพราะ `"0"` เป็น falsy — ค่าที่ตั้งผิดเป็น 0 เคยเงียบสนิทเหมือนไม่ได้ตั้ง
- **JS ที่คัดลอกกติกามาจาก PHP ต้องมีเทสต์เทียบเสมอ** — `BrowserScriptParityTest` **ดึงตัวฟังก์ชันออกมาจาก `add-record.php` ตรง ๆ** (หา `const <ชื่อ> = ` แล้วนับวงเล็บจนถึง `;` นอกวงเล็บ) ไปรันด้วย node แล้วเทียบผลกับ PHP ชุดค่าเดียวกับ `MoneyInputParsingTest` ⚠️ **ห้ามคัดลอกโค้ด JS มาไว้ในเทสต์** ไม่งั้นได้สำเนาที่ 3 ให้เพี้ยนอีก · เปลี่ยนวิธีประกาศฟังก์ชัน (เช่นไปเป็น `function foo()`) เทสต์จะแดง — ต้องมาอัปเดตตัวดึง ไม่ใช่ปิดเทสต์ · ไม่มี node = skip (CI ของ GitHub มี node ติดมาอยู่แล้ว)
- ⚠️ **การ์ด "เทียบวันเดียวกันในสัปดาห์" ต้องจบช่วงที่วันนี้ ไม่ใช่สิ้นเดือน** — ตาราง "กำไรเฉลี่ยตามวัน" ที่อยู่ใต้มันในหน้าเดียวกันใช้ `resolveWeekdayWindow()` ซึ่งจบที่วันนี้ · เดิมการ์ดไล่ `Y-m-t` สองบรรทัดบนจอเดียวกันจึงใช้คำว่า "เดือนนี้" เหมือนกันแต่คนละฐาน (วัดจริง: การ์ดบอกเฉลี่ยจันทร์ ฿9,000 + ป้าย "ต่ำกว่าปกติ" ขณะที่ตารางบอก ฿1,000 จาก 1 วัน — จันทร์ 3 วันนั้นยังมาไม่ถึง)
  · ⚠️ **เทสต์ของการ์ดนี้ต้องปัก `$today` เสมอ** — เดิม 3 ตัวไม่ปัก ผลจึงเปลี่ยนตามวันที่รันจริง (เทสต์ข้าง ๆ ในไฟล์เดียวกันมีคอมเมนต์เตือนไว้แล้วแต่ตัวอื่นไม่ได้ทำตาม)
- **เกณฑ์ตัวอย่างขั้นต่ำของวันในสัปดาห์ = `RecordService::WEEKDAY_MIN_SAMPLE`** — `comparable` (มีอะไรให้เทียบไหม) ยังเป็น `>= 1` แต่การฟันธง "สูงกว่า/ต่ำกว่าปกติ" ใช้ `trend_reliable` (`>= 3`) เกณฑ์เดียวกับที่ตารางแยกตามวันใช้เลือกวันดี/วันเงียบ · **อย่าฮาร์ดโค้ด 3 ซ้ำในเพจ**

> ตรรกะที่เคยอยู่ที่ controller และถูกย้ายลง service/helper แล้ว — อย่าย้ายกลับ:
> แปลงปี พ.ศ. → `resolve_calendar_year()` · cutoff เดือนอนาคต → `resolve_calendar_month()` ·
> typed-confirm ตอนลบร้าน → `ShopService::confirmationNameFor()` + `deleteShop()` ·
> ตรวจรูปแบบเดือนของเป้าหมาย → `GoalService` (controller ส่งค่าดิบ ห้ามใช้ `normalize_month_input` กับข้อมูล)

**`?month` ต้องผ่าน `resolve_calendar_month()` ทุกจุด ไม่มีข้อยกเว้น** — ปัจจุบันครบแล้วทั้ง 7 จุด
(`dashboard.php`, `overview.php`, `history.php`, `api/dashboard-data.php`, `api/overview-data.php`,
`api/month-grid.php`, `api/export.php`) และ picker ทุกตัวมี `max="<เดือนนี้>"`
⚠️ `dashboard.php`/`api/dashboard-data.php` ต้องแยก `month=` **ที่ว่าง** (= ไม่ได้เลือก → `null`)
ออกจากเดือนปัจจุบัน — สองไฟล์นี้ต้องเขียนเหมือนกันเป๊ะ ๆ ไม่งั้นหน้าเว็บกับ endpoint ตอบคนละอย่าง

---

## การเทสต์ (Testing)

> **สถานะปัจจุบัน: setup ครบแล้ว** — `phpunit/phpunit` อยู่ใน `require-dev`, มี `phpunit.xml` (testsuite `Unit`/`Integration`), มี `tests/bootstrap.php`
> `composer test` รันได้เลย ไม่ต้อง setup อะไรเพิ่ม

### ⚠️ ความครอบคลุมไม่เท่ากันทั้งระบบ — อย่าเชื่อตัวเลขรวม

จำนวนเทสต์รวมดูเยอะ แต่กระจุกอยู่ที่ **ฝั่งอ่าน/รายงาน** เกือบทั้งหมด ฝั่ง **เขียนข้อมูล** แทบไม่มี:

| พื้นที่ | สถานะ |
|---|---|
| Annual / Overview / Xlsx / Dashboard / Export (อ่าน) | ครอบแน่น |
| RecordService ฝั่งอ่าน (grid, weekday, days-since, unfilled) | ครอบแน่น |
| Auth (login/logout/reset password/session guard) | ครอบแล้ว (integration) |
| ShopService / GoalService / ProfileService / RecordService เขียน-ลบ | ครอบแล้ว |
| ชั้น controller (`api/*.php`) — ด่านตรวจ (auth/405/415/CSRF/409) | ครอบแล้ว (`EndpointGuardChainTest`, `RecordEndpointGuardTest`) |
| ชั้น controller — ตรรกะเฉพาะของแต่ละ action | ครอบแล้วทุกไฟล์: `records.php` ทุกคำสั่งรวมนำเข้า CSV (`RecordActionEndpointTest`) · `shops.php` (`ShopEndpointTest`) · `goals.php`/`profile.php` (`GoalAndProfileEndpointTest`) · `auth.php` รวม forgot/reset (`AuthEndpointTest`) · `*-data.php` (`DataEndpointParityTest`) |
| Session หมดอายุ / เตะเครื่องอื่นออก | ครอบแล้ว (`SessionLifetimeTest`) |
| ไฟล์ที่ดาวน์โหลดจริง (CSV กันสูตร Excel, BOM, xlsx) | ครอบแล้ว (`ExportEndpointTest`) |
| ชั้นเพจ — เปิดขึ้นจริง, กันคนไม่ล็อกอิน, ไม่โกหก ฿0, หดช่วงอนาคต | ครอบแล้ว (`PageRenderTest`) |
| JS ทุกหน้า — parse ผ่านไหม + เรียกฟังก์ชันที่ไม่มีนิยามไหม | ครอบแล้ว (`BrowserScriptParityTest` — 8 หน้า + header/footer) ⚠️ `php -l` บอก "ไม่มี error" แม้ JS ในหน้านั้นพังทั้งบล็อก |
| JS ที่ต้องตรงกับ PHP (อ่านจำนวนเงิน / วันที่กำกวม / แยกหัวตาราง) | ครอบแล้ว (ดึงฟังก์ชันจาก `add-record.php` ไปรันด้วย node แล้วเทียบกับกติกา PHP ตัวจริง) |
| **JS ส่วนที่ต้องมี DOM** (วางจาก Excel ทั้งกระบวนการ, เติมค่าเมื่อเปลี่ยนวัน, month-grid, typed-confirm) | **ยังต้อง verify มือ** — ไม่มี DOM ในเทสต์ |
| **EmailService การส่งจริง** | กฎ "ตั้งค่าครบไหม" ครอบด้วยพฤติกรรมแล้ว (constructor รับค่าแทนที่ได้) — **การส่งถึงกล่องจดหมายจริงยังต้อง verify มือ** |

⚠️ **รายการที่ต้องลองด้วยมือ อยู่ที่ [docs/manual-checks.md](docs/manual-checks.md)** — การวางจาก Excel (ต้องมีคลิปบอร์ดจริง) และอีเมลรีเซ็ตรหัสผ่าน (ต้องมีกล่องจดหมายจริง) · ทั้งสองเรื่องระบบทดสอบแทนไม่ได้ และการวางเคยพังแบบเงียบสนิทมาแล้วโดยที่เทสต์เขียวหมด

**JS ส่วนที่เหลือยังไม่มีตาข่าย** — logic ที่อยู่ตรงนั้นให้ย้ายลง service/helper ก่อนแก้
(มีตัวอย่างในหัวข้อด้านบน) หรือถ้าย้ายไม่ได้ ให้ verify ด้วยเบราว์เซอร์จริง

- ⚠️ **เทสต์ที่เขียวไม่ได้แปลว่าโค้ดถูก — ต้องลองทำให้โค้ดพังแล้วดูว่าแดงไหมทุกครั้ง** กับดักที่เจอมาแล้วจริงในโปรเจกต์นี้:
  · **เทสต์ concurrency ไปจองล็อกเองด้วยวิธีที่เข้มกว่าโค้ด** → เขียวแม้โค้ดใช้ล็อกที่กันอะไรไม่ได้ (ต้องจองผ่านเมธอดเดียวกับที่โค้ดใช้)
  · **`assertNotSame(200, …)`** → 404 "ไม่รู้จักคำสั่ง" ก็ผ่าน ทั้งที่ยังไม่ถึงด่านที่จะตรวจ (ต้องส่ง payload ที่ใช้ได้จริงแล้วยืนยันรหัสสถานะเป๊ะ ๆ)
  · **`assertContains($status, [302,401,403])`** → ถอด `requireAuth()` ออกก็ยังผ่าน เพราะด่าน CSRF ตอบรหัสเดียวกัน (ต้องดู `Location` ว่าไป `login.php` ไหม)
  · **`value="…" selected`** กับ `<input type="month">` → คำว่า selected ไม่มีวันปรากฏ เทสต์จึงไม่ได้ตรวจอะไร (ต้องยืนยันทั้ง "ค่าอนาคตไม่มี" และ "ค่าที่หดแล้วมี")
  · **หา `"Warning:"` ในหน้า HTML** → PHP ห่อเป็น `<b>Warning</b>:` ต้อง `strip_tags()` ก่อน · และเซิร์ฟเวอร์ทดสอบต้องรันด้วย `APP_ENV=development` ไม่งั้น `display_errors` ปิดอยู่ ไม่มีอะไรให้ตรวจตั้งแต่แรก
  · **`markTestSkipped` ตอนยกเซิร์ฟเวอร์ไม่ขึ้น** → เทสต์ทั้งชั้นหายไปโดย CI ยังเขียว (ต้อง `fail()` เมื่อ DB พร้อมแล้วแต่เซิร์ฟเวอร์ไม่ขึ้น)
  · **สถานะ 0 จากคำขอที่ไปไม่ถึงเซิร์ฟเวอร์** → ผ่านทุก assert ที่เขียนว่า "ไม่ใช่ 200"

- ⚠️ **เทสต์ต้องเรียก "กติกาตัวจริง" ไม่ใช่เขียนกติกาซ้ำไว้ในเทสต์** — `BrowserScriptParityTest` ดึงฟังก์ชัน JS จาก `add-record.php` และเรียก `RecordService::parseImportDate()` + `isAmbiguousSlashDate()` ผ่าน Reflection (สองตัวรวมกัน เพราะ `parseImportCsv()` ก็ทำแบบนั้น) · เคยเขียนกติกาซ้ำไว้ในเทสต์แล้วกลายเป็นสำเนาที่ 3 ให้เพี้ยน และเคยใช้ตัวเดียวจนเทสต์ **ผ่านแม้ทำให้ JS คืน null ทุกกรณี**
- ⚠️ **เทสต์อัปโหลดไฟล์ต้องรันเซิร์ฟเวอร์ด้วย `upload_max_filesize` ที่สูงกว่าเพดานของแอป** — เพดานของแอปคือ 2MB (`api/records.php`) ถ้า php.ini ตั้งไว้ 2M พอดี PHP จะปฏิเสธก่อน ด่านของแอปไม่มีวันทำงาน แล้วเทสต์ที่ตั้งชื่อว่าตรวจด่านนั้นจะผ่านด้วยข้อความคนละอัน (เซิร์ฟเวอร์จริงตั้งไว้สูงกว่า ด่านของแอปจึงเป็นตัวจริงบน production)
- ⚠️ **ด่านตรวจสิทธิ์ของ `ExportService` (CSV) เทสต์ที่ endpoint ไม่ได้** — `api/export.php` เรียก `resolve_current_shop_id()` ซึ่งซ่อม session กลับไปร้านของเจ้าตัวก่อนเสมอ คำขอจึงไม่มีวันเดินมาถึง · ต้องเทสต์ที่ชั้น Service (stub `findByIdAndUserId` → null) เหมือน `ExportServiceXlsxTest`
- ⚠️ **`startSession()` ในเทสต์อ่านอีเมลจริงของผู้ใช้จาก DB** — ไม่ใช่ค่าคงที่ เพราะบางหน้าเทียบอีเมลใน session กับข้อมูลอื่น (หน้าตั้งรหัสใหม่เตือนเมื่อลิงก์เป็นของคนละบัญชี) ค่าคงที่ทำให้เทสต์เรื่องนั้นเขียนไม่ได้เลย

- ⚠️ **เทสต์ที่เทียบ "สองทางต้องตอบเหมือนกัน" ต้องเลือกค่าที่แยกกันได้จริง** — กับดักที่เจอมาแล้ว:
  · เทียบ `?month=` (ไม่มี range) กับ `?range=month_pick&month=…` แล้วดู `range.type` → ค่านั้นสะท้อน `range` ตรง ๆ อยู่แล้ว เป็นจริงโดยโครงสร้าง
  · ใช้ปี พ.ศ. **ของปีปัจจุบัน** ทดสอบการแปลงปี → ถอดการแปลงออกแล้วค่าตกนอกช่วง 2000–2100 กลับไปใช้ปีปัจจุบันพอดี ได้คำตอบเดียวกันโดยบังเอิญ (ต้องใช้ปีในอดีต เช่น 2565 → 2022)
  · **ที่พิสูจน์แล้วว่าแยกไม่ได้จริง ๆ:** "`month=` ว่าง" กับ "`month=` เดือนปัจจุบัน" ให้ผลเหมือนกันทุกไบต์ทั้งบนหน้าเว็บและใน endpoint — สิ่งที่ตรวจได้คือ "สองไฟล์ตอบตรงกันไหม" (`testThePageAndTheEndpointNeverDisagree` ไล่ query 9 แบบ)
- ⚠️ **`startSession()` ของเทสต์ต้องมีคีย์ชุดเดียวกับ `AuthService::establishSession()`** — เทสต์ชั้น controller ทุกตัวใช้ session ที่สร้างเอง ถ้าคีย์ไม่ตรง ทั้งชั้นจะรันอยู่บน session ที่ไม่มีวันเกิดขึ้นจริง · `AuthEndpointTest::testTheTestSessionHasTheSameShapeAsARealLogin()` ล็อกไว้ (ล็อกอินจริงแล้วเทียบคีย์) ⚠️ ต้องหาไฟล์ session ที่ "เพิ่งเกิดใหม่" เพราะล็อกอินสำเร็จเรียก `session_regenerate_id` — ค้นด้วย user_id จะไปเจอไฟล์ของเทสต์ก่อนหน้า

- ⚠️ **ตัวปักคีย์ session ต้องล็อกอินจาก session ที่ยังไม่มีคีย์ auth เลย** (`startBlankSession()`) — `session_regenerate_id(true)` ยก `$_SESSION` เดิมข้ามไปไฟล์ใหม่ทั้งก้อน ถ้าเริ่มจาก session ที่เทสต์เขียนคีย์ครบไว้แล้ว ไฟล์หลังล็อกอินจะมีคีย์ครบเสมอ **ไม่ว่าแอปจะเขียนอะไรจริง ๆ** (เทสต์เคยเขียวแม้ตัด `establishSession()` เหลือคีย์เดียว) · และต้องยืนยัน "รายชื่อคีย์ที่คาดหวัง" แบบตายตัวด้วย กันกรณีสองฝั่งขาดเหมือนกัน
- ⚠️ **ตัวตรวจ JS ต้องดึงหน้าจากเซิร์ฟเวอร์จริง ไม่ใช่อ่านไฟล์ดิบ** — โค้ดที่ PHP แทรก (`<?= json_encode(...) ?>`) เป็นส่วนหนึ่งของ JS ที่เบราว์เซอร์ได้รับ · เวอร์ชันแรกอ่านไฟล์แล้วแทน `<?= … ?>` ด้วย `null` จึงไม่มีวันจับได้ว่า PHP ปล่อย JS ผิดไวยากรณ์ออกมา ทั้งที่ docblock อ้างว่าครอบ
- ⚠️ **ตัดคอมเมนต์ก่อนตัดสตริงเสมอ** ตอนสแกนโค้ด JS — คอมเมนต์ที่มี `'` อยู่ข้างในจะจับคู่กับเครื่องหมายคำพูดตัวถัดไปในโค้ดจริง แล้วลบทุกอย่างระหว่างกลางทิ้ง โค้ดช่วงนั้นหายจากการตรวจโดยไม่มีอะไรบอก
- ⚠️ **ไฟล์ที่ฝัง `<script>` ทุกไฟล์ต้องอยู่ใน `scriptedPageProvider()`** — `testEveryFileWithInlineScriptIsChecked()` กวาดเทียบให้ (แบบเดียวกับ `testEveryApiFileIsAccountedFor` ของฝั่ง `api/`) เดิมรายชื่อเป็นค่าตายตัว เพิ่มสคริปต์ลงหน้าที่ไม่อยู่ในรายชื่อแล้วมันพังยังไงก็ไม่มีใครรู้
- ⚠️ **ห้ามทิ้งค่าที่ `GET_LOCK` คืนมา** — คืน `0` เมื่อรอจนหมดเวลา และ `NULL` เมื่อผิดพลาด ทิ้งไว้เฉย ๆ = ชุดเทสต์เดินต่อโดยไม่มีล็อก ซึ่งคือปัญหาเดิมที่ตั้งใจแก้พอดี · ตอนนี้ skip พร้อมบอกเหตุผล

- ⚠️ **ค่าตัวเลขจาก env ต้องผ่าน `config_positive_int()` ทุกตัว ไม่มีข้อยกเว้น** — เคยมี 5 ตัวที่ข้ามไปใช้ `(int)(getenv() ?: default)` ซึ่ง `(int)"eight"` = 0 และ `(int)"-5"` = -5 ผ่านเข้าไปเงียบ ๆ · ผลจริงที่วัดได้: พิมพ์ `PASSWORD_MIN_LENGTH=eight` ผิดตัวเดียว **ความยาวรหัสผ่านขั้นต่ำเหลือ 4** สำหรับทั้งการสมัคร รีเซ็ต และเปลี่ยนรหัส โดยไม่มี log อะไรเลย · `ConfigEnvironmentTest` ล็อกไว้แล้ว
- ⚠️ **`SCHEMA_GUARD_ENABLED` ห้ามอ่านด้วย `getenv() ?: 'true'`** — `"0"` เป็น falsy จึงกลายเป็น `'true'` (เปิดการ์ด) ขณะที่ค่าขยะอย่าง `"disabled"` กลายเป็น false (ปิดการ์ดเงียบ ๆ) = **กลับหัวกับที่ผู้ตั้งค่าคาดหวังทั้งสองทาง** · ตอนนี้ค่าขยะ = เปิดไว้ + เขียน log
- ⚠️ **ข้อความ TTL ในอีเมลรีเซ็ตต้องผ่าน `max(1, …)` เหมือนที่ repository ทำ** — ไม่งั้นอีเมลบอกอายุลิงก์คนละค่ากับที่ระบบใช้จริง
- ⚠️ **เทสต์ล็อกอินต้อง "ใช้งานต่อ" ด้วย session ที่ได้มา ไม่ใช่ดูแค่ปลายทางของ redirect** — เคยพิสูจน์แล้วว่าเปลี่ยน `auth_started_at` เป็น `0` ทำให้ผู้ใช้หลุดทันทีในคำขอถัดไป (นับว่าหมดอายุ) **โดยที่เทสต์ทั้ง 933 ตัวยังเขียว** เพราะไม่มีใครแตะ session ที่การล็อกอินสร้าง · `ControllerTestCase::sessionIdFrom()` ดึง id ใหม่จาก `Set-Cookie` มาให้
- ⚠️ **ตัดคอมเมนต์/สตริงออกจาก JS ต้องเดินทีละตัวอักษร ห้ามใช้ regex สองชั้น** — ตัดสตริงก่อน คอมเมนต์ที่มี `'` จะกลืนโค้ด · ตัดคอมเมนต์ก่อน สตริงที่มี `//` จะกลืนโค้ด **ทั้งสองลำดับพังคนละทาง** (พิสูจน์ทั้งคู่แล้ว) → `stripCommentsAndStrings()`
- ⚠️ **หน้าที่ฝัง `<script>` ต้องอยู่ใน *ทั้งสอง* รายชื่อ** — `scriptedPageProvider` (เรียกฟังก์ชันที่ไม่มีนิยาม) และ `renderedPageProvider` (parse ผ่านไหม) · ลงทะเบียนแค่รายชื่อเดียวแล้วอีกด้านยังบอด · `testEveryFileWithInlineScriptIsChecked()` เช็กทั้งคู่

### เทสต์ชั้นหน้าเว็บ (controller + page) — `ControllerTestCase`

`api/*.php` เรียก `includes/bootstrap.php` (session_start + security header + `db()` + schema
guard) จึง `require` เข้ามาเรียกใน process ของ PHPUnit ไม่ได้ · `tests/Integration/ControllerTestCase.php`
จึง **ยก `php -S` ขึ้นมา 1 ตัวต่อคลาสเทสต์** ชี้ `DB_*` ไปที่ test DB เดิม แล้วยิง HTTP จริง

- `startSession($userId, $shopId)` — เขียนไฟล์ session ตรง ๆ (ไม่ต้องรู้รหัสผ่าน, เลี่ยง rate limit) ⚠️ **คีย์ต้องตรงกับ `AuthService::establishSession()` เป๊ะ ๆ** (`email`, `auth_started_at`, `last_activity_at`, `current_shop_id`, `current_shop_name`, `session_version`) — เดิมเขียนคีย์ที่แอปไม่ได้อ่าน แล้ว `isAuthSessionAlive()` เติมให้เองเงียบ ๆ ทำให้ด่านหมดเวลาเทสต์ไม่ได้เลย
- `get/post` = โหมดฟอร์ม (ตอบ **302 + flash**) · `getJson/postJson` = โหมด XHR (ตอบ **รหัสสถานะจริง**)
  ⚠️ **ทั้งสองโหมดต้องตัดสินเหมือนกัน ต่างแค่วิธีบอกผู้ใช้** — เทสต์ 409/405 ต้องยิงทั้งคู่
  (`api_respond()` เลือกจาก `Accept: application/json`)
- `csrfTokenFor($sessionId)` — ดึง token จากหน้าจริง · `flashMessages()` — อ่านไฟล์ session ดิบ (fail เมื่อไฟล์หาย เพราะ `session_regenerate_id(true)` ลบไฟล์ทิ้ง)
- ⚠️ ชื่อ field คือ **`shop_context_id`** (จาก `shop_context_field()`) ไม่ใช่ `shop_context`
- ⚠️ **เพิ่ม endpoint ใหม่ใน `api/` ต้องเพิ่มชื่อใน `EndpointGuardChainTest`** — มี `testEveryApiFileIsAccountedFor()` กวาด `glob('api/*.php')` เทียบกับรายชื่อในเทสต์ ลืมแล้วชุดเทสต์แดงทันที
- ⚠️ **payload ในรายชื่อต้องเป็นคำสั่งที่ไฟล์นั้นรู้จักจริง** — `api/shops.php`/`api/profile.php` วางด่าน CSRF ไว้ *ข้างใน* แต่ละ `if ($action === …)` ส่งคำสั่งมั่ว ๆ จะตกไป 404 ตั้งแต่ยังไม่ถึงด่าน
- **เทสต์ทั้งชั้นนี้แชร์ test DB เดียวกัน** — `IntegrationTestCase` จับ `GET_LOCK('ad_profit_test_suite')` ของ MySQL ตอน `setUpBeforeClass` สองโปรเซสที่รันพร้อมกันจึง **เข้าคิว** แทนที่จะล้างข้อมูลของกันและกันกลางคัน (เดิมเจอ `Duplicate entry` / `Table … doesn't exist` ที่ไม่เกี่ยวกับโค้ดเลย)
  · ⚠️ **ห้ามเปลี่ยนกลับไปใช้ `flock` บนไฟล์** — `ControllerTestCase` ยก `php -S` ด้วย `proc_open` ลูกจะ **สืบทอด file descriptor ของล็อก** ถ้า phpunit ถูกฆ่ากลางคัน เซิร์ฟเวอร์ที่ค้างอยู่จะถือล็อกไว้ตลอดไป การรันครั้งต่อ ๆ ไปค้างรอเงียบ ๆ โดยไม่มีอะไรบอกว่าทำไม (เกิดขึ้นจริงมาแล้ว — ใช้ `lsof <lock>` หาตัวที่ถืออยู่) · ล็อกของ MySQL ผูกกับ **การเชื่อมต่อ** จึงคืนเองเมื่อโปรเซสตาย
- ⚠️ **เทสต์ที่ส่ง payload ใหญ่ ๆ ทำให้ชุดเทสต์ทั้งชุดล้มด้วยหน่วยความจำ** — รันไฟล์เดียวผ่าน แต่รันรวมตาย (`Premature end of PHP process`) · ถ้าจะทดสอบด่านที่เกี่ยวกับขนาด ให้จำลอง **สภาพที่ด่านนั้นตรวจจริง** แทนการส่งของใหญ่จริง (เช่นด่าน 413 ตรวจว่า "body มีความยาวแต่ PHP แกะไม่ได้" → ส่ง multipart ที่ boundary ไม่ตรงกับเนื้อ)

### Framework
- **PHPUnit** เป็น dev dependency, รันด้วย `composer test`
- ⚠️ **ชุดเทสต์ใช้เวลา ~4 นาที ซึ่งเกินเพดานปริยาย 300 วินาทีของ composer** — `composer.json` จึงตั้ง `config.process-timeout` ไว้ · ถ้าไม่ตั้ง `composer test` จะถูกฆ่ากลางคันพร้อมข้อความ "exceeded the timeout" ซึ่งอ่านแล้วนึกว่าเทสต์ค้าง ทั้งที่มันแค่ยังไม่เสร็จ
- ⚠️ **ต้องใช้ PHP 8.4+ ในการรันเทสต์** (PHPUnit 13 require `php >= 8.4.1`) — เครื่องที่เป็น 8.3 จะ `composer install` dev dependency ไม่ผ่าน; ความเข้ากันได้กับ 8.3 คุมด้วย `php -l` ใน CI job `lint-php83` เท่านั้น (syntax ไม่ใช่ runtime)
- `phpunit.xml` เปิด `failOnWarning="true"` + `failOnNotice="true"` → warning/notice = เทสต์แดง (CI บังคับ)

### โครงสร้าง
```
tests/
  bootstrap.php        ← test bootstrap (ดูกฎด้านล่าง)
  Unit/                ← mock repository, ไม่แตะ DB
  Integration/         ← MySQL test DB จริง
phpunit.xml            ← แยก testsuite: Unit / Integration
```

### กฎเหล็กของ test bootstrap
- `tests/bootstrap.php` ต้อง **define constant ที่จำเป็น + include `includes/functions.php` และ `includes/auth.php`** เท่านั้น
- **ห้าม include `includes/bootstrap.php` เต็ม** ใน unit test (มันสั่ง `session_start`, security headers, `db()`, schema guard — มี side effect)
- constant กำหนดใน bootstrap ก่อน include config ได้ เพราะ `config.php` ใช้ `if (!defined(...))`

### Unit tests (เริ่มที่นี่ก่อนเสมอ — เร็ว ไม่ต้อง DB)
**ก่อนเขียน: ตรวจ constructor ของ Service ก่อน — รับ PDO ไม่เหมือนกัน**
- รับ `?PDO $db = null` (ส่ง `null` ข้าม transaction ได้): **RecordService, ShopService, GoalService, ProfileService** เท่านั้น
- **AuthService** รับ `PDO $db` แบบ **required** (ส่ง null ไม่ได้) → เทสต์เป็น integration หรือ mock PDO
- **DashboardService, AnnualService, OverviewService, OverviewDailyService, OverviewAnnualService** → **ไม่มี param PDO เลย** (รับแต่ repository) → mock repo ล้วน ไม่ต้องยุ่ง db
- **ExportService** → ไม่มี param PDO เช่นกัน แต่ `__construct(RecordService, ShopRepository)` — รับ **1 Service + 1 Repository** → เวลา unit test ต้อง **mock `RecordService` ด้วย** ไม่ใช่แค่ repository

**วิธี double repository (PHPUnit 13 — แยก 2 กรณีให้ชัด):**
- **pure stub** — แค่ต้องการค่า return (เช่น `userCanAccessShop` คืน `true`, `findByIdAndUserId` คืน row) ไม่ verify การเรียก → ใช้ `$this->createStub(XxxRepository::class)` **← default ของโปรเจกต์นี้** เพราะเคสส่วนใหญ่ fail ที่ validation ก่อนแตะ repo
- **verify interaction** — ต้องเช็กว่า method ถูกเรียก/เรียกกี่ครั้ง/ด้วยอาร์กิวเมนต์อะไร (`->expects($this->once())` ฯลฯ) → ใช้ `$this->createMock(XxxRepository::class)`
- เหตุผล: PHPUnit 13 ขึ้น notice ("No expectations were configured for the mock object... Consider using a test stub instead") ถ้าใช้ `createMock` แบบไม่ตั้ง expectation

assert แบบ **result-array** (`assertFalse($result['success'])`, `assertStringContainsString('<คำไทย>', $result['error'])`)

เป้าหมายหลัก (validation + business rule):
- `RecordService`: revenue/ad_cost ติดลบ → fail, note > 255 → fail
- `GoalService`: ไม่กรอกเป้าเลย → fail
- `ShopService`: สร้างเกิน 20 ร้าน → fail, ลบร้านสุดท้าย → fail
- `ProfileService`: display_name > 120 → fail, change email/password (verify รหัสเดิม, รหัสใหม่ต้องต่างเดิม)
- `ExportService`: รูปแบบ CSV — วันที่เป็น **ISO `YYYY-MM-DD`**, ค่าที่ไม่มี (ROAS/เทียบเมื่อวาน) เป็น **เซลล์ว่าง** ไม่ใช่ `'–'`, payload มี `note_column_index` + `blank_row_before_totals` ให้ controller ใช้ + `buildMonthlyCsvFilename` sanitize ชื่อไฟล์ (⚠️ **การกัน formula injection (เติม `'` หน้า cell ที่ขึ้นต้น `= + - @ \t \r`) อยู่ที่ controller `api/export.php` (closure `$sanitizeCsvCell`) และทำ *เฉพาะคอลัมน์โน้ต* ไม่ใช่ทุกเซลล์** — `buildMonthlyCsvPayload()` คืน payload ดิบ (`note` ส่งผ่านตรง ๆ) → อย่าเขียน unit test formula-injection ที่ Service)
- `AnnualService`: `isValidYear` รับ **2000–2100** เท่านั้น (⚠️ **การแปลงปี พ.ศ. −543 อยู่ที่ controller** เช่น `annual-data.php`/`annual.php`/`overview.php` **ไม่ใช่ใน Service** — อย่าเขียน unit test แปลงปีที่ Service)
- `OverviewService`/`OverviewDailyService`/`OverviewAnnualService`: `can_view` = จำนวนร้าน ≥ 2
- `DashboardService`: สูตร profit / ROAS (ad_cost=0 → null) / margin (DashboardService ไม่ยุ่งกับปี)

### Integration tests (เมื่อจำเป็นต้องแตะ DB)
- **ต้องใช้ MySQL test DB จริง** — SQL เป็น MySQL-specific (`ON DUPLICATE KEY`, `FOR UPDATE`, `DATE_FORMAT`, `information_schema`) SQLite in-memory ใช้ไม่ได้
- seed schema จาก `database/schema.sql` เข้า test DB ก่อนรัน — **loader ตัด `CREATE DATABASE`/`USE ad_profit` ทิ้ง** (schema hardcode ชื่อ DB จริง ดู gotchas) ให้ DDL ลงเฉพาะ DB ที่ต่ออยู่
- **ใช้ test DB แยกผ่าน env `TEST_DB_*`** — base class ต่อ PDO เอง (ไม่ผ่าน `db()`) แล้วฉีดเข้า repo/service; **SAFETY: ชื่อต้องลงท้าย `_test` ไม่งั้น throw**; แต่ละ test isolate ด้วย `TRUNCATE` (ไม่ใช่ tx rollback เพราะ service เปิด transaction เอง)
- **เตรียม test DB (ทำครั้งเดียว):** `CREATE DATABASE ad_profit_test;` แล้ว set env เท่าที่ต่างจาก default — `TEST_DB_HOST` (default `127.0.0.1`), `TEST_DB_PORT` (`3306`), `TEST_DB_NAME` (`ad_profit_test`), `TEST_DB_USER` (`root`), `TEST_DB_PASS` (`''`); ต่อไม่ได้ → integration **skip** (ไม่ error)
- เป้าหมาย: `RecordRepository` upsert unique(shop,date), FK cascade ตอนลบร้าน/ลบ user, `UserRepository` increment session_version, `AuthService` register (สร้าง user + default shop)

### ข้อควรระวัง
- `AuthService` เขียน `$_SESSION` + เรียก `session_regenerate_id(true)` (ต้องมี active session) → ส่วน `establishSession` เทสต์ยากใน CLI ให้เทสต์เฉพาะ branch validation/rate-limit หรือทำเป็น integration ที่ start session — **ทำแล้วใน `tests/Integration/AuthServiceTest.php`:** `setUp()` เปิด session เอง (`ini_set` save_path ถ้าว่าง + `session_start(['use_cookies'=>'0','cache_limiter'=>''])` กัน "headers already sent" ใน CLI) โดยไม่แตะ AuthService/base class
- ตั้ง `$_SESSION = []` ใน `setUp()` เมื่อเทสต์โค้ดที่อ่าน session
- error message เป็นภาษาไทย → assert ด้วย substring ที่เป็นคำไทย (เช่น `'ติดลบ'`, `'ไม่มีสิทธิ์'`)

### ⭐ วิธีหาบั๊กรายงานที่ได้ผลที่สุด: "ร้านที่นิ่งสนิท เดินทีละวันทั้งปี"

สร้างร้านที่ทำกำไร **วันละเท่ากันเป๊ะ** ทุกวัน กรอกครบทุกวัน (ทั้งปีนี้และปีก่อน) แล้ววนเรียก
service ด้วย `$today` ไล่ทีละวันตั้งแต่ 1 ม.ค. ถึง 31 ธ.ค. · **ตัวเลขไหนขยับ = บั๊ก**
เพราะธุรกิจไม่มีอะไรเปลี่ยนเลยแม้แต่วันเดียว

วิธีนี้จับได้จริงในรอบเดียว:
- ประมาณการสิ้นปีกระโดด **+฿7,527 ข้ามคืน** (16 พ.ค.) และ **+฿4,790** (16 มี.ค.)
- ก่อนหน้านั้นจับ "เทียบปีก่อน ↓9.9%" ที่ดีขึ้นเองทุกวัน · การ์ดเดือนแย่สุดชี้เดือนปัจจุบันตลอดต้นเดือน
- กราฟ 6 เดือนเปลี่ยนค่าเมื่อกดสลับช่วงเวลาเฉย ๆ

⚠️ **ต้องให้ stub ของ repository จำลองการตัดวันจริง** (`$notAfterDate`) ไม่งั้นได้ผลเหมือนกัน
ทั้งก่อนและหลังแก้ · และ **ข้อมูลตั้งต้นต้องเขียนเป็น "อัตราต่อวัน × จำนวนวันของเดือนนั้น"**
ไม่ใช่ยอดเดือนลอย ๆ ไม่งั้นเคสเดือนสั้น (ก.พ.) จะไม่เคยถูกเดินผ่าน

ตัวแปรอื่นที่ใช้ได้ผลเหมือนกัน: **"สองที่ต้องพูดตรงกัน"** — หน้าจอ vs ไฟล์ Excel vs CSV,
แดชบอร์ด vs หน้ารายปี, กราฟ vs ตารางที่อยู่เหนือมัน · บั๊กรายงานเกือบทั้งหมดในโปรเจกต์นี้
เป็นรูปแบบ "กติกาถูกบังคับใช้ที่หนึ่งแต่ไปไม่ถึงอีกที่หนึ่ง"

### workflow
- เขียน/แก้โค้ดเสร็จ → รัน `composer test` ให้ผ่านทั้งหมดก่อนจบงาน
- เพิ่มฟีเจอร์ใหม่ใน Service → เพิ่ม unit test ครอบ business rule นั้นด้วยเสมอ
- แก้ bug → เขียน test ที่ reproduce bug ก่อน แล้วค่อยแก้ให้ผ่าน

---

## คำสั่งที่ใช้บ่อย

```bash
composer install            # ติดตั้ง dependency
composer test                          # รันเทสต์ทั้งหมด
vendor/bin/phpunit --testsuite Unit    # รันเฉพาะ unit (เร็ว ไม่ต้อง DB)
```

## อย่าทำ

- อย่าใส่ business logic ใน Repository หรือ Page/API
- อย่า throw exception สำหรับ validation error (ใช้ result-array)
- อย่าเขียน SQL ด้วยการ concat ค่าจาก user
- อย่าสร้าง helper/guard ใหม่ถ้ามีใน `includes/` อยู่แล้ว
- อย่าให้ integration test แตะ database จริง (ใช้ test DB เท่านั้น)
