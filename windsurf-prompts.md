# 🛠️ Prompt สำหรับสั่ง Windsurf — ก็อปไปวางได้เลย

> **เตรียมก่อนเริ่ม:** วางไฟล์ 2 ไฟล์ไว้ใน root ของ project  
> 1. `project-brief.md` → ฟีเจอร์ / หน้าจอ / สูตรคำนวณ  
> 2. `architecture.md` → สถาปัตยกรรม / มาตรฐานโค้ด / โครงสร้างโฟลเดอร์  
>  
> **ทุก Phase ต้องยึดทั้ง 2 ไฟล์** — ถ้าโค้ดไม่ตรง ให้อ้างอิงกลับ

---

## Phase 1 — ฐานข้อมูล + ล็อกอิน

```
อ่านไฟล์ 2 ไฟล์นี้ให้เข้าใจทั้งหมดก่อนเริ่มทำงาน:
1. project-brief.md → ความต้องการระบบทั้งหมด (ฟีเจอร์/หน้าจอ/สูตร)
2. architecture.md → สถาปัตยกรรม/มาตรฐานโค้ด/โครงสร้างโฟลเดอร์

จากนั้นเริ่มทำ Phase 1:
1. สร้างโครงสร้างโฟลเดอร์ตาม architecture.md ส่วนที่ 3
2. สร้าง bootstrap.php, config.php, database.php, functions.php ตาม architecture.md ส่วนที่ 5
3. ออกแบบฐานข้อมูลให้รองรับทุกฟีเจอร์ใน project-brief.md → สร้าง database/schema.sql + sample_data.sql
4. สร้างระบบ Auth (login/register/logout) ตาม pattern Controller → Service → Repository
5. สมัครเสร็จให้สร้างร้านค้าแรกอัตโนมัติ
6. สร้างเอกสาร 5 ไฟล์ใน /docs/ ตาม architecture.md ส่วนที่ 8

ต้องทำตามมาตรฐานใน architecture.md ทุกข้อ (security, coding style, error handling)
```

**✅ ทดสอบ:** สมัคร → ล็อกอิน → ล็อกเอาท์ → ล็อกอินใหม่ ใช้ได้ก่อนไป Phase 2

---

## Phase 2 — บันทึก + ประวัติ

```
ทำ Phase 2 ตาม project-brief.md ส่วน 2.3 และ 2.5:
1. หน้าบันทึกข้อมูลรายวัน (4 ช่อง: วันที่, รายได้, ค่าแอด, โน้ต)
2. กรอกวันซ้ำให้อัปเดตทับ ไม่ใช่ error
3. บันทึกสำเร็จแสดง toast + ล้างฟอร์ม + แสดงรายการล่าสุด 7 วัน
4. หน้าประวัติ — ตารางรายการของเดือนที่เลือก (ทุก column ตาม brief)
5. แก้ไข (modal) + ลบ (confirm) ได้
6. แถวรวมท้ายตาราง

ต้องใช้ pattern Controller → Service → Repository ตาม architecture.md
SQL ต้องอยู่ใน Repository เท่านั้น
```

**✅ ทดสอบ:** กรอกข้อมูล 3-4 วัน → ดูประวัติ → แก้ไข → ลบ → กรอกวันซ้ำต้องอัปเดตทับ

---

## Phase 3 — แดชบอร์ด

```
ทำ Phase 3 ตาม project-brief.md ส่วน 2.4:
1. ตัวเลือกช่วงเวลา: สัปดาห์นี้ / สัปดาห์ก่อน / เดือนนี้ / เดือนก่อน / กำหนดเอง (เลือกวันเริ่ม-สิ้นสุด)
2. การ์ดสรุป 4 ใบ (ยอดขาย, ค่าแอด, กำไร, ROAS) — คำนวณตามช่วงที่เลือก
3. ใต้การ์ดแสดง % เปรียบเทียบเดือนก่อน (แสดงเฉพาะเมื่อเลือกรายเดือน)
4. สถิติเพิ่มเติม (เฉลี่ยต่อวัน, อัตรากำไร, วันขายดีสุด/แย่สุด)
5. กราฟแท่งรายวัน (รายได้ vs ค่าแอด) — เปลี่ยนตามช่วงที่เลือก
6. กราฟเส้น 6 เดือนย้อนหลัง (ยอดขาย, ค่าแอด, กำไร) — แสดงรายเดือนเสมอ
7. เลือกช่วงเวลา → ข้อมูลทั้งหน้าอัปเดตอัตโนมัติ

ดูสูตรคำนวณ + กรณี "แสดง –" ในส่วนที่ 4 ของ brief ด้วย
```

**✅ ทดสอบ:** เลือกสัปดาห์นี้/เดือนนี้/กำหนดเอง → ข้อมูลเปลี่ยนถูกต้อง, ค่าแอด=0 ไม่พัง

---

## Phase 4 — ระบบร้านค้า

```
ทำ Phase 4 ตาม project-brief.md ส่วน 2.2:
1. สร้างร้านใหม่ได้ (กดปุ่ม + ร้านใหม่ → modal กรอกชื่อ)
2. Dropdown ที่ header สลับร้าน → ข้อมูลทุกหน้าเปลี่ยนตามร้านที่เลือก
3. ลบร้านได้ (ลบข้อมูลทั้งหมดในร้านด้วย)
4. ถ้าเหลือร้านเดียว → ห้ามลบ
5. ทุกหน้าต้องกรองข้อมูลตามร้านที่เลือกอยู่
```

**✅ ทดสอบ:** สร้างร้านที่ 2 → กรอกข้อมูล → สลับร้าน → ข้อมูลต้องแยกกัน

---

## Phase 5 — ตั้งเป้าหมาย

```
ทำ Phase 5 ตาม project-brief.md ส่วน 2.4 (เป้าหมายรายเดือน):
1. ปุ่ม "🎯 ตั้งเป้าเดือนนี้" ในหน้าแดชบอร์ด → เปิด modal
2. ตั้งเป้ารายได้ + เป้ากำไร แยกกัน (ทั้งคู่ optional)
3. แสดง progress bar + % ที่ทำได้
4. ถึงเป้า ≥ 100% → แสดง "🎉 ถึงเป้าแล้ว!"
5. แก้ไข/ลบเป้าที่ตั้งไว้ได้
```

**✅ ทดสอบ:** ตั้งเป้า → ดู progress → กรอกข้อมูลเพิ่ม → progress อัปเดต → แก้ไขเป้า

---

## Phase 6 — หน้ารวมทุกร้าน

```
ทำ Phase 6 ตาม project-brief.md ส่วน 2.6:
1. หน้ารวมทุกร้าน — แสดงเฉพาะเมื่อมี ≥ 2 ร้าน
2. ตารางเปรียบเทียบร้าน (ยอดขาย, ค่าแอด, กำไร, ROAS, อัตรากำไร) + แถวรวม
3. กราฟแท่งเปรียบเทียบระหว่างร้าน
4. กราฟเส้นแนวโน้มทุกร้าน 6 เดือนย้อนหลัง
5. Tab "🏪 รวมร้าน" ใน bottom nav — ซ่อนถ้ามีร้านเดียว
```

**✅ ทดสอบ:** มี 2 ร้าน → เห็น tab + ตาราง + กราฟ → ลบร้านเหลือ 1 → tab หาย

---

## Phase 7 — สรุปประจำปี

```
ทำ Phase 7 ตาม project-brief.md ส่วน 2.7:
1. หน้าสรุปประจำปี — Dropdown เลือกปี (พ.ศ.)
2. การ์ดสรุปทั้งปี (ยอดขาย, ค่าแอด, กำไร, ROAS, เดือนขายดีสุด/แย่สุด)
3. ตารางเทียบ 12 เดือน (ยอดขาย, ค่าแอด, กำไร, ROAS, อัตรากำไร) + แถวรวม
4. กราฟแท่ง 12 เดือน (ยอดขาย, ค่าแอด, กำไร)
5. เพิ่ม tab "📅 รายปี" ใน bottom nav
```

**✅ ทดสอบ:** เลือกปี → เห็นตาราง 12 เดือน + กราฟ → เดือนที่ไม่มีข้อมูลแสดง 0

---

## Phase 8 — Export CSV

```
ทำ Phase 8 ตาม project-brief.md ส่วน 2.5 (Export):
1. ปุ่ม "📥 Export CSV" ในหน้าประวัติ
2. กดแล้วดาวน์โหลดไฟล์ CSV ของเดือนที่เลือก
3. ชื่อไฟล์: {ชื่อร้าน}_{ปี-เดือน}.csv
4. ต้องรองรับภาษาไทยเปิดใน Excel ได้ถูกต้อง (ใส่ BOM UTF-8)
```

**✅ ทดสอบ:** กดดาวน์โหลด → เปิดใน Excel → ภาษาไทยถูกต้อง → ข้อมูลครบ

---

## Phase 9 — Responsive + UX + Edge Cases + Review

```
ทำ Phase 9 — ตรวจสอบและ polish ทั้งระบบ:

1. ทดสอบ responsive ทุกหน้าบนหน้าจอ 360px (มือถือ)
2. ตรวจ edge cases ทั้งหมดตาม project-brief.md ส่วนที่ 6:
   - ค่าแอด = 0 → ROAS แสดง "–" ไม่พัง
   - ยังไม่มีข้อมูลเลย → หน้าว่างมีข้อความเชิญชวน
   - ร้านเดียว → ซ่อน tab รวมร้าน + ปุ่มลบร้าน disable
   - เดือนที่ไม่มีข้อมูล → แสดงหน้าว่างไม่ใช่ error
   - เลือกช่วงเวลากำหนดเอง วันเริ่ม > วันสิ้นสุด → แสดง error
3. ตรวจ security ตาม architecture.md ส่วนที่ 4.2 ทุกข้อ
4. ตรวจว่าทุกไฟล์ใช้ pattern Controller → Service → Repository ตาม architecture.md
5. ตรวจว่าไม่มี SQL นอก Repository
6. ใส่ toast, loading spinner, confirm dialog ให้ครบทุกจุด
7. อัปเดตเอกสารใน /docs/ ให้ครบทุกไฟล์

แก้ไขทุกอย่างที่ยังไม่ตรงกับ project-brief.md และ architecture.md
```

---

## 🔧 Prompt แก้บัก (ใช้เมื่อเจอปัญหา)

```
ล็อกอินแล้ว redirect ไม่ทำงาน ช่วยแก้หน่อย
```

```
กราฟไม่แสดงข้อมูล ช่วยตรวจสอบว่า API return ข้อมูลถูกต้องหรือเปล่า
```

```
ตรงนี้ไม่ตรงกับ project-brief.md ส่วนที่ 2.4 — การ์ดสรุปยังไม่มี % เปรียบเทียบเดือนก่อน ช่วยเพิ่มให้หน่อย
```

```
หน้า history ตาราง column ยังไม่ครบตาม project-brief.md ส่วน 2.5 ขาด ROAS กับเทียบเมื่อวาน ช่วยเพิ่ม
```

## 🔍 Prompt ตรวจคุณภาพโค้ด (ใช้หลังจบแต่ละ Phase)

```
ตรวจสอบโค้ดที่เพิ่งทำว่าตรงตาม architecture.md หรือยัง:
1. SQL อยู่ใน Repository เท่านั้นหรือเปล่า
2. Business logic อยู่ใน Service หรือเปล่า
3. Controller ทำแค่รับ request → เรียก Service → ส่ง View หรือเปล่า
4. ใช้ e() escape output ทุกจุดใน HTML หรือเปล่า
5. มี CSRF token ทุก POST form หรือเปล่า
ถ้ามีจุดไหนไม่ตรง ช่วยแก้ให้ตรงตาม architecture.md
```
